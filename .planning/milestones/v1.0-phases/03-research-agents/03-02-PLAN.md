---
phase: 03-research-agents
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - "{protoDir}/.watson/sections/{sectionName}/LAYOUT.md"
  - "{protoDir}/.watson/sections/{sectionName}/DESIGN.md"
autonomous: false
requirements: [LOUP-01, LOUP-02, LOUP-03, LOUP-05, LOUP-06]

must_haves:
  truths:
    - "Ported agents produce LAYOUT.md and DESIGN.md artifacts from a real Figma frame"
    - "Agents consume library book data via libraryPaths[] — chapter/page files from watson/library/"
    - "Output files land in .watson/sections/{sectionName}/ staging paths (not .loupe/)"
    - "Both layout and design agents dispatch in parallel (background mode) without serialization"
    - "Output artifacts conform to canonical schemas (LAYOUT-EXAMPLE.md, DESIGN-EXAMPLE.md)"
  artifacts:
    - path: ".watson/sections/{sectionName}/LAYOUT.md"
      provides: "Layout spec with token references from library books"
      contains: "Token Quick-Reference"
    - path: ".watson/sections/{sectionName}/DESIGN.md"
      provides: "Design spec with component and token mapping from library books"
      contains: "Unmapped Values"
  key_links:
    - from: "~/.claude/skills/watson/agents/layout.md"
      to: "watson/library/design-system/*/CHAPTER.md"
      via: "libraryPaths[] array resolved by subskill, read by agent"
      pattern: "libraryPaths"
    - from: "~/.claude/skills/watson/agents/design.md"
      to: "watson/library/design-system/*/CHAPTER.md"
      via: "libraryPaths[] array resolved by subskill, read by agent"
      pattern: "libraryPaths"
    - from: "layout.md + design.md output"
      to: ".watson/sections/"
      via: "staging directory write"
      pattern: "\\.watson/sections/"
---

<objective>
Integration smoke test: dispatch ported Watson agents against a real Figma frame to validate library book consumption and .watson/ staging output.

Purpose: Plan 03-01 ports agents mechanically. This plan verifies the port works end-to-end: agents read library book chapters via libraryPaths[], produce schema-compliant artifacts, write to .watson/sections/ staging, and run in parallel without serialization. This is the gate that confirms the port is functional, not just syntactically correct.

Output: Validated LAYOUT.md and DESIGN.md artifacts in .watson/sections/, human-approved.
</objective>

<execution_context>
@/Users/austindick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austindick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.0-phases/03-research-agents/03-CONTEXT.md
@.planning/milestones/v1.0-phases/03-research-agents/03-01-SUMMARY.md

<interfaces>
<!-- Library book structure available for libraryPaths[] resolution -->

From ~/.claude/skills/watson/library/LIBRARY.md:
Books: design-system (source-derived, 4 chapters), playground-conventions (foundational, 7 chapters)

design-system chapters (relevant for layout + design agents):
- library/design-system/global-theme/CHAPTER.md — spacing tokens, radius tokens, color tokens
- library/design-system/components/CHAPTER.md — FauxDS component variants, props, sizes

Watson agent contract output paths:
- Staging: {protoDir}/.watson/sections/{sectionName}/LAYOUT.md (< 80 lines)
- Staging: {protoDir}/.watson/sections/{sectionName}/DESIGN.md (< 80 lines)

Canonical schemas:
- .planning/artifact-schemas/LAYOUT-EXAMPLE.md — Token Quick-Reference, Component Tree, Annotated CSS
- .planning/artifact-schemas/DESIGN-EXAMPLE.md — Component Mapping, Typography, Color Tokens, Unmapped Values
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dispatch layout and design agents against a real Figma section with library books</name>
  <files>
    {protoDir}/.watson/sections/{sectionName}/LAYOUT.md
    {protoDir}/.watson/sections/{sectionName}/DESIGN.md
  </files>
  <action>
Run the ported Watson layout and design agents end-to-end against a real Figma frame section. This validates the full chain: libraryPaths[] resolution, Figma data fetch, token/component mapping, and .watson/ staging output.

**Step 1: Resolve libraryPaths[]**
Read ~/.claude/skills/watson/library/LIBRARY.md and ~/.claude/skills/watson/library/design-system/BOOK.md to identify relevant chapter paths. For the layout agent, resolve paths to chapters containing spacing/radius tokens. For the design agent, resolve paths to chapters containing components, color tokens, and typography presets. Build a libraryPaths[] array with absolute paths to the relevant CHAPTER.md files.

**Step 2: Get a test section**
Ask the user to provide a Figma frame URL. Run the Watson decomposer agent (agents/decomposer.md) to get the section list. Select one section with sufficient complexity (auto-layout, mixed spacing, component instances).

**Step 3: Dispatch both agents in parallel (background)**
Dispatch the Watson layout agent (agents/layout.md) and design agent (agents/design.md) simultaneously as background tasks against the selected section. Pass:
- nodeId: from decomposer output
- sectionName: from decomposer output
- libraryPaths: the resolved array from Step 1
- blueprintPath: the prototype's blueprint/ directory (for existing vocabulary — may not exist on first run)
- watsonMode: true

Both agents must run in background mode. Record wall-clock start time before dispatch and end time after both complete.

**Step 4: Validate outputs**
After both agents complete:
- Verify .watson/sections/{sectionName}/LAYOUT.md exists and is under 80 lines
- Verify .watson/sections/{sectionName}/DESIGN.md exists and is under 80 lines
- Check LAYOUT.md has all 3 required sections: Token Quick-Reference, Component Tree, Annotated CSS
- Check DESIGN.md has all 4 required sections: Component Mapping, Typography, Color Tokens, Unmapped Values
- Verify no .loupe/ paths appear in either output file
- Check that token references trace to library book data (not hardcoded FauxDS values)

**Step 5: Timing check**
Report the elapsed time for the parallel dispatch. Both agents should complete in approximately single-agent time (< 1.5x), not 2x (which would indicate serialization — bug #7406).
  </action>
  <verify>
    <automated>
# Verify output files exist and meet schema requirements
SECTIONS_DIR=$(find . -path "*/.watson/sections/*/LAYOUT.md" -print -quit 2>/dev/null | xargs dirname 2>/dev/null)
if [ -z "$SECTIONS_DIR" ]; then echo "FAIL: No .watson/sections/*/LAYOUT.md found"; exit 1; fi
LAYOUT="$SECTIONS_DIR/LAYOUT.md"
DESIGN="$SECTIONS_DIR/DESIGN.md"
if [ ! -f "$LAYOUT" ]; then echo "FAIL: LAYOUT.md missing"; exit 1; fi
if [ ! -f "$DESIGN" ]; then echo "FAIL: DESIGN.md missing"; exit 1; fi
LAYOUT_LINES=$(wc -l < "$LAYOUT")
DESIGN_LINES=$(wc -l < "$DESIGN")
if [ "$LAYOUT_LINES" -gt 80 ]; then echo "FAIL: LAYOUT.md is $LAYOUT_LINES lines (budget: 80)"; exit 1; fi
if [ "$DESIGN_LINES" -gt 80 ]; then echo "FAIL: DESIGN.md is $DESIGN_LINES lines (budget: 80)"; exit 1; fi
echo "PASS: LAYOUT.md ($LAYOUT_LINES lines), DESIGN.md ($DESIGN_LINES lines)"
    </automated>
  </verify>
  <done>
    - LAYOUT.md and DESIGN.md produced in .watson/sections/{sectionName}/
    - Both under 80-line budget
    - Both contain required schema sections
    - Agents consumed library books via libraryPaths[] (not hardcoded paths)
    - Parallel dispatch confirmed (timing approximately 1x, not 2x)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of ported agent output quality</name>
  <files>
    {protoDir}/.watson/sections/{sectionName}/LAYOUT.md
    {protoDir}/.watson/sections/{sectionName}/DESIGN.md
  </files>
  <action>
Present the LAYOUT.md and DESIGN.md output files to the user for quality review. Display:
1. The full LAYOUT.md content (Token Quick-Reference, Component Tree, Annotated CSS)
2. The full DESIGN.md content (Component Mapping, Typography, Color Tokens, Unmapped Values)
3. Parallel dispatch timing results
4. Confirmation that output paths are .watson/sections/ (not .loupe/sections/)
  </action>
  <verify>User types "approved" or describes issues to fix</verify>
  <done>User has confirmed: token references map to real library book data, component mappings are accurate, output is in .watson/ paths, parallel timing is acceptable</done>
</task>

</tasks>

<verification>
Phase 3 gate checks (all must pass):

1. All 7 agent files exist in watson/agents/ (from Plan 01)
2. Layout and design agents produce schema-compliant artifacts from real Figma data (from Plan 02 Task 1)
3. Agents consume library book chapters via libraryPaths[] — no hardcoded library paths
4. Output lands in .watson/sections/ staging paths
5. Parallel dispatch verified (timing check)
6. interaction.md is a documented placeholder with deferral notice (from Plan 01)
7. consolidator.md outputs to blueprint/ paths (from Plan 01)
8. Human approval of artifact quality (from Plan 02 Task 2)
</verification>

<success_criteria>
- Both LAYOUT.md and DESIGN.md produced from real Figma data using Watson agents
- Library book consumption via libraryPaths[] confirmed working
- .watson/sections/ staging paths used correctly
- Parallel dispatch timing acceptable (not serialized)
- Human approved artifact quality
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/03-research-agents/03-02-SUMMARY.md`
</output>
