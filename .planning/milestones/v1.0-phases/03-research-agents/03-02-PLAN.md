---
phase: 03-research-agents
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/skills/watson/agents/builder.md
  - ~/.claude/skills/watson/agents/reviewer.md
  - ~/.claude/skills/watson/agents/consolidator.md
  - ~/.claude/skills/watson/agents/interaction.md
autonomous: true
requirements: [LOUP-01, LOUP-03, LOUP-06]

must_haves:
  truths:
    - "Builder agent reads LAYOUT.md + DESIGN.md from .watson/sections/ and implements code using library-derived tokens and components"
    - "Reviewer agent audits built code against .watson/sections/ spec files and fixes discrepancies in-place"
    - "Consolidator agent merges all .watson/sections/ artifacts into blueprint/ files and cleans up staging"
    - "Interaction agent exists as a documented placeholder noting deferral to post-v1 (per CONTEXT.md locked decision)"
    - "No agent file contains hardcoded FauxDS references"
  artifacts:
    - path: "~/.claude/skills/watson/agents/builder.md"
      provides: "Code generation from spec files"
      contains: "watson:builder"
    - path: "~/.claude/skills/watson/agents/reviewer.md"
      provides: "Spec compliance audit and in-place fixes"
      contains: "watson:reviewer"
    - path: "~/.claude/skills/watson/agents/consolidator.md"
      provides: "Section-to-blueprint merge and staging cleanup"
      contains: "watson:consolidator"
    - path: "~/.claude/skills/watson/agents/interaction.md"
      provides: "Placeholder for deferred interaction agent"
      contains: "watson:interaction"
  key_links:
    - from: "agents/builder.md"
      to: ".watson/sections/{sectionName}/LAYOUT.md"
      via: "reads section-level layout spec"
      pattern: "\\.watson/sections/"
    - from: "agents/builder.md"
      to: "libraryPaths[]"
      via: "reads library chapters for component variant validation"
      pattern: "libraryPaths"
    - from: "agents/reviewer.md"
      to: ".watson/sections/{sectionName}/"
      via: "reads section spec files for property-by-property audit"
      pattern: "\\.watson/sections/"
    - from: "agents/consolidator.md"
      to: "blueprint/"
      via: "writes consolidated LAYOUT.md and DESIGN.md to blueprintPath"
      pattern: "blueprint/"
    - from: "agents/consolidator.md"
      to: ".watson/sections/"
      via: "reads then deletes staging directory after verification"
      pattern: "\\.watson/sections/"
---

<objective>
Port the four build/merge pipeline agents (builder, reviewer, consolidator, interaction) from Loupe to Watson contract format.

Purpose: These agents are the "output" side of the pipeline — they consume section-level spec files to build code, review it, and consolidate section artifacts into blueprint files. The interaction agent is a documented placeholder per the locked deferral decision.

Output: Four agent .md files in watson/agents/ conforming to the agent contract spec.
</objective>

<execution_context>
@/Users/austindick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austindick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

## Source agents (copy from these, then adapt)
@~/.claude/skills/loupe/agents/04-builder.md
@~/.claude/skills/loupe/agents/05-reviewer.md
@~/.claude/skills/loupe/agents/06-consolidator.md
@~/.claude/skills/loupe/agents/03-interactions.md

## Watson contract and conventions
@~/.claude/skills/watson/references/agent-contract.md
@~/.claude/skills/watson/agents/librarian.md (frontmatter convention reference)

## Phase decisions
@.planning/milestones/v1.0-phases/03-research-agents/03-CONTEXT.md

<interfaces>
<!-- Watson Agent Contract — shared parameters -->
Shared params: blueprintPath (string), libraryPaths (string[]), watsonMode (boolean)
Pipeline params: sectionName (string)

<!-- Builder specific: nodeId NOT needed — builder works from spec files -->
<!-- Builder: layoutPath, designPath, interactionPath?, targetFilePath, sectionScope -->
<!-- Reviewer: layoutPath, designPath, sourceFilePath, sectionScope -->
<!-- Consolidator: sectionsGlob (string) -->
<!-- Interaction: nodeId, interactionContext? -->

<!-- Output paths (from agent-contract.md): -->
<!-- builder: modified targetFilePath -->
<!-- reviewer: modified sourceFilePath + conversation summary -->
<!-- consolidator: blueprint/LAYOUT.md, blueprint/DESIGN.md, blueprint/INTERACTION.md -->
<!-- interaction: .watson/sections/{sectionName}/INTERACTION.md (deferred) -->

<!-- Staging paths: .watson/sections/ (not .loupe/sections/) -->
<!-- Blueprint paths: {protoDir}/blueprint/ (not .loupe/) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port builder and reviewer agents to Watson contract</name>
  <files>~/.claude/skills/watson/agents/builder.md, ~/.claude/skills/watson/agents/reviewer.md</files>
  <action>
**Builder (copy from loupe/agents/04-builder.md -> watson/agents/builder.md):**

Frontmatter:
```yaml
name: watson:builder
dispatch_mode: background
inputs:
  shared:
    - blueprintPath: "string — absolute path to prototype's blueprint/ directory"
    - libraryPaths: "string[] — pre-resolved chapter/page paths for component variant validation"
    - watsonMode: "boolean — suppress interactive prompts when true"
  agent_specific:
    - layoutPath: "string — path to .watson/sections/{sectionName}/LAYOUT.md"
    - designPath: "string — path to .watson/sections/{sectionName}/DESIGN.md"
    - interactionPath: "string? — path to .watson/sections/{sectionName}/INTERACTION.md (optional)"
    - targetFilePath: "string — path to prototype file to modify"
    - sectionScope: "string — section name or 'all'"
    - sectionName: "string — section name from decomposer output"
outputs:
  - "modified targetFilePath"
```

Body changes:
1. Replace `.loupe/sections/` with `.watson/sections/` everywhere
2. Replace `libraryPath` (single) with `libraryPaths` (array) — Step 2 reads ALL files in libraryPaths[] to load component and token reference data
3. Remove hardcoded FauxDS component names — replace "FauxDS component" with "design system component from library". Replace `--faux-*` token references with "design system tokens from library"
4. Keep the TODO comment format for unmapped values but make it generic: `{/* TODO: unmapped — closest DS match: [ComponentName] (gap: [explanation]); raw: prop="value" */}`
5. Keep all execution logic: section boundary detection, protected zones, compile verification
6. File under 2,500 tokens

**Reviewer (copy from loupe/agents/05-reviewer.md -> watson/agents/reviewer.md):**

Frontmatter:
```yaml
name: watson:reviewer
dispatch_mode: background
inputs:
  shared:
    - blueprintPath: "string — absolute path to prototype's blueprint/ directory"
    - libraryPaths: "string[] — pre-resolved chapter/page paths"
    - watsonMode: "boolean — suppress interactive prompts when true"
  agent_specific:
    - layoutPath: "string — path to .watson/sections/{sectionName}/LAYOUT.md"
    - designPath: "string — path to .watson/sections/{sectionName}/DESIGN.md"
    - interactionPath: "string? — path to .watson/sections/{sectionName}/INTERACTION.md (optional)"
    - sourceFilePath: "string — the built source file from builder agent"
    - sectionScope: "string — the section name for locating the region to review"
    - sectionName: "string — section name from decomposer output"
outputs:
  - "modified sourceFilePath (discrepancies fixed in-place)"
  - "summary report in conversation"
```

Body changes:
1. Replace `.loupe/sections/` with `.watson/sections/` everywhere
2. Remove hardcoded FauxDS references — generalize "FauxDS component" to "design system component"
3. Generalize token references: `--faux-*` becomes "design system tokens"
4. Keep all execution logic: mechanical checklist generation, 2-pass fix strategy, ESCALATE rules, summary report format
5. File under 2,500 tokens
  </action>
  <verify>
    <automated>for f in builder reviewer; do echo "=== $f ==="; grep -c "faux\|FauxButton\|FauxBadge" ~/.claude/skills/watson/agents/$f.md && echo "FAIL: FauxDS refs" || echo "PASS"; grep -c "\.loupe" ~/.claude/skills/watson/agents/$f.md && echo "FAIL: .loupe refs" || echo "PASS"; grep -c "watson:" ~/.claude/skills/watson/agents/$f.md; done</automated>
    Verify: Both files contain "watson:" in frontmatter name.
    Verify: Both files contain "libraryPaths" (not "libraryPath" singular without the s).
    Verify: Both files reference ".watson/sections/" paths.
  </verify>
  <done>builder.md and reviewer.md exist at watson/agents/ with Watson frontmatter, .watson/sections/ paths, libraryPaths[], zero FauxDS hardcoding, all review/build logic preserved</done>
</task>

<task type="auto">
  <name>Task 2: Port consolidator agent and create interaction placeholder</name>
  <files>~/.claude/skills/watson/agents/consolidator.md, ~/.claude/skills/watson/agents/interaction.md</files>
  <action>
**Consolidator (copy from loupe/agents/06-consolidator.md -> watson/agents/consolidator.md):**

Frontmatter:
```yaml
name: watson:consolidator
dispatch_mode: background
inputs:
  shared:
    - blueprintPath: "string — absolute path to prototype's blueprint/ directory"
    - libraryPaths: "string[] — pre-resolved chapter/page paths"
    - watsonMode: "boolean — suppress interactive prompts when true"
  agent_specific:
    - sectionsGlob: "string — glob path to .watson/sections/ directory"
    - sectionName: "string — section name from decomposer output"
outputs:
  - "blueprint/LAYOUT.md (consolidated)"
  - "blueprint/DESIGN.md (consolidated)"
  - "blueprint/INTERACTION.md (consolidated, when interaction agent is active)"
  - ".watson/sections/ cleaned up after successful consolidation"
```

Body changes:
1. Replace ALL `.loupe/` paths with `.watson/` — this includes `.loupe/sections/`, `.loupe/LAYOUT.md`, `.loupe/DESIGN.md`
2. **Consolidated output paths:** The Loupe version writes to `.loupe/LAYOUT.md` and `.loupe/DESIGN.md`. Watson consolidator writes to `blueprint/LAYOUT.md` and `blueprint/DESIGN.md` inside the prototype directory (using blueprintPath to resolve the absolute path).
3. Existing consolidated files: Replace "existing .loupe/LAYOUT.md" with "existing blueprint/LAYOUT.md at blueprintPath/LAYOUT.md"
4. Cleanup path: `.watson/sections/` instead of `.loupe/sections/`
5. Remove "Loupe Agent 06" from the header comment — use "Watson Consolidator"
6. Keep the No INTERACTIONS.md constraint for now (matches current state — Agent 3 deferred). But add a note: "When the interaction agent is active in a future version, also consolidate .watson/sections/*/INTERACTION.md into blueprint/INTERACTION.md."
7. Remove any `--faux-*` specific references if present — use generic "design system tokens"
8. Keep all merge logic: union, dedup, conflict flagging, 80-line soft budget
9. File under 2,500 tokens

**Interaction placeholder (create watson/agents/interaction.md):**

Create a minimal placeholder file that documents the deferral:

```yaml
name: watson:interaction
dispatch_mode: foreground
inputs:
  shared:
    - blueprintPath: "string — absolute path to prototype's blueprint/ directory"
    - libraryPaths: "string[] — pre-resolved chapter/page paths"
    - watsonMode: "boolean — suppress interactive prompts when true"
  agent_specific:
    - nodeId: "string — Figma section nodeId"
    - interactionContext: "string? — pre-gathered context from discuss (skips interview when provided)"
    - sectionName: "string — section name from decomposer output"
outputs:
  - "{protoDir}/.watson/sections/{sectionName}/INTERACTION.md (< 50 lines)"
```

Body: A short (under 30 lines total) file that states:
- Role: Gather or infer interaction and state context for a section
- Status: Deferred to post-v1 per Phase 3 context decision
- When active: Will support three modes — (A) interactive interview, (B) pre-gathered context from discuss, (C) infer-only from Figma variants
- Output format reference: @references/artifact-schemas/INTERACTION-EXAMPLE.md
- The file exists so the agent registry in agent-contract.md has a corresponding file and downstream pipeline code can check for its existence
  </action>
  <verify>
    <automated>for f in consolidator interaction; do echo "=== $f ==="; test -f ~/.claude/skills/watson/agents/$f.md && echo "EXISTS" || echo "MISSING"; grep -c "watson:" ~/.claude/skills/watson/agents/$f.md 2>/dev/null; done</automated>
    Verify: grep -c "\.loupe" ~/.claude/skills/watson/agents/consolidator.md returns 0.
    Verify: grep -c "blueprint/" ~/.claude/skills/watson/agents/consolidator.md returns at least 2.
    Verify: grep -c "\.watson/sections/" ~/.claude/skills/watson/agents/consolidator.md returns at least 2.
    Verify: grep -ci "deferred\|post-v1" ~/.claude/skills/watson/agents/interaction.md returns at least 1.
    Verify: wc -l ~/.claude/skills/watson/agents/interaction.md returns under 40 lines.
  </verify>
  <done>consolidator.md writes to blueprint/ paths (not .loupe/), reads from .watson/sections/, zero FauxDS hardcoding. interaction.md exists as documented placeholder noting post-v1 deferral with correct Watson frontmatter.</done>
</task>

</tasks>

<verification>
All four agent files exist at ~/.claude/skills/watson/agents/:
```bash
ls ~/.claude/skills/watson/agents/{builder,reviewer,consolidator,interaction}.md
```

Zero FauxDS hardcoding:
```bash
grep -rn "faux\|FauxButton\|FauxBadge\|FauxCard" ~/.claude/skills/watson/agents/{builder,reviewer,consolidator,interaction}.md
# Expected: no output
```

Zero .loupe references:
```bash
grep -rn "\.loupe" ~/.claude/skills/watson/agents/{builder,reviewer,consolidator,interaction}.md
# Expected: no output
```

Consolidator writes to blueprint/ (not .loupe/):
```bash
grep -c "blueprint/" ~/.claude/skills/watson/agents/consolidator.md
# Expected: at least 2
```

All seven Watson agents now present:
```bash
ls ~/.claude/skills/watson/agents/
# Expected: builder.md consolidator.md decomposer.md design.md interaction.md layout.md librarian.md
```
</verification>

<success_criteria>
- Four agent .md files created at watson/agents/ with Watson contract frontmatter
- Builder and reviewer reference .watson/sections/ staging paths and libraryPaths[]
- Consolidator writes consolidated output to blueprint/ (not .loupe/) and cleans .watson/sections/
- Interaction agent is a documented placeholder noting post-v1 deferral
- Zero hardcoded FauxDS references across all four files
- All seven pipeline agents (including librarian from Phase 2) now exist in watson/agents/
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/03-research-agents/03-02-SUMMARY.md`
</output>
