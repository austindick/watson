---
phase: 03-research-agents
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/skills/watson/agents/decomposer.md
  - ~/.claude/skills/watson/agents/layout.md
  - ~/.claude/skills/watson/agents/design.md
autonomous: true
requirements: [LOUP-01, LOUP-02]  # LOUP-03 staging only; consolidation to blueprint/ is in Plan 02

must_haves:
  truths:
    - "Decomposer agent accepts a Figma URL and produces a sections[] JSON list using Watson contract parameters"
    - "Layout agent reads pre-resolved libraryPaths[] chapters, fetches Figma section data, and produces LAYOUT.md at .watson/sections/{sectionName}/LAYOUT.md"
    - "Design agent reads pre-resolved libraryPaths[] chapters, fetches Figma section data, and produces DESIGN.md at .watson/sections/{sectionName}/DESIGN.md"
    - "No agent file contains hardcoded FauxDS references — all design system vocabulary comes from libraryPaths at runtime"
    - "All three agents have Watson contract frontmatter (watson:name, dispatch_mode, shared + agent_specific inputs)"
  artifacts:
    - path: "~/.claude/skills/watson/agents/decomposer.md"
      provides: "Section decomposition from Figma frame"
      contains: "watson:decomposer"
    - path: "~/.claude/skills/watson/agents/layout.md"
      provides: "Layout extraction and token mapping"
      contains: "watson:layout"
    - path: "~/.claude/skills/watson/agents/design.md"
      provides: "Design token and component mapping"
      contains: "watson:design"
  key_links:
    - from: "agents/decomposer.md"
      to: "mcp__figma__get_metadata"
      via: "Figma MCP call for node tree"
      pattern: "mcp__figma__get_metadata"
    - from: "agents/layout.md"
      to: "libraryPaths[]"
      via: "reads pre-resolved chapter paths for spacing/radius tokens"
      pattern: "libraryPaths"
    - from: "agents/design.md"
      to: "libraryPaths[]"
      via: "reads pre-resolved chapter paths for components/colors/typography"
      pattern: "libraryPaths"
    - from: "agents/layout.md"
      to: ".watson/sections/{sectionName}/LAYOUT.md"
      via: "writes section-level staging artifact"
      pattern: "\\.watson/sections/"
    - from: "agents/design.md"
      to: ".watson/sections/{sectionName}/DESIGN.md"
      via: "writes section-level staging artifact"
      pattern: "\\.watson/sections/"
---

<objective>
Port the three research/analysis pipeline agents (decomposer, layout, design) from Loupe to Watson contract format.

Purpose: These agents are the "input" side of the pipeline — they read Figma data and produce section-level LAYOUT.md and DESIGN.md artifacts. Porting them to Watson contract means they accept libraryPaths[] (pre-resolved chapter/page paths) instead of a single libraryPath, write to .watson/sections/ instead of .loupe/sections/, and have Watson-style frontmatter.

Output: Three complete agent .md files in watson/agents/ conforming to the agent contract spec.
</objective>

<execution_context>
@/Users/austindick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austindick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

## Source agents (copy from these, then adapt)
@~/.claude/skills/loupe/agents/00-decomposer.md
@~/.claude/skills/loupe/agents/01-layout.md
@~/.claude/skills/loupe/agents/02-tokens.md

## Watson contract and conventions
@~/.claude/skills/watson/references/agent-contract.md
@~/.claude/skills/watson/agents/librarian.md (frontmatter convention reference)

## Canonical output schemas
@~/.claude/skills/watson/references/artifact-schemas/LAYOUT-EXAMPLE.md
@~/.claude/skills/watson/references/artifact-schemas/DESIGN-EXAMPLE.md

## Phase research
@.planning/milestones/v1.0-phases/03-research-agents/03-RESEARCH.md
@.planning/milestones/v1.0-phases/03-research-agents/03-CONTEXT.md

<interfaces>
<!-- Watson Agent Contract — shared parameters all agents receive -->
Shared params: blueprintPath (string), libraryPaths (string[]), watsonMode (boolean)
Pipeline params: sectionName (string)

<!-- Decomposer specific: figmaUrl (string) -->
<!-- Layout specific: nodeId (string) -->
<!-- Design specific: nodeId (string) -->

<!-- Output paths (from agent-contract.md): -->
<!-- decomposer: sections[] JSON inline -->
<!-- layout: {protoDir}/.watson/sections/{sectionName}/LAYOUT.md -->
<!-- design: {protoDir}/.watson/sections/{sectionName}/DESIGN.md -->

<!-- Watson frontmatter convention (from librarian.md): -->
<!-- name: watson:{agentName} -->
<!-- dispatch_mode: foreground | background -->
<!-- inputs: { shared: [...], agent_specific: [...] } -->
<!-- outputs: [...] -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port decomposer agent to Watson contract</name>
  <files>~/.claude/skills/watson/agents/decomposer.md</files>
  <action>
Copy ~/.claude/skills/loupe/agents/00-decomposer.md to ~/.claude/skills/watson/agents/decomposer.md, then adapt:

**Frontmatter:** Replace Loupe frontmatter with Watson convention:
```yaml
name: watson:decomposer
dispatch_mode: foreground
inputs:
  shared:
    - blueprintPath: "string — absolute path to prototype's blueprint/ directory"
    - libraryPaths: "string[] — pre-resolved chapter/page paths"
    - watsonMode: "boolean — suppress interactive prompts when true"
  agent_specific:
    - figmaUrl: "string — full Figma frame URL including node-id param"
outputs:
  - "sections[] JSON inline (each entry: name, nodeId, dimensions, parent?)"
```

**Body changes:**
1. Replace all `.loupe/` path references with `.watson/` (there may not be any in decomposer since it outputs JSON inline, but check)
2. Replace `library.md` / `fauxds-library.md` references with `libraryPaths[]` if present (decomposer likely doesn't reference these)
3. Keep all Critical Constraints, Execution steps, Output Format exactly as-is — decomposer logic is identical between Loupe and Watson
4. The decomposer does NOT use libraryPaths — it only reads Figma metadata. The shared params are passed for contract consistency but not consumed.
5. Keep the file under 2,500 tokens
6. Remove any references to "Loupe" — replace with "Watson" where the skill name appears

**What NOT to change:**
- MCP tool names (mcp__figma__get_metadata is correct)
- Section tag detection logic (::section, ::subsection)
- Approval flow and recovery paths
- Output JSON format ({name, nodeId, dimensions, parent?})
  </action>
  <verify>
    <automated>wc -w ~/.claude/skills/watson/agents/decomposer.md | awk '{if ($1 > 2500) print "FAIL: over 2500 words"; else print "PASS"}'</automated>
    Verify: grep -c "watson:decomposer" ~/.claude/skills/watson/agents/decomposer.md returns 1.
    Verify: grep -c "libraryPaths" ~/.claude/skills/watson/agents/decomposer.md returns at least 1 (in frontmatter).
    Verify: grep -c "\.loupe" ~/.claude/skills/watson/agents/decomposer.md returns 0.
    Verify: grep -c "fauxds-library" ~/.claude/skills/watson/agents/decomposer.md returns 0.
  </verify>
  <done>decomposer.md exists at watson/agents/ with Watson frontmatter, no .loupe references, no hardcoded FauxDS references, all Loupe execution logic preserved</done>
</task>

<task type="auto">
  <name>Task 2: Port layout agent to Watson contract</name>
  <files>~/.claude/skills/watson/agents/layout.md</files>
  <action>
Copy ~/.claude/skills/loupe/agents/01-layout.md to ~/.claude/skills/watson/agents/layout.md, then adapt:

**Frontmatter:** Replace with Watson convention:
```yaml
name: watson:layout
dispatch_mode: background
inputs:
  shared:
    - blueprintPath: "string — absolute path to prototype's blueprint/ directory"
    - libraryPaths: "string[] — pre-resolved chapter/page paths for spacing tokens, radius tokens"
    - watsonMode: "boolean — suppress interactive prompts when true"
  agent_specific:
    - nodeId: "string — Figma section nodeId from decomposer"
    - sectionName: "string — section name from decomposer output"
outputs:
  - "{protoDir}/.watson/sections/{sectionName}/LAYOUT.md (< 80 lines)"
```

**Critical body changes:**
1. **libraryPaths[] instead of libraryPath:** Step 1 currently reads "Read library.md at libraryPath". Change to: "Read each file in libraryPaths[]. Look for spacing tokens (--*-spacing-*) and radius tokens (--*-radius-*) across all provided chapter/page files. The token names will vary by design system — do not assume --faux-* prefix."
2. **Source-agnostic token references:** The Loupe version has an inlined spacing token lookup table with `--faux-spacing-1` through `--faux-spacing-12`. This must be REMOVED. Instead, instruct the agent to build the spacing-to-token mapping dynamically from whatever tokens are found in the libraryPaths files. The agent reads the token tables from the library chapters at runtime.
3. **Output paths:** Replace `.loupe/sections/[sectionName]/LAYOUT.md` with `.watson/sections/[sectionName]/LAYOUT.md` everywhere (Step 10 and Output Format section).
4. **Existing vocabulary path:** Replace "existing .loupe/LAYOUT.md" with "existing blueprint/LAYOUT.md at blueprintPath" — the Watson equivalent of vocabulary context is the consolidated blueprint file.
5. **Remove all `--faux-*` hardcoded references:** The Loupe version mentions `--faux-spacing-*` and `--faux-radius-*` by name. Replace with generic descriptions: "spacing tokens from library" and "radius tokens from library". The actual token prefixes come from the library book content.
6. **Artifact schema reference:** Change from `.planning/artifact-schemas/LAYOUT-EXAMPLE.md` to `@references/artifact-schemas/LAYOUT-EXAMPLE.md` (Watson-relative path).
7. Keep all execution logic (fetch strategy, overflow fallback, component tree, annotated CSS, 80-line budget) intact.
8. Keep the file under 2,500 tokens.
  </action>
  <verify>
    <automated>grep -c "faux" ~/.claude/skills/watson/agents/layout.md | awk '{if ($1 > 0) print "FAIL: contains faux references"; else print "PASS: source-agnostic"}'</automated>
    Verify: grep -c "watson:layout" returns 1.
    Verify: grep -c "libraryPaths" returns at least 2 (frontmatter + body).
    Verify: grep -c "\.loupe" returns 0.
    Verify: grep -c "\.watson/sections/" returns at least 1.
    Verify: grep -c "blueprintPath" returns at least 2 (frontmatter + existing vocabulary step).
  </verify>
  <done>layout.md exists at watson/agents/ with Watson frontmatter, reads libraryPaths[] for token vocabulary, writes to .watson/sections/, references blueprintPath for existing vocabulary, zero FauxDS hardcoding</done>
</task>

<task type="auto">
  <name>Task 3: Port design agent to Watson contract</name>
  <files>~/.claude/skills/watson/agents/design.md</files>
  <action>
Copy ~/.claude/skills/loupe/agents/02-tokens.md to ~/.claude/skills/watson/agents/design.md, then adapt:

**Frontmatter:** Replace with Watson convention:
```yaml
name: watson:design
dispatch_mode: background
inputs:
  shared:
    - blueprintPath: "string — absolute path to prototype's blueprint/ directory"
    - libraryPaths: "string[] — pre-resolved chapter/page paths for components, color tokens, typography presets"
    - watsonMode: "boolean — suppress interactive prompts when true"
  agent_specific:
    - nodeId: "string — Figma section nodeId from decomposer"
    - sectionName: "string — section name from decomposer output"
outputs:
  - "{protoDir}/.watson/sections/{sectionName}/DESIGN.md (< 80 lines)"
```

**Critical body changes:**
1. **libraryPaths[] instead of libraryPath:** Step 1 currently reads "Read library.md at libraryPath. Load all three sections: Components, Color tokens, Typography." Change to: "Read each file in libraryPaths[]. Collect: (1) component definitions with variants, sizes, and props; (2) color token definitions with exact hex values; (3) typography preset definitions with font-size and font-weight. The token naming convention will vary by design system."
2. **Source-agnostic component references:** Remove all mentions of specific FauxDS components (FauxButton, FauxBadge, FauxCard, FauxInput, FauxStack). Instead: "Match each visual element against the component definitions found in libraryPaths. Record the component name, variant, and props as defined in the library."
3. **Source-agnostic token references:** Remove `--faux-color-*`, `--slate-*`, `--faux-type-*` hardcoded prefixes. Instead: "Use whatever token names are defined in the library chapters. The exact prefix varies by design system."
4. **Color matching rule preserved but generalized:** Keep the EXACT hex match rule (this is a locked decision from CONTEXT.md). But describe it as: "Color matching is EXACT hex only — compare each Figma hex value (normalized to lowercase #rrggbb) against the color tokens found in libraryPaths. Non-matches go to Unmapped Values."
5. **Output paths:** Replace `.loupe/sections/[sectionName]/DESIGN.md` with `.watson/sections/[sectionName]/DESIGN.md` everywhere.
6. **Existing vocabulary path:** Replace "existing .loupe/DESIGN.md" with "existing blueprint/DESIGN.md at blueprintPath".
7. **Unmapped Values section:** Keep the locked decision — always present, write `_None_` if empty. Keep the format with CSS approach hints for custom elements.
8. **Artifact schema reference:** Change to `@references/artifact-schemas/DESIGN-EXAMPLE.md`.
9. Keep the file under 2,500 tokens.
  </action>
  <verify>
    <automated>grep -c "faux\|FauxButton\|FauxBadge\|FauxCard\|FauxInput\|FauxStack" ~/.claude/skills/watson/agents/design.md | awk '{if ($1 > 0) print "FAIL: contains FauxDS references"; else print "PASS: source-agnostic"}'</automated>
    Verify: grep -c "watson:design" returns 1.
    Verify: grep -c "libraryPaths" returns at least 2.
    Verify: grep -c "\.loupe" returns 0.
    Verify: grep -c "\.watson/sections/" returns at least 1.
    Verify: grep -c "Unmapped Values" returns at least 2 (constraint + output format).
    Verify: grep -ci "exact hex" returns at least 1.
  </verify>
  <done>design.md exists at watson/agents/ with Watson frontmatter, reads libraryPaths[] for component/token vocabulary, writes to .watson/sections/, enforces exact hex color matching, Unmapped Values always present, zero FauxDS hardcoding</done>
</task>

</tasks>

<verification>
All three agent files exist at ~/.claude/skills/watson/agents/:
- `decomposer.md` — Watson frontmatter, no .loupe references
- `layout.md` — Watson frontmatter, libraryPaths[], .watson/sections/ output, source-agnostic
- `design.md` — Watson frontmatter, libraryPaths[], .watson/sections/ output, source-agnostic, exact hex matching

Zero FauxDS hardcoding across all three files:
```bash
grep -rn "faux\|FauxButton\|FauxBadge\|FauxCard" ~/.claude/skills/watson/agents/{decomposer,layout,design}.md
# Expected: no output
```

All files reference libraryPaths (not libraryPath singular):
```bash
grep -c "libraryPaths" ~/.claude/skills/watson/agents/{decomposer,layout,design}.md
# Expected: at least 1 per file
```
</verification>

<success_criteria>
- Three agent .md files created at watson/agents/ with Watson contract frontmatter
- All agents accept libraryPaths[] (array, not single path)
- Layout and design agents write to .watson/sections/{sectionName}/ staging paths
- Zero hardcoded FauxDS references — all design system vocabulary loaded from libraryPaths at runtime
- Each file under 2,500 tokens
- Execution logic preserved from Loupe originals (MCP tool names, overflow fallback, line budgets, output schemas)
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/03-research-agents/03-01-SUMMARY.md`
</output>
