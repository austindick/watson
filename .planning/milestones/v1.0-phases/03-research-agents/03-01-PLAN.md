---
phase: 03-research-agents
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/skills/watson/agents/decomposer.md
  - ~/.claude/skills/watson/agents/layout.md
  - ~/.claude/skills/watson/agents/design.md
  - ~/.claude/skills/watson/agents/interaction.md
  - ~/.claude/skills/watson/agents/builder.md
  - ~/.claude/skills/watson/agents/reviewer.md
  - ~/.claude/skills/watson/agents/consolidator.md
autonomous: true
requirements: [LOUP-01, LOUP-02, LOUP-03, LOUP-04, LOUP-05, LOUP-06]

must_haves:
  truths:
    - "All 7 agent files exist in watson/agents/ with Watson-contract-compliant frontmatter"
    - "Every agent accepts libraryPaths[] (array) not libraryPath (string) and reads each path directly"
    - "Every agent writes output to .watson/sections/ staging paths (not .loupe/sections/)"
    - "Agents reference blueprintPath for existing vocabulary reads (not .loupe/LAYOUT.md or .loupe/DESIGN.md)"
    - "Agent 3 (interaction.md) is a documented placeholder noting post-v1 deferral, not a full implementation"
    - "Consolidator outputs to blueprint/LAYOUT.md and blueprint/DESIGN.md (not .loupe/ paths) and cleans .watson/sections/"
    - "No agent file contains hardcoded paths to fauxds-library.md or any specific library book"
  artifacts:
    - path: "~/.claude/skills/watson/agents/decomposer.md"
      provides: "Figma frame decomposition into named sections"
      contains: "libraryPaths"
    - path: "~/.claude/skills/watson/agents/layout.md"
      provides: "Figma auto-layout to LAYOUT.md spec"
      contains: "libraryPaths"
    - path: "~/.claude/skills/watson/agents/design.md"
      provides: "Figma visual styles to DESIGN.md spec"
      contains: "libraryPaths"
    - path: "~/.claude/skills/watson/agents/interaction.md"
      provides: "Documented placeholder — deferred to post-v1"
      contains: "deferred"
    - path: "~/.claude/skills/watson/agents/builder.md"
      provides: "Spec-to-code builder"
      contains: "libraryPaths"
    - path: "~/.claude/skills/watson/agents/reviewer.md"
      provides: "Property-by-property review and fix"
      contains: ".watson/sections"
    - path: "~/.claude/skills/watson/agents/consolidator.md"
      provides: "Section staging merge to blueprint files"
      contains: "blueprint/"
  key_links:
    - from: "~/.claude/skills/watson/agents/layout.md"
      to: "libraryPaths[]"
      via: "Step 1 reads each path in libraryPaths array"
      pattern: "libraryPaths"
    - from: "~/.claude/skills/watson/agents/consolidator.md"
      to: "blueprint/"
      via: "Step 5 writes to blueprint/LAYOUT.md and blueprint/DESIGN.md"
      pattern: "blueprint/"
    - from: "~/.claude/skills/watson/agents/builder.md"
      to: ".watson/sections/"
      via: "reads layoutPath and designPath from staging"
      pattern: "\\.watson/sections/"
---

<objective>
Port all 7 Loupe pipeline agents to Watson contract and validate contract compliance.

Purpose: The Loupe agents (757 lines across 7 files) are proven working. This is a mechanical port that translates them to Watson's agent contract: renamed files (no numeric prefix), libraryPaths[] array instead of libraryPath string, .watson/ instead of .loupe/ paths, blueprintPath instead of hardcoded vocabulary paths, and source-agnostic library book reading instead of fauxds-library.md references. Agent 3 (interaction) becomes a documented placeholder per CONTEXT.md locked decision.

Output: 7 agent .md files in ~/.claude/skills/watson/agents/ conforming to the Watson agent contract spec.
</objective>

<execution_context>
@/Users/austindick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austindick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.0-phases/03-research-agents/03-CONTEXT.md
@.planning/milestones/v1.0-phases/03-research-agents/03-RESEARCH.md

<interfaces>
<!-- Watson agent contract — the target spec all ported agents must conform to -->

From ~/.claude/skills/watson/references/agent-contract.md:

Shared Parameters (all agents):
| Parameter | Type | Description |
|-----------|------|-------------|
| blueprintPath | string | Absolute path to prototype's blueprint/ directory |
| libraryPaths | string[] | Pre-resolved chapter/page paths. Agents read each path directly. |
| watsonMode | boolean | Suppress interactive prompts when true |

Pipeline Parameters (layout, design, interaction, builder, reviewer, consolidator):
| Parameter | Type | Description |
|-----------|------|-------------|
| sectionName | string | Section name from decomposer output |

Agent Registry (Watson naming, no numeric prefix):
| Agent | File | Dispatch | Agent-Specific Params | Outputs |
|-------|------|----------|-----------------------|---------|
| decomposer | agents/decomposer.md | foreground | figmaUrl | sections[] JSON |
| layout | agents/layout.md | background | nodeId | .watson/sections/{sectionName}/LAYOUT.md |
| design | agents/design.md | background | nodeId | .watson/sections/{sectionName}/DESIGN.md |
| interaction | agents/interaction.md | foreground* | nodeId, interactionContext? | .watson/sections/{sectionName}/INTERACTION.md |
| builder | agents/builder.md | background | layoutPath, designPath, interactionPath?, targetFilePath, sectionScope | modified targetFilePath |
| reviewer | agents/reviewer.md | background | layoutPath, designPath, sourceFilePath, sectionScope | in-place fixes |
| consolidator | agents/consolidator.md | background | sectionsGlob | blueprint/LAYOUT.md, blueprint/DESIGN.md, blueprint/INTERACTION.md |

Output Path Conventions:
- Section staging: {protoDir}/.watson/sections/{sectionName}/LAYOUT.md (< 80 lines)
- Consolidated: {protoDir}/blueprint/LAYOUT.md (no line budget)
- Staging cleaned up after consolidation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port decomposer, layout, and design agents to Watson contract</name>
  <files>
    ~/.claude/skills/watson/agents/decomposer.md
    ~/.claude/skills/watson/agents/layout.md
    ~/.claude/skills/watson/agents/design.md
  </files>
  <action>
Copy each Loupe agent file and apply the Watson translation. For each of the 3 agents:

**Source files (read these first):**
- ~/.claude/skills/loupe/agents/00-decomposer.md -> watson/agents/decomposer.md
- ~/.claude/skills/loupe/agents/01-layout.md -> watson/agents/layout.md
- ~/.claude/skills/loupe/agents/02-tokens.md -> watson/agents/design.md

**Translation rules (apply to ALL 3 agents):**

1. **Frontmatter:** Change `name: agent-XX-name` to Watson contract name (e.g., `name: decomposer`, `name: layout`, `name: design`). Add `dispatch: foreground` or `dispatch: background` per the agent registry.

2. **libraryPath -> libraryPaths[]:** Replace the single `libraryPath` input parameter with `libraryPaths` (string array). Update every reference to "read library.md at libraryPath" to "read each file path in libraryPaths array". The agent iterates over libraryPaths and reads each chapter/page file directly — it never navigates LIBRARY.md or BOOK.md.

3. **Output paths:** Replace all `.loupe/sections/` references with `.watson/sections/`. For example: `.loupe/sections/[sectionName]/LAYOUT.md` becomes `.watson/sections/{sectionName}/LAYOUT.md`.

4. **Existing vocabulary reads:** Replace `existingLayoutPath` / `existingDesignPath` (pointing to `.loupe/LAYOUT.md` or `.loupe/DESIGN.md`) with a read from `blueprintPath` — the agent reads `{blueprintPath}/LAYOUT.md` or `{blueprintPath}/DESIGN.md` if it exists, for vocabulary consistency. The parameter name changes from `existingLayoutPath` to using `blueprintPath` (shared parameter).

5. **Remove FauxDS-specific hardcoding:** Replace any mention of "fauxds-library.md" or a hardcoded path to a specific library file with "the library book chapter/page files provided via libraryPaths[]". The agent reads whatever library data is passed to it — it does not know or care which design system it is. Keep the token mapping LOGIC intact (nearest-match for spacing, exact-hex for colors) — just remove the assumption that the source is FauxDS specifically.

6. **Decomposer-specific:** The decomposer does not read library books (it only reads Figma metadata). It still receives libraryPaths[] in the shared parameters but does not use them. Keep its figmaUrl parameter. No output path changes needed (decomposer returns inline JSON, not file output).

7. **Agent title:** Change "Agent 01: Layout" to "Agent: Layout", "Agent 02: Tokens" to "Agent: Design", "Agent 00: Decomposer" to "Agent: Decomposer". The Watson contract drops numeric prefixes.

8. **Role/heading in design agent:** Rename from "Tokens" to "Design" throughout — Watson calls this agent "design" not "tokens".

**Preserve everything else unchanged:** All execution steps, critical constraints, MCP tool names, overflow fallback patterns, mapping logic, line budgets, output format schemas. The port is mechanical — do not rewrite logic.
  </action>
  <verify>
    <automated>
# Verify all 3 files exist and contain Watson contract markers
for f in decomposer layout design; do
  FILE="$HOME/.claude/skills/watson/agents/$f.md"
  if [ ! -f "$FILE" ]; then echo "MISSING: $FILE"; exit 1; fi
  if grep -q "\.loupe/" "$FILE"; then echo "FAIL: $f.md still contains .loupe/ path"; exit 1; fi
  if grep -q "libraryPath[^s]" "$FILE" | grep -v "libraryPaths"; then echo "FAIL: $f.md uses libraryPath (singular)"; exit 1; fi
  echo "PASS: $f.md"
done
echo "All 3 agents ported"
    </automated>
  </verify>
  <done>
    - decomposer.md, layout.md, design.md exist in watson/agents/
    - All three use libraryPaths[] (not libraryPath)
    - All output paths reference .watson/sections/ (not .loupe/sections/)
    - Existing vocabulary reads use blueprintPath (not .loupe/ paths)
    - No hardcoded fauxds-library.md references — agents read from libraryPaths[]
    - All execution logic, constraints, and mapping rules preserved from Loupe originals
  </done>
</task>

<task type="auto">
  <name>Task 2: Port builder, reviewer, consolidator agents and create interaction placeholder</name>
  <files>
    ~/.claude/skills/watson/agents/interaction.md
    ~/.claude/skills/watson/agents/builder.md
    ~/.claude/skills/watson/agents/reviewer.md
    ~/.claude/skills/watson/agents/consolidator.md
  </files>
  <action>
Port the remaining 4 agents. Read the Loupe source files first, then apply the same translation rules from Task 1.

**Source files:**
- ~/.claude/skills/loupe/agents/03-interactions.md -> watson/agents/interaction.md (SPECIAL — see below)
- ~/.claude/skills/loupe/agents/04-builder.md -> watson/agents/builder.md
- ~/.claude/skills/loupe/agents/05-reviewer.md -> watson/agents/reviewer.md
- ~/.claude/skills/loupe/agents/06-consolidator.md -> watson/agents/consolidator.md

**Apply the same 8 translation rules from Task 1 to builder, reviewer, and consolidator.**

**Additional consolidator-specific translations:**
- Replace `.loupe/LAYOUT.md` and `.loupe/DESIGN.md` output paths with `blueprint/LAYOUT.md` and `blueprint/DESIGN.md` (consolidated output goes to the blueprint directory, accessed via blueprintPath)
- Replace `existingLayoutPath`/`existingDesignPath` input parameters with reads from `{blueprintPath}/LAYOUT.md` and `{blueprintPath}/DESIGN.md`
- Replace `.loupe/sections/` glob pattern with `.watson/sections/` in both the sectionsGlob input and cleanup step
- Update cleanup step: delete `.watson/sections/` (not `.loupe/sections/`) after verification
- Update header comment: "Consolidated by Watson Agent: Consolidator" (not "Loupe Agent 06")
- The consolidator still does NOT create INTERACTION.md (Agent 3 deferred — this was already a constraint in the Loupe version)

**Additional builder-specific translations:**
- Replace `.loupe/sections/[name]/` in spec file input paths with `.watson/sections/{sectionName}/`
- Replace `libraryPath` with `libraryPaths` — builder reads library books from the array for component variant validation

**Additional reviewer-specific translations:**
- Replace `.loupe/sections/[sectionScope]/` staging path references with `.watson/sections/{sectionScope}/`
- Confirm staging verification step checks .watson/ paths

**Agent 3 (interaction.md) — DOCUMENTED PLACEHOLDER:**
Per CONTEXT.md locked decision, Agent 3 (Interactions) is deferred to post-v1. Do NOT port the full Loupe 03-interactions.md. Instead, create a placeholder file with:
- Watson contract frontmatter: `name: interaction`, `dispatch: foreground`
- A clear heading: "Agent: Interaction"
- Role section: one sentence describing the interaction agent's purpose
- A prominent "## Status: Deferred" section stating: "This agent is deferred to post-v1. The discuss subskill handles interaction context gathering for Watson 1.0. See CONTEXT.md locked decision: Agent 3 deferral."
- Inputs/Outputs sections matching the Watson agent contract (nodeId, interactionContext?, sectionName, blueprintPath, libraryPaths[], watsonMode) so the contract is documented even though the implementation is deferred
- No execution steps — just the contract stub and deferral notice
- Target: ~25-30 lines (compact but complete contract documentation)
  </action>
  <verify>
    <automated>
# Verify all 4 files exist and contain Watson contract markers
for f in interaction builder reviewer consolidator; do
  FILE="$HOME/.claude/skills/watson/agents/$f.md"
  if [ ! -f "$FILE" ]; then echo "MISSING: $FILE"; exit 1; fi
  if grep -q "\.loupe/" "$FILE"; then echo "FAIL: $f.md still contains .loupe/ path"; exit 1; fi
  echo "PASS: $f.md"
done
# Verify interaction is a placeholder
if grep -qi "deferred" "$HOME/.claude/skills/watson/agents/interaction.md"; then
  echo "PASS: interaction.md contains deferral notice"
else
  echo "FAIL: interaction.md missing deferral notice"
  exit 1
fi
# Verify consolidator writes to blueprint/
if grep -q "blueprint/" "$HOME/.claude/skills/watson/agents/consolidator.md"; then
  echo "PASS: consolidator.md outputs to blueprint/"
else
  echo "FAIL: consolidator.md missing blueprint/ output path"
  exit 1
fi
echo "All 4 agents ported"
    </automated>
  </verify>
  <done>
    - builder.md, reviewer.md, consolidator.md exist in watson/agents/ with full Watson contract compliance
    - interaction.md exists as a documented placeholder with deferral notice and contract stub
    - Consolidator outputs to blueprint/LAYOUT.md and blueprint/DESIGN.md, cleans .watson/sections/
    - All 4 agents use libraryPaths[] (not libraryPath), .watson/ paths (not .loupe/), blueprintPath for vocabulary reads
    - No hardcoded fauxds-library.md references in any file
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run a full contract compliance sweep across all 7 agent files:

1. All 7 files exist in ~/.claude/skills/watson/agents/ (decomposer, layout, design, interaction, builder, reviewer, consolidator)
2. Zero occurrences of `.loupe/` in any agent file
3. Zero occurrences of `libraryPath` (singular without trailing 's') used as a parameter name
4. Zero occurrences of hardcoded `fauxds-library.md` path references
5. Every pipeline agent (layout, design, builder, reviewer, consolidator) references `.watson/sections/` for staging paths
6. Consolidator references `blueprint/` for consolidated output
7. interaction.md contains "deferred" and is under 40 lines
8. All agent frontmatter includes `name:` and `dispatch:` fields matching the Watson agent registry
</verification>

<success_criteria>
- 7 agent files in watson/agents/ conforming to Watson agent contract
- Zero Loupe-era path references (.loupe/, fauxds-library.md, libraryPath singular)
- All staging paths use .watson/sections/, all consolidated output uses blueprint/
- interaction.md is a documented placeholder with contract stub and deferral notice
- Every agent reads library data from libraryPaths[] array — source-agnostic
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/03-research-agents/03-01-SUMMARY.md`
</output>
