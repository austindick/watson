---
phase: 09-agent-3-interactions
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - ~/.claude/skills/watson/skills/loupe.md
  - ~/.claude/skills/watson/agents/consolidator.md
  - ~/.claude/skills/watson/SKILL.md
autonomous: true
requirements:
  - INTR-01
  - INTR-03
  - INTR-04

must_haves:
  truths:
    - "Loupe dispatches interaction agent sequentially after layout + design wait gate completes, for each figma section"
    - "Loupe resolves interactionPath from interaction agent output and passes it to builder (replacing hardcoded null)"
    - "Discuss-only sections skip interaction agent dispatch (same guard as layout + design)"
    - "Interaction agent failure falls back to interactionPath: null — builder proceeds with library defaults"
    - "Consolidator merges section-level INTERACTION.md files into blueprint/INTERACTION.md using union-merge"
    - "Consolidator constraint 5 (No INTERACTION.md) is removed"
  artifacts:
    - path: "~/.claude/skills/watson/skills/loupe.md"
      provides: "Interaction agent dispatch in Phase 2 + interactionPath resolution in Phase 3"
      contains: "interaction.md"
    - path: "~/.claude/skills/watson/agents/consolidator.md"
      provides: "INTERACTION.md consolidation following union-merge pattern"
      contains: "INTERACTION.md"
    - path: "~/.claude/skills/watson/SKILL.md"
      provides: "crossSectionFlows forwarding in Discuss->Loupe chain and Tier 2 routing"
      contains: "crossSectionFlows"
  key_links:
    - from: "skills/loupe.md"
      to: "agents/interaction.md"
      via: "background agent dispatch after layout+design wait gate"
      pattern: "interaction.md.*background"
    - from: "skills/loupe.md"
      to: "agents/builder.md"
      via: "interactionPath parameter (no longer hardcoded null)"
      pattern: "interactionPath:.*INTERACTION.md"
    - from: "agents/consolidator.md"
      to: "blueprint/INTERACTION.md"
      via: "union-merge of section-level INTERACTION.md files"
      pattern: "INTERACTION.md.*union"
---

<objective>
Wire interaction agent into the loupe pipeline and extend consolidator to handle INTERACTION.md.

Purpose: Plan 01 created the interaction agent and the discuss interactionContext emit. This plan wires them into the pipeline — loupe.md dispatches the interaction agent after layout + design complete and passes the resolved interactionPath to the builder. The consolidator is updated to merge section-level INTERACTION.md files into blueprint/INTERACTION.md, removing the old constraint that prohibited it.

Output: Updated skills/loupe.md with interaction agent dispatch in Phase 2 and interactionPath resolution in Phase 3. Updated agents/consolidator.md with INTERACTION.md consolidation and constraint 5 removed. Updated SKILL.md to forward crossSectionFlows in both Discuss->Loupe chain and Tier 2 routing.
</objective>

<execution_context>
@/Users/austindick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austindick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-agent-3-interactions/09-CONTEXT.md
@.planning/phases/09-agent-3-interactions/09-RESEARCH.md
@.planning/phases/09-agent-3-interactions/09-01-SUMMARY.md

@~/.claude/skills/watson/skills/loupe.md
@~/.claude/skills/watson/agents/consolidator.md
@~/.claude/skills/watson/SKILL.md
@~/.claude/skills/watson/references/agent-contract.md

<interfaces>
<!-- From Plan 01 — interaction agent contract -->

agents/interaction.md dispatch params:
  nodeId: string (section nodeId)
  sectionName: string
  interactionContext: object|null (from discuss sections[].interactionContext)
  crossSectionFlows: array|null (from discuss return status top-level)
  blueprintPath: string
  libraryPaths: string[]
  watsonMode: true
  layoutPath: string (.watson/sections/{sectionName}/LAYOUT.md)
  designPath: string (.watson/sections/{sectionName}/DESIGN.md)

Output: .watson/sections/{sectionName}/INTERACTION.md (< 50 lines)

From skills/discuss.md return status (updated in Plan 01):
  sections[].interactionContext — per-section interaction context (null when not discussed)
  crossSectionFlows[] — top-level cross-section flow array

From agents/consolidator.md — current constraint 5 to remove:
  "5. **No INTERACTION.md** — do NOT create or consolidate an INTERACTION.md file."

From skills/loupe.md Phase 3 — current hardcoded null:
  interactionPath: null    (interaction agent deferred — Watson 1.0)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add interaction agent dispatch to loupe.md Phase 2, resolve interactionPath in Phase 3, and update SKILL.md routing</name>
  <files>~/.claude/skills/watson/skills/loupe.md, ~/.claude/skills/watson/SKILL.md</files>
  <action>
Two files modified: loupe.md (Changes 1-4, 6) and SKILL.md (Change 5).

**Change 1: Add interaction agent dispatch after layout+design wait gate in Phase 2**

After the existing text "Wait for every layout and design agent across all sections to complete before proceeding to Phase 3" and before the "For sections where referenceType = discuss-only" paragraph, insert a new block:

```
**Interaction agent dispatch (sequential per figma section):**

After ALL layout + design agents have completed, dispatch the interaction agent for each figma section sequentially:

For each section where `referenceType = "figma"`:

1. Resolve paths for the interaction agent:
   - layoutPath = `.watson/sections/{section.name}/LAYOUT.md`
   - designPath = `.watson/sections/{section.name}/DESIGN.md`
   - sectionInteractionContext = sections[i].interactionContext (from discuss return status; may be null)

2. Dispatch `@agents/interaction.md` as **background** agent:
   ```
   nodeId: {section.nodeId}
   sectionName: {section.name}
   interactionContext: {sectionInteractionContext}
   crossSectionFlows: {crossSectionFlows}
   blueprintPath: {blueprintPath}
   libraryPaths: {libraryPaths}
   watsonMode: true
   layoutPath: {layoutPath}
   designPath: {designPath}
   ```

3. Wait for completion.

4. Check `.watson/sections/{section.name}/INTERACTION.md` exists:
   - If yes: store interactionPath for this section
   - If no (agent failed): retry once silently. On second failure: set interactionPath to null for this section — builder proceeds with library defaults only

For sections where `referenceType = "discuss-only"`: skip interaction agent dispatch (same as layout + design). Discuss already populated blueprint/INTERACTION.md in the discuss-only path.
```

**Change 2: Replace hardcoded interactionPath: null in Phase 3**

In Phase 3 Build + Review section, find the builder dispatch block. Replace:
```
interactionPath: null    (interaction agent deferred — Watson 1.0)
```
With:
```
interactionPath: {interactionPath for this section}    (resolved from Phase 2 interaction agent output; null if agent failed or section is discuss-only)
```

For discuss-only sections, set interactionPath to `{blueprintPath}/INTERACTION.md` if that file exists (populated by discuss), otherwise null.

**Change 3: Update loupe.md Inputs table**

The loupe.md Inputs table currently has: blueprintPath, sections, hasFullFrame, fullFrameUrl. Add two new optional parameters that come from discuss return status:

| Parameter | Type | Description |
|-----------|------|-------------|
| crossSectionFlows | array or null | Cross-section interaction flows from discuss (passed to interaction agent and consolidator) |

**Change 4: Update Loupe Phase 4 (Consolidate) — pass crossSectionFlows**

In the consolidator dispatch block, add a new parameter:
```
crossSectionFlows: {crossSectionFlows}    (from discuss return status; passed through to consolidator for cross-section flow consolidation)
```

**Change 5: Update SKILL.md routing (two locations)**

In SKILL.md "Discuss -> Loupe Chain" section, update the ready_for_build dispatch line to include crossSectionFlows:
"Dispatch @skills/loupe.md with blueprintPath, sections[], hasFullFrame, fullFrameUrl, crossSectionFlows from the return status."

In SKILL.md "Tier 2 (build)" routing, add `crossSectionFlows: null` to the loupe dispatch (no discuss context in direct Tier 2).

**Change 6: Update Designer-Language Progress Reference table**

Add a row for the interaction agent:
| Interaction agent running | "Detailing interaction states for the [section name]..." |

Also add a progress update line in the Phase 2 interaction dispatch block:
Progress update: "Detailing interaction states for the [section.name]..."
  </action>
  <verify>
    <automated>grep -q "interaction.md" ~/.claude/skills/watson/skills/loupe.md && grep -q "interactionPath" ~/.claude/skills/watson/skills/loupe.md && grep -q "crossSectionFlows" ~/.claude/skills/watson/skills/loupe.md && ! grep -q "interactionPath: null.*deferred" ~/.claude/skills/watson/skills/loupe.md && grep -q "crossSectionFlows" ~/.claude/skills/watson/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Loupe Phase 2 dispatches interaction agent sequentially after layout+design wait gate. Phase 3 passes resolved interactionPath to builder (no longer hardcoded null). Discuss-only sections skip interaction dispatch. Error handling retries once then falls back to null. SKILL.md forwards crossSectionFlows from discuss return status.</done>
</task>

<task type="auto">
  <name>Task 2: Extend consolidator to handle INTERACTION.md</name>
  <files>~/.claude/skills/watson/agents/consolidator.md</files>
  <action>
Three changes to consolidator.md:

**Change 1: Remove constraint 5**

Delete constraint 5: "**No INTERACTION.md** — do NOT create or consolidate an INTERACTION.md file. Agent 3 is deferred; no stub file."

Renumber remaining constraints (6 becomes 5, 7 becomes 6, 8 becomes 7).

**Change 2: Update Role description**

Change from: "Merge all section-level LAYOUT.md and DESIGN.md artifacts..."
To: "Merge all section-level LAYOUT.md, DESIGN.md, and INTERACTION.md artifacts from `.watson/sections/` into consolidated project-level vocabulary files at `blueprint/LAYOUT.md`, `blueprint/DESIGN.md`, and `blueprint/INTERACTION.md`."

**Change 3: Add Inputs — crossSectionFlows parameter**

Add to the Inputs section:
- `crossSectionFlows` — optional array of cross-section flow objects from discuss return status. Appended as a `## Cross-Section Flows` section at the end of consolidated INTERACTION.md. May be null/empty.

**Change 4: Update Outputs section**

Add to the Outputs list:
- `{blueprintPath}/INTERACTION.md` — consolidated interaction vocabulary (union of all sections + prior runs + cross-section flows)

**Change 5: Add Step 3b — Union-merge INTERACTION.md**

Insert a new step after Step 4 (Union-merge DESIGN.md) and before Step 5 (Write consolidated files). Call it "Step 4b: Union-merge INTERACTION.md" (or renumber all steps).

The INTERACTION.md union-merge follows the same pattern as LAYOUT.md and DESIGN.md but with these specific rules:

Read all `.watson/sections/*/INTERACTION.md` files (same glob pattern). Read existing `{blueprintPath}/INTERACTION.md` as priorInteraction if it exists.

Union rules by section:
- **Tier 1 (DS States) rows**: deduplicate by component name (normalized). If same component appears in multiple sections with different states lists, keep the most complete states list and add `(seen in N sections)` note.
- **Tier 2 (Custom States) rows**: keep all — section-specific overrides are rarely duplicated.
- **Tier 3 (Net-New) rows**: keep all — net-new interactions are section-specific.
- **Transitions rows**: deduplicate by trigger description (normalized lowercase comparison). If same trigger with different animation/duration, keep both with conflict comment.
- **User Flows**: keep all — flows are section-specific by definition. Each flow subsection gets a section attribution if from different sections.
- **Responsive Behavior rows**: deduplicate by breakpoint (normalized). If same breakpoint with different behaviors from different sections, keep both rows with section attribution suffix.

After the union-merged content, if `crossSectionFlows` is provided and non-empty, append:

```markdown
---

## Cross-Section Flows

| Flow | Sections | Steps |
|------|----------|-------|
| {flow.name} | {flow.sections joined with ", "} | {flow.steps joined with " -> "} |
```

Apply the same 80-line soft budget with overflow-allowed rule.

**Change 6: Update Step 5 (Write consolidated files)**

Add writing of `{blueprintPath}/INTERACTION.md` alongside LAYOUT.md and DESIGN.md. Use the same header comment format:
```
<!-- Consolidated by Watson Agent: Consolidator | Sections: {section names} | Date: {YYYY-MM-DD} -->
```

**Change 7: Update Step 6 (Verify and clean up)**

Update the verification gate: INTERACTION.md is optional — if no section produced an INTERACTION.md and no priorInteraction exists, skip verification for it. But if INTERACTION.md was produced, verify it exists and has at least 5 lines before cleanup.

Update the gate logic: cleanup requires LAYOUT.md AND DESIGN.md verified. INTERACTION.md verification is additive — its absence does not block cleanup (a section may not have had any interaction data).

Update the final report to include INTERACTION.md:
```
Consolidated: blueprint/LAYOUT.md ({lines}), blueprint/DESIGN.md ({lines}), blueprint/INTERACTION.md ({lines})
```

**Change 8: Update Step 1 (Read all section artifacts)**

Add: Use the Glob tool to find all `.watson/sections/*/INTERACTION.md` files matching sectionsGlob. Read each file. Partial sections (missing INTERACTION.md) are valid — skip for that section only.
  </action>
  <verify>
    <automated>! grep -q "No INTERACTION.md" ~/.claude/skills/watson/agents/consolidator.md && grep -q "INTERACTION.md" ~/.claude/skills/watson/agents/consolidator.md && grep -q "crossSectionFlows" ~/.claude/skills/watson/agents/consolidator.md && grep -q "Cross-Section Flows" ~/.claude/skills/watson/agents/consolidator.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>Consolidator constraint 5 removed. INTERACTION.md union-merge step added following the same pattern as LAYOUT/DESIGN. Cross-section flows appended from crossSectionFlows parameter. Verification gate updated — INTERACTION.md optional but verified when present. Cleanup unchanged (gated on LAYOUT + DESIGN).</done>
</task>

</tasks>

<verification>
1. loupe.md Phase 2 dispatches @agents/interaction.md as background after layout+design wait gate
2. loupe.md Phase 3 uses resolved interactionPath (not hardcoded null)
3. loupe.md skips interaction dispatch for discuss-only sections
4. SKILL.md forwards crossSectionFlows to loupe from discuss return status
5. consolidator.md constraint 5 ("No INTERACTION.md") is deleted
6. consolidator.md reads, union-merges, and writes INTERACTION.md
7. consolidator.md appends Cross-Section Flows table from crossSectionFlows parameter
</verification>

<success_criteria>
- The full pipeline path works: discuss emits interactionContext -> SKILL.md forwards crossSectionFlows to loupe -> loupe dispatches interaction agent -> interaction agent writes section INTERACTION.md -> loupe passes interactionPath to builder -> consolidator merges into blueprint/INTERACTION.md
- No hardcoded `interactionPath: null` remains in loupe.md
- Consolidator no longer prohibits INTERACTION.md
- Error handling: interaction agent failure gracefully falls back to null interactionPath
</success_criteria>

<output>
After completion, create `.planning/phases/09-agent-3-interactions/09-02-SUMMARY.md`
</output>