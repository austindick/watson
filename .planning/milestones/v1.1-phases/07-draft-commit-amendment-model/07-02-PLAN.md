---
phase: 07-draft-commit-amendment-model
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - /Users/austindick/.claude/skills/watson/SKILL.md
  - /Users/austindick/.claude/skills/watson/agents/builder.md
autonomous: true
requirements:
  - DRFT-01
  - DRFT-04

must_haves:
  truths:
    - "Returning to a prototype with pending amendments shows the pending count in the context summary"
    - "A fourth action choice 'Review pending amendments' appears when drafts: is non-empty"
    - "Review pending amendments shows the same diff format as the commit gate and offers Commit all / Discard all / Keep pending"
    - "A soft warning fires once per session when the user triggers a build with pending amendments"
    - "Builder reads only [COMMITTED] lines and skips [PENDING] lines"
    - "Pre-Phase-7 amendments without markers are treated as [COMMITTED] by builder (backwards compatibility)"
  artifacts:
    - path: "~/.claude/skills/watson/SKILL.md"
      provides: "Session-start surfacing of pending amendments, soft build warning"
      contains: "Review pending amendments"
    - path: "~/.claude/skills/watson/agents/builder.md"
      provides: "[COMMITTED]-only amendment filter with backwards compat"
      contains: "[COMMITTED]"
  key_links:
    - from: "SKILL.md returning-prototype flow"
      to: "blueprint/STATUS.md drafts: array"
      via: "YAML frontmatter parsing (already exists)"
      pattern: "drafts:"
    - from: "SKILL.md soft build warning"
      to: "/tmp/watson-active.json pendingWarningShown"
      via: "session-local flag prevents repeat warning"
      pattern: "pendingWarningShown"
    - from: "builder.md amendment filter"
      to: "blueprint files ## Discuss Amendments"
      via: "line-by-line prefix check"
      pattern: "\\[COMMITTED\\]|\\[PENDING\\]"
---

<objective>
Add session-start surfacing of pending amendments to SKILL.md and add the [COMMITTED]-only amendment filter to builder.md. Together with Plan 01, this completes the draft/commit lifecycle: pending amendments are visible at session start, optionally reviewable, and genuinely excluded from builds.

Purpose: Pending amendments must surface every session (DRFT-04) and be excluded from builds until committed (completing DRFT-01).
Output: Updated SKILL.md with pending surfacing and soft warning; updated builder.md with amendment filter.
</objective>

<execution_context>
@/Users/austindick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austindick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-draft-commit-amendment-model/07-CONTEXT.md
@.planning/phases/07-draft-commit-amendment-model/07-RESEARCH.md
@.planning/phases/07-draft-commit-amendment-model/07-01-SUMMARY.md

<interfaces>
<!-- SKILL.md returning-prototype flow (lines ~31-34) — current behavior: -->
```markdown
3. Check for `blueprint/STATUS.md`
   - **Exists -> Returning prototype.** Parse YAML frontmatter. Display summary:
     "[prototype_name] — [N] section(s) built ([list]). Last discussed: [last_discussed]."
     Then offer choices: "Continue building / Discuss changes / Start fresh"
     Wait for user selection, then proceed to Intent Classification.
```

<!-- SKILL.md Tier 2 build dispatch (Routing section, lines ~113-118): -->
```markdown
**Tier 2 (build):**
If `blueprint/CONTEXT.md` is template-only...
Dispatch `@skills/loupe.md` with:
- `blueprintPath`: resolved blueprint/ path
- `sections`: inferred from blueprint if populated
...
```

<!-- builder.md Step 1 (lines ~40-42) — current spec file reading: -->
```markdown
### Step 1: Read spec files
Read `layoutPath` (LAYOUT.md) and `designPath` (DESIGN.md). Check if INTERACTION.md exists at `interactionPath`; if yes, load it; if no, note "library component defaults only" and proceed.
```

<!-- builder.md also receives `blueprintPath` as input (line ~29) -->

<!-- /tmp/watson-active.json — Phase 6 state file, currently written as `echo '{}' > /tmp/watson-active.json` on load -->

<!-- SKILL.md is currently 167 lines. Hard limit is 200. Budget: ~13 lines for Phase 7 additions (target <=195 for Phase 8 buffer). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add session-start pending surfacing and soft build warning to SKILL.md</name>
  <files>/Users/austindick/.claude/skills/watson/SKILL.md</files>
  <action>
SKILL.md is currently 167 lines with a 200-line hard limit. All additions must be tight. Target: <=195 lines after changes.

**1. Extend returning-prototype flow (Routing section, step 3 "Exists -> Returning prototype"):**

Replace the current returning-prototype block with a conditional version. After "Parse YAML frontmatter", add a check on `drafts:`:

```
- **Exists -> Returning prototype.** Parse YAML frontmatter.
  If `drafts:` is non-empty (M > 0):
    Display summary: "[prototype_name] — [N] section(s) built ([list]). [M] pending amendment(s). Last discussed: [last_discussed]."
    Offer choices: "Continue building / Discuss changes / Review pending amendments / Start fresh"
  Else:
    Display summary: "[prototype_name] — [N] section(s) built ([list]). Last discussed: [last_discussed]."
    Offer choices: "Continue building / Discuss changes / Start fresh"
  Wait for user selection, then proceed to Intent Classification.
```

**2. Add "Review pending amendments" handler:** Add a brief handler below the returning-prototype flow (or inline). When user selects "Review pending amendments":
- Scan blueprint/LAYOUT.md, blueprint/DESIGN.md, blueprint/INTERACTION.md for lines starting with `[PENDING]`
- Assemble a design-language diff (same format as the commit gate diff from discuss.md Plan 01)
- Show via AskUserQuestion: header "Pending", question shows the diff, options: ["Commit all", "Discard all", "Keep pending and continue"]
- "Commit all": run the commit-all sequence (replace `[PENDING]` with `[COMMITTED]` in all blueprint files, clear STATUS.md `drafts: []`), then proceed to Intent Classification
- "Discard all": delete all `[PENDING]` lines from blueprint files, clear STATUS.md `drafts: []`, confirm "Pending amendments discarded. Committed decisions preserved.", then proceed to Intent Classification
- "Keep pending and continue": proceed to Intent Classification as-is

**3. Add soft build warning to Tier 2 dispatch:** Before the Tier 2 "Dispatch `@skills/loupe.md`" line, add:

```
Before dispatching loupe: if STATUS.md `drafts:` is non-empty AND `/tmp/watson-active.json` does not contain `"pendingWarningShown": true`:
  AskUserQuestion — header: "Pending Changes", question: "[M] pending amendment(s) won't be included in the build.", options: ["Commit and build", "Build without pending"]
  "Commit and build": run commit-all sequence (same as above), then dispatch loupe
  "Build without pending": write `"pendingWarningShown": true` to /tmp/watson-active.json (Edit tool to add key to JSON), then dispatch loupe as-is
```

**Line budget check:** The returning flow change adds ~5 lines (conditional + extra choice). The review handler adds ~5 lines (compact). The soft warning adds ~3 lines. Total: ~13 lines. 167 + 13 = 180, well within 200.

IMPORTANT: Keep all additions compact. Use the existing SKILL.md writing style (terse, no redundancy). Do NOT expand existing sections that don't need changes.
  </action>
  <verify>
    <automated>wc -l /Users/austindick/.claude/skills/watson/SKILL.md | awk '{exit ($1 > 195) ? 1 : 0}' && grep -q "Review pending amendments" /Users/austindick/.claude/skills/watson/SKILL.md && grep -q "pendingWarningShown" /Users/austindick/.claude/skills/watson/SKILL.md && echo "PASS"</automated>
  </verify>
  <done>SKILL.md shows pending count in returning-prototype summary when drafts: non-empty. "Review pending amendments" choice appears with commit/discard/keep options. Soft build warning fires once per session at Tier 2 dispatch. File stays under 195 lines.</done>
</task>

<task type="auto">
  <name>Task 2: Add [COMMITTED]-only amendment filter to builder.md</name>
  <files>/Users/austindick/.claude/skills/watson/agents/builder.md</files>
  <action>
Add an amendment filter step to builder.md. The builder currently reads per-section spec files (LAYOUT.md, DESIGN.md from `.watson/sections/`) via `layoutPath` and `designPath`, and also receives `blueprintPath`.

**1. Add amendment filter after Step 1 "Read spec files":** Insert a new sub-step or extend Step 1 with the following:

"After reading spec files, also read the blueprint-level amendment overlay. Read `{blueprintPath}/LAYOUT.md`, `{blueprintPath}/DESIGN.md`, and `{blueprintPath}/INTERACTION.md` (if it exists). Locate the `## Discuss Amendments` section in each file. Filter amendment lines:

- Lines starting with `[PENDING] `: **skip entirely** — these are uncommitted design decisions
- Lines starting with `[COMMITTED] `: **apply** (strip the `[COMMITTED] ` prefix before using the amendment data)
- Lines with no marker prefix (pre-Phase-7 format): **apply as-is** — backwards compatibility for prototypes built before the draft/commit model

Apply committed amendments as an overlay on the per-section spec data, following the existing amendment-as-overlay contract."

**2. Update Critical Constraints:** Add constraint 8:
"8. Only apply `[COMMITTED]` amendments from blueprint files — skip `[PENDING]` lines entirely. Lines without a marker prefix (pre-Phase-7) are treated as committed."

Keep the addition compact — builder.md is an agent file read by Claude, not a human doc.
  </action>
  <verify>
    <automated>grep -q "\[COMMITTED\]" /Users/austindick/.claude/skills/watson/agents/builder.md && grep -q "\[PENDING\]" /Users/austindick/.claude/skills/watson/agents/builder.md && grep -q "backwards" /Users/austindick/.claude/skills/watson/agents/builder.md && echo "PASS"</automated>
  </verify>
  <done>builder.md filters amendments: applies [COMMITTED] and unmarked lines, skips [PENDING] lines. Backwards compatibility rule documented. Critical constraint 8 added.</done>
</task>

</tasks>

<verification>
1. Read SKILL.md and confirm: returning-prototype shows pending count when drafts: non-empty
2. Confirm: "Review pending amendments" choice exists with Commit all / Discard all / Keep pending options
3. Confirm: soft build warning references /tmp/watson-active.json pendingWarningShown flag
4. Confirm: SKILL.md line count is <= 195
5. Read builder.md and confirm: [COMMITTED]-only filter with backwards compat for unmarked lines
6. Confirm: builder.md Critical Constraints includes the amendment filter rule
</verification>

<success_criteria>
- SKILL.md returning-prototype flow conditionally shows pending amendment count and review option
- SKILL.md soft build warning fires once per session before Tier 2 dispatch when drafts: non-empty
- builder.md reads only [COMMITTED] amendment lines, skips [PENDING], treats unmarked as committed
- SKILL.md stays under 195 lines (buffer for Phase 8)
- All four DRFT requirements are covered across Plan 01 and Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/07-draft-commit-amendment-model/07-02-SUMMARY.md`
</output>
