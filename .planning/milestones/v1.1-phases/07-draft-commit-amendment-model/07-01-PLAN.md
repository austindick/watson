---
phase: 07-draft-commit-amendment-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/austindick/.claude/skills/watson/skills/discuss.md
autonomous: true
requirements:
  - DRFT-01
  - DRFT-02
  - DRFT-03

must_haves:
  truths:
    - "Every new amendment written by discuss has a [PENDING] prefix — no bare amendments"
    - "STATUS.md drafts: array is updated with the amendment ID slug after each amendment write"
    - "Same-property amendments update in place — one entry per property, always the latest decision"
    - "The Ready gate shows a design-language diff of pending amendments grouped by file before offering build"
    - "Let's build commits ALL pending amendments then starts the build"
    - "Just save decisions leaves amendments as [PENDING] with no build"
    - "Mid-build discuss flow writes [PENDING] markers and updates STATUS.md drafts: using the same logic as the main flow"
  artifacts:
    - path: "~/.claude/skills/watson/skills/discuss.md"
      provides: "Amendment write logic with [PENDING] prefix, commit gate diff, commit-all sequence, STATUS.md drafts management"
      contains: "[PENDING]"
  key_links:
    - from: "discuss.md amendment write"
      to: "blueprint/STATUS.md drafts: array"
      via: "Edit tool updates YAML frontmatter after each amendment"
      pattern: "drafts:"
    - from: "discuss.md Ready gate"
      to: "blueprint files [PENDING] -> [COMMITTED]"
      via: "commit-all sequence replaces markers"
      pattern: "\\[COMMITTED\\]"
---

<objective>
Add the [PENDING]/[COMMITTED] amendment lifecycle to discuss.md. Every amendment discuss writes gets a [PENDING] prefix. The "Ready?" gate is extended to show a design-language diff and commit all pending amendments on "Let's build." STATUS.md drafts: array tracks pending amendment IDs.

Purpose: This is the core mechanic of the draft/commit model — no design decision is silently locked in.
Output: Updated discuss.md with pending marker writes, commit gate diff, commit-all sequence, and STATUS.md drafts management.
</objective>

<execution_context>
@/Users/austindick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austindick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-draft-commit-amendment-model/07-CONTEXT.md
@.planning/phases/07-draft-commit-amendment-model/07-RESEARCH.md

<interfaces>
<!-- Key sections of discuss.md that will be modified. -->

From discuss.md lines ~334-369 — current amendment write format:
```markdown
## Blueprint Write: Surgical Amendments (LAYOUT.md, DESIGN.md, INTERACTION.md)

**Amendment format:**
Changes to existing decisions:
- [property]: [old value] -> [new value] (reason)

New decisions not previously in the file:
- [property]: [value] (new decision)

**Appending to existing `## Discuss Amendments`:** If the section already exists from a prior discuss session, append new amendments below the existing ones. Never replace existing amendments.
```

From discuss.md lines ~411-431 — current Ready gate:
```markdown
## "Ready to Build?" Detection

- header: "Ready?"
- question: "We've covered the core design decisions. Ready to build?"
- options: ["Let's build", "I want to discuss more", "Just save decisions (no build)"]

**"Just save decisions"** → return status `discussion_only`.
```

From discuss.md lines ~475-483 — mid-build discuss flow:
```markdown
- header: "Next step"
- question: "Want me to rebuild the affected section(s) now, or save the changes for later?"
- options: ["Rebuild now", "Save for later"]
```

From watson-init.md — STATUS.md YAML frontmatter includes:
```yaml
drafts: []
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add [PENDING] amendment write logic and STATUS.md drafts management</name>
  <files>/Users/austindick/.claude/skills/watson/skills/discuss.md</files>
  <action>
Modify the "Blueprint Write: Surgical Amendments" section (lines ~334-369) of discuss.md:

1. **Amendment format update:** Change the format examples and instructions so every new amendment line is prefixed with `[PENDING]`. The format becomes:

Changes to existing decisions:
```
[PENDING] [property]: [old value] -> [new value] (reason)
```

New decisions not previously in the file:
```
[PENDING] [property]: [value] (new decision)
```

Update the examples block to show `[PENDING]` prefix on every line.

2. **Same-property update-in-place rule:** Add instruction BEFORE the "Appending to existing" paragraph. When writing an amendment for a property, scan the existing `## Discuss Amendments` section for an existing entry matching the same property slug (the text before the first colon). If found:
- Replace the existing line (regardless of its current `[PENDING]` or `[COMMITTED]` status) with a new `[PENDING]` line reflecting the new value, referencing the old committed value as the "old" value if it was committed
- This preserves one-entry-per-property semantics

If NOT found: append as normal.

3. **STATUS.md drafts: update rule:** Add a new paragraph after the amendment write instructions titled "**STATUS.md drafts: update**". After EACH amendment write to a blueprint file:
- Derive the amendment ID slug: take the property portion (e.g., `header/layout`), replace `/` with `-`, replace spaces with `-`, lowercase. Result: `header-layout`
- Read `blueprint/STATUS.md` YAML frontmatter
- If the slug is not already in `drafts:`, append it using the Edit tool (never Write)
- If the slug is already present, no change needed

4. **Amendment removal during conversation:** Add a note: "If the user explicitly reverses a pending amendment during conversation, delete the `[PENDING]` line from the blueprint file and remove its slug from STATUS.md `drafts:` array. This keeps the pending state accurate."

5. **Update the "Appending to existing" paragraph:** Change from "Never replace existing amendments" to reflect the new same-property update-in-place behavior. Existing amendments with a DIFFERENT property are preserved; same-property amendments are replaced.
  </action>
  <verify>
    <automated>grep -c "PENDING" /Users/austindick/.claude/skills/watson/skills/discuss.md | xargs test 5 -le</automated>
  </verify>
  <done>discuss.md amendment write section uses [PENDING] prefix on all new amendments, includes same-property update-in-place logic, includes STATUS.md drafts: update after each write, and includes amendment removal on conversational reversal</done>
</task>

<task type="auto">
  <name>Task 2: Extend Ready gate with diff display and commit-all sequence</name>
  <files>/Users/austindick/.claude/skills/watson/skills/discuss.md</files>
  <action>
Modify the "Ready to Build? Detection" section (lines ~411-431) and add a new section for the commit sequence. Also update the mid-build flow (lines ~475-483).

1. **Ready gate diff display:** Insert new instructions BEFORE the AskUserQuestion block in "Ready to Build? Detection":

"**Pending amendment diff (conditional):** Before showing the Ready gate, check `blueprint/STATUS.md drafts:` array.
- If `drafts:` is empty: proceed directly to the existing gate (no diff, no commit step needed).
- If `drafts:` is non-empty: scan all three blueprint files (LAYOUT.md, DESIGN.md, INTERACTION.md) for lines starting with `[PENDING]`. Assemble a diff grouped by target file using design language — translate amendment syntax into plain descriptions. Example: `sidebar/width: not specified -> 280px` becomes 'Sidebar narrows to 280px.' Display the diff before the gate question."

The diff display format:
```
> Here's what will be locked in:
>
> **Layout changes:**
> - [translated amendment 1]
> - [translated amendment 2]
>
> **Design changes:**
> - [translated amendment]
>
> **Interaction changes:**
> (none)
```

2. **Gate options stay the same:** `["Let's build", "I want to discuss more", "Just save decisions (no build)"]` — no change to the option names.

3. **Commit-all sequence on "Let's build":** Add a new subsection "**Commit-all sequence**" after the Ready gate. When user selects "Let's build" AND `drafts:` was non-empty:

```
Before returning status "ready_for_build":
1. For each blueprint file (LAYOUT.md, DESIGN.md, INTERACTION.md):
   a. Read file
   b. In the ## Discuss Amendments section, replace all "[PENDING] " prefixes with "[COMMITTED] "
   c. Write changes using Edit tool (never Write)
2. Clear STATUS.md drafts: array to [] using Edit tool
3. Then return status "ready_for_build" as normal
```

If "Just save decisions" is chosen: skip the commit-all sequence entirely. Amendments remain `[PENDING]`, `drafts:` array unchanged. Return status `discussion_only`.

4. **Mid-build discuss flow update (Dedup Contract section, lines ~475-483):** Update the "Next step" gate to integrate with the pending model:

Replace the current "Rebuild now / Save for later" gate with:
```
- header: "Next step"
- question: "Want me to rebuild the affected section(s) now, or save the changes for later?"
- options: ["Rebuild now", "Save for later"]
```

Add note: "Both 'Rebuild now' and 'Save for later' leave new amendments as `[PENDING]`. The commit-all sequence only runs at the main Ready gate ('Let's build'). 'Rebuild now' returns `ready_for_build` — the builder will use only `[COMMITTED]` amendments. 'Save for later' returns `discussion_only`."

This ensures the mid-build flow writes amendments with `[PENDING]` prefix and updates STATUS.md `drafts:` using the same logic defined in Task 1.
  </action>
  <verify>
    <automated>grep -c "COMMITTED" /Users/austindick/.claude/skills/watson/skills/discuss.md | xargs test 3 -le</automated>
  </verify>
  <done>Ready gate conditionally shows design-language diff when drafts: is non-empty. "Let's build" runs commit-all (replace [PENDING] with [COMMITTED], clear drafts:) before returning ready_for_build. "Just save decisions" preserves [PENDING] state. Mid-build flow uses same [PENDING] write logic and leaves amendments pending.</done>
</task>

</tasks>

<verification>
1. Read discuss.md and confirm: amendment format examples all show [PENDING] prefix
2. Confirm: same-property update-in-place logic is documented
3. Confirm: STATUS.md drafts: update instruction exists after amendment write
4. Confirm: Ready gate has conditional diff display (only when drafts: non-empty)
5. Confirm: commit-all sequence is documented (replace [PENDING] with [COMMITTED], clear drafts:)
6. Confirm: mid-build flow references [PENDING] marker logic
7. Confirm: "Just save decisions" explicitly leaves amendments as [PENDING]
</verification>

<success_criteria>
- discuss.md contains [PENDING] prefix on all amendment write examples and instructions
- STATUS.md drafts: management logic is specified (add slug on write, clear on commit-all)
- Ready gate shows diff only when drafts: is non-empty
- Commit-all sequence changes [PENDING] to [COMMITTED] in all blueprint files
- Mid-build discuss flow writes [PENDING] markers and updates drafts: consistently
- No auto-commit behavior — every amendment starts as [PENDING] without exception
</success_criteria>

<output>
After completion, create `.planning/phases/07-draft-commit-amendment-model/07-01-SUMMARY.md`
</output>
