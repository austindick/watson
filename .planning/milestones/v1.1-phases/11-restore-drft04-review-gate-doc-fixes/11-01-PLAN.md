---
phase: 11-restore-drft04-review-gate-doc-fixes
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/skills/watson/SKILL.md
  - ~/.claude/skills/watson/agents/interaction.md
  - .planning/phases/10-3-agent-parallel-dispatch/10-01-SUMMARY.md
autonomous: true
requirements: [DRFT-04]

must_haves:
  truths:
    - "SKILL.md Path B step 5 presents an AskUserQuestion gate with three options (Commit all / Discard all / Keep pending) when drafts: is non-empty"
    - "interaction.md frontmatter declares dispatch: background"
    - "10-01-SUMMARY.md frontmatter includes requirements_completed with PARA-01 through PARA-04"
    - "SKILL.md remains at or under 200 lines after the edit"
  artifacts:
    - path: "~/.claude/skills/watson/SKILL.md"
      provides: "Path B step 5 AskUserQuestion review gate for pending amendments"
      contains: "AskUserQuestion"
    - path: "~/.claude/skills/watson/agents/interaction.md"
      provides: "Correct dispatch metadata"
      contains: "dispatch: background"
    - path: ".planning/phases/10-3-agent-parallel-dispatch/10-01-SUMMARY.md"
      provides: "Complete requirements_completed frontmatter"
      contains: "PARA-01"
  key_links:
    - from: "~/.claude/skills/watson/SKILL.md"
      to: "blueprint files (LAYOUT.md, DESIGN.md, INTERACTION.md)"
      via: "Path B step 5 scans for [PENDING] lines and renders diff"
      pattern: "\\[PENDING\\]"
    - from: "~/.claude/skills/watson/SKILL.md"
      to: "STATUS.md drafts:"
      via: "Commit all and Discard all clear drafts: [] after processing"
      pattern: "drafts:"
---

<objective>
Restore the DRFT-04 amendment review gate that was dropped during Phase 8's SKILL.md rewrite, and fix two documentation mismatches identified by the v1.1 milestone audit.

Purpose: DRFT-04 requires that session start surfaces pending amendments. Phase 8 replaced the interactive AskUserQuestion gate with a passive note. This plan restores the gate and fixes two metadata issues (interaction.md dispatch field, 10-01-SUMMARY.md requirements_completed).
Output: Three surgically edited files — no new files, no new patterns.
</objective>

<execution_context>
@/Users/austindick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austindick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-restore-drft04-review-gate-doc-fixes/11-RESEARCH.md

<interfaces>
<!-- Current SKILL.md Path B step 5 (line 59) — the line to replace: -->
5. If `drafts:` non-empty, include pending amendments note

<!-- Tier 2 pre-build commit-all pattern (SKILL.md lines 134-137) — reuse this pattern, do NOT duplicate: -->
Before dispatching loupe: if STATUS.md `drafts:` is non-empty AND `/tmp/watson-active.json` does not contain `"pendingWarningShown": true`:
  AskUserQuestion — header: "Pending Changes", question: "[M] pending amendment(s) won't be included in the build.", options: ["Commit and build", "Build without pending"]
  - "Commit and build": replace all `[PENDING]` with `[COMMITTED]` in blueprint files, clear STATUS.md `drafts: []`, then dispatch loupe.
  - "Build without pending": add `"pendingWarningShown": true` to /tmp/watson-active.json (Edit tool), then dispatch loupe as-is.

<!-- interaction.md frontmatter (line 3) — current value: -->
dispatch: foreground

<!-- 10-01-SUMMARY.md frontmatter (lines 1-29) — requirements_completed key is absent entirely -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore AskUserQuestion review gate in SKILL.md Path B step 5</name>
  <files>~/.claude/skills/watson/SKILL.md</files>
  <action>
Replace SKILL.md line 59 (Path B step 5: "If `drafts:` non-empty, include pending amendments note") with the interactive AskUserQuestion gate. The replacement text must fit within ~7-8 lines to keep the total file at or under 200 lines (current: 193 lines, replacing 1 line).

Replacement for step 5:
```
5. If `drafts:` non-empty:
   Scan {blueprintPath}/LAYOUT.md, DESIGN.md, INTERACTION.md for [PENDING] lines;
   render grouped by file in design language. AskUserQuestion — header: "Pending",
   question: "[diff]\n\nWhat would you like to do?",
   options: ["Commit all", "Discard all", "Keep pending and continue"]
   - "Commit all": replace all [PENDING] with [COMMITTED] in blueprint files (Edit),
     set STATUS.md `drafts: []` (Edit), then proceed to Intent Classification
   - "Discard all": delete all [PENDING] lines from blueprint files (Edit),
     set STATUS.md `drafts: []` (Edit), confirm "Amendments discarded.", proceed
   - "Keep pending and continue": proceed to Intent Classification unchanged
```

CRITICAL constraints:
- Do NOT touch lines 134-138 (Tier 2 pre-build warning). That warning covers mid-session builds; this gate covers session-start. Both are necessary.
- Use Edit tool, not Write tool.
- After editing, run `wc -l ~/.claude/skills/watson/SKILL.md` and verify the count is <= 200. If over 200, tighten the wording (shorten handler descriptions, remove redundant words) until it fits.
- The diff assembly must scan blueprint files directly for [PENDING] lines (forward scan), NOT reverse-map from STATUS.md drafts: slugs.
  </action>
  <verify>
    <automated>wc -l ~/.claude/skills/watson/SKILL.md | awk '{if ($1 <= 200) print "PASS: "$1" lines"; else print "FAIL: "$1" lines (max 200)"}'</automated>
  </verify>
  <done>SKILL.md Path B step 5 contains AskUserQuestion gate with "Commit all", "Discard all", "Keep pending and continue" options. File is at or under 200 lines. Tier 2 pre-build warning (lines 134-138 area) is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Fix interaction.md dispatch frontmatter and 10-01-SUMMARY.md requirements_completed</name>
  <files>~/.claude/skills/watson/agents/interaction.md, .planning/phases/10-3-agent-parallel-dispatch/10-01-SUMMARY.md</files>
  <action>
Two independent edits:

1. **interaction.md** (line 3): Change `dispatch: foreground` to `dispatch: background`. Use Edit tool. This aligns the frontmatter with actual loupe.md behavior (loupe dispatches interaction as a background agent).

2. **10-01-SUMMARY.md** frontmatter: The `requirements_completed` key is absent entirely. Read the frontmatter first, then use Edit tool to insert `requirements_completed: [PARA-01, PARA-02, PARA-03, PARA-04]` as a new field before the closing `---` of the frontmatter block (after the `metrics:` block, before the closing `---` on line 29). Do NOT use Write tool — Edit only to preserve the rest of the file.
  </action>
  <verify>
    <automated>grep "dispatch:" ~/.claude/skills/watson/agents/interaction.md && grep "PARA-0" .planning/phases/10-3-agent-parallel-dispatch/10-01-SUMMARY.md</automated>
  </verify>
  <done>interaction.md shows "dispatch: background". 10-01-SUMMARY.md frontmatter contains "requirements_completed: [PARA-01, PARA-02, PARA-03, PARA-04]".</done>
</task>

</tasks>

<verification>
All three success criteria from the roadmap phase definition:

1. SKILL.md Path B step 5 presents AskUserQuestion review gate (Commit all / Discard all / Keep pending) when drafts: is non-empty — verified by reading the file and confirming AskUserQuestion text exists at step 5
2. interaction.md frontmatter declares dispatch: background — verified by `grep "dispatch: background" ~/.claude/skills/watson/agents/interaction.md`
3. 10-01-SUMMARY.md frontmatter includes PARA-01 through PARA-04 in requirements_completed — verified by `grep "PARA-0" .planning/phases/10-3-agent-parallel-dispatch/10-01-SUMMARY.md`

Additional constraint: `wc -l ~/.claude/skills/watson/SKILL.md` <= 200
</verification>

<success_criteria>
- SKILL.md Path B step 5 is an interactive AskUserQuestion gate, not a passive note
- SKILL.md is at or under 200 lines
- Tier 2 pre-build warning is preserved unchanged
- interaction.md declares dispatch: background
- 10-01-SUMMARY.md includes requirements_completed: [PARA-01, PARA-02, PARA-03, PARA-04]
</success_criteria>

<output>
After completion, create `.planning/phases/11-restore-drft04-review-gate-doc-fixes/11-01-SUMMARY.md`
</output>
