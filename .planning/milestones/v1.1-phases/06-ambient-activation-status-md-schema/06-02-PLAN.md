---
phase: 06-ambient-activation-status-md-schema
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - ~/.claude/rules/watson-ambient.md
  - ~/.claude/skills/watson/SKILL.md
  - ~/.claude/settings.json
autonomous: false
requirements: [AMBI-01, AMBI-02, AMBI-03]
gap_closure: true

must_haves:
  truths:
    - "User in a Prototype Playground context sees a suggestion to activate Watson without needing to know about /watson"
    - "Watson is ON or OFF for the entire session — no per-message ambient detection"
    - "/watson off deactivates Watson and deletes state file"
    - "Status line shows Watson: ON when active, nothing when inactive"
    - "After /clear, Watson state survives via state file and status line persists"
  artifacts:
    - path: "~/.claude/rules/watson-ambient.md"
      provides: "Path-specific rule that suggests Watson activation in Playground context"
      contains: "paths:"
    - path: "~/.claude/skills/watson/SKILL.md"
      provides: "Session toggle model with Routing section, state file write/delete, /watson off, welcome message"
      contains: "watson-active"
    - path: "~/.claude/settings.json"
      provides: "Status line hook and SessionStart/SessionEnd hooks for state lifecycle"
      contains: "watson"
  key_links:
    - from: "~/.claude/rules/watson-ambient.md"
      to: "/watson invocation"
      via: "Rule suggests user run /watson when in src/pages/**"
      pattern: "src/pages"
    - from: "~/.claude/skills/watson/SKILL.md"
      to: "/tmp/watson-active.json"
      via: "SKILL.md writes state file on load, deletes on /watson off"
      pattern: "watson-active"
    - from: "~/.claude/settings.json"
      to: "/tmp/watson-active.json"
      via: "Status line script reads state file; SessionEnd hook deletes it"
      pattern: "watson"
---

<objective>
Close the AMBI-01 gap: Watson offers to activate at session start when the user is in a Prototype Playground context, without requiring the user to know about /watson. Implement the full session toggle model (state file, status line, rules prompt, SKILL.md routing updates, /watson off, hooks).

Purpose: The paths: approach in SKILL.md frontmatter was incompatible with slash command registration. This plan uses path-specific rules (~/.claude/rules/) as the ambient trigger — they support paths: globs independently of skill registration, preserving /watson slash command.

Output: watson-ambient.md rule file, updated SKILL.md with session toggle model, updated settings.json with hooks and status line.
</objective>

<execution_context>
@/Users/austindick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austindick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ambient-activation-status-md-schema/06-CONTEXT.md
@.planning/phases/06-ambient-activation-status-md-schema/06-01-SUMMARY.md

<interfaces>
<!-- Current SKILL.md frontmatter (lines 1-5) — must preserve name, description, version -->
From ~/.claude/skills/watson/SKILL.md:
```yaml
---
name: watson
description: Design discussion and prototype building for the Faire Prototype Playground. Use when the user says "/watson", "build a prototype", "design discussion", or any design prototyping request for a frontend UI.
version: 1.1.0
---
```

<!-- Current SKILL.md Activation section starts at line 13, ends ~line 35 -->
<!-- Current SKILL.md is 162 lines — budget: 38 lines of headroom to 200 -->

<!-- settings.json structure — permissions array, need to add hooks -->
From ~/.claude/settings.json:
```json
{
  "permissions": {
    "allow": [...]
  }
}
```

<!-- State file protocol from CONTEXT.md decisions -->
State file: /tmp/watson-active.json
Written by: SKILL.md on load
Deleted by: /watson off, SessionEnd hook
Read by: status line script, SessionStart hook (clear recovery)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ambient rule, update SKILL.md to session toggle model, configure hooks</name>
  <files>~/.claude/rules/watson-ambient.md, ~/.claude/skills/watson/SKILL.md, ~/.claude/settings.json</files>
  <action>
**1. Create ~/.claude/rules/ directory if it does not exist, then create ~/.claude/rules/watson-ambient.md:**

```yaml
---
paths:
  - "src/pages/**"
---
```

Body content (3-5 lines, no more):
- Detect that the user is working in the Prototype Playground
- Suggest: "You're in the Prototype Playground. Use /watson to activate Watson for design discussion and prototyping."
- Do NOT auto-activate Watson or load SKILL.md content — just suggest the slash command
- If Watson is already active (user has invoked /watson this session), this rule is redundant — stay silent

**2. Update ~/.claude/skills/watson/SKILL.md — session toggle model:**

Changes to make (in order):

a) **Frontmatter** — keep name, description, version. Do NOT add paths. Bump version to 1.2.0.

b) **Opening paragraph** (line 9) — Replace the current text about "activates automatically via paths glob" with session toggle framing:
   "Watson is a session-level toggle — ON or OFF for the entire Claude Code session. Activate with /watson, deactivate with /watson off. When ON, Watson handles all design-related messages; Tier 0 passthrough still catches non-design work. When OFF, Watson is completely silent."

c) **Rename "Activation" section header to "Routing"** — The section assumes Watson is already ON (loaded via /watson). Keep all existing logic (blueprint gate, Tier 0, STATUS.md check, returning flow) unchanged.

d) **Add /watson off handling** — At the top of the Routing section, before the blueprint gate, add:
   "If the user's message is `/watson off`: delete /tmp/watson-active.json if it exists (bash: `rm -f /tmp/watson-active.json`), respond 'Watson deactivated for this session.', then exit. Do not proceed to blueprint gate."

e) **Add state file write** — After the opening description paragraph and before the Routing section, add a one-line instruction:
   "On load, write state file: `echo '{}' > /tmp/watson-active.json`"
   This fires every time SKILL.md loads (every /watson invocation), keeping the state file fresh.

f) **Add welcome message** — In the Routing section, after the blueprint gate passes (blueprint/ exists) and before Tier 0 check, on first invocation only (no prior Watson response in conversation):
   "Welcome: briefly confirm Watson is active, note which prototype directory was detected, invite the user to share what they'd like to work on. Keep to 2-3 lines."

g) **Drop the voice prefix instruction** — Remove the line about prefixing with `🕵️ Watson ►`. The status line replaces this as the persistent Watson signal. Per CONTEXT.md locked decision.

h) **Remove the prose on line 9 claiming paths glob activation** — this is now false. Replace with the session toggle text from (b).

**Line budget check:** Current = 162 lines. Net changes: replace ~2 lines opening text, rename 1 header, add ~6 lines (/watson off + state write + welcome), remove ~1 line (voice prefix). Estimated: ~167 lines. Under 200.

**3. Update ~/.claude/settings.json — add hooks:**

Read the current settings.json first. Add the following keys at the top level (alongside "permissions"):

a) **Status line hook** — Add a `hooks` object (or extend if it exists) with a `PostToolUse` or equivalent hook that runs a status line script. The exact hook format for Claude Code settings.json:

```json
"hooks": {
  "PostToolUse": [
    {
      "command": "if [ -f /tmp/watson-active.json ]; then echo 'Watson: ON'; fi",
      "matcher": ""
    }
  ],
  "SessionEnd": [
    {
      "command": "rm -f /tmp/watson-active.json",
      "matcher": ""
    }
  ],
  "SessionStart": [
    {
      "command": "if [ -f /tmp/watson-active.json ]; then echo '⚡ Watson was active before /clear. Run /watson to reactivate.'; fi",
      "matcher": "clear"
    }
  ]
}
```

Note: The exact hook key names and structure should follow Claude Code's settings.json hook convention. If the convention uses different keys (e.g., `"hooks"` at top level with event names as keys), adapt accordingly. The status line approach may need to use Claude Code's `statusLine` configuration if available — check the existing share-proto-statusline.js pattern mentioned in CONTEXT.md. If a statusLine config key exists, prefer that over PostToolUse hooks for the "Watson: ON" display.

**Important constraints:**
- Do NOT add paths: to SKILL.md frontmatter (breaks slash command — this is the whole reason for this plan)
- Do NOT remove or alter the blueprint gate, Tier 0, STATUS.md routing, or returning flow logic — only rename the section and add the toggle framing
- Keep SKILL.md under 200 lines
- State file is /tmp/watson-active.json — minimal JSON, just needs to exist
  </action>
  <verify>
    <automated>bash -c 'test -f ~/.claude/rules/watson-ambient.md && grep -q "src/pages" ~/.claude/rules/watson-ambient.md && echo "Rule: OK" || echo "Rule: MISSING"; grep -q "watson off" ~/.claude/skills/watson/SKILL.md && echo "Off handler: OK" || echo "Off handler: MISSING"; grep -q "watson-active" ~/.claude/skills/watson/SKILL.md && echo "State file ref: OK" || echo "State file ref: MISSING"; grep -q "Routing" ~/.claude/skills/watson/SKILL.md && echo "Routing section: OK" || echo "Routing section: MISSING"; wc -l < ~/.claude/skills/watson/SKILL.md | awk "{if (\$1 <= 200) print \"Lines: OK (\" \$1 \")\"; else print \"Lines: OVER (\" \$1 \")\"}"'</automated>
  </verify>
  <done>
    - watson-ambient.md exists with paths: ["src/pages/**"] and suggests /watson activation
    - SKILL.md has Routing section (not Activation), /watson off handler, state file write, welcome message, no voice prefix, no paths: in frontmatter, under 200 lines
    - settings.json has SessionEnd cleanup hook and SessionStart clear-recovery hook
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify session toggle model end-to-end in Playground</name>
  <action>
    Human verifies the full session toggle lifecycle in the Prototype Playground. This is a behavioral verification — Watson is a Claude Code skill whose output is Claude's response, not testable by static analysis.
  </action>
  <what-built>
    Session toggle model for Watson ambient activation:
    1. Path-specific rule at ~/.claude/rules/watson-ambient.md suggests /watson when in Playground
    2. SKILL.md updated with session toggle framing, /watson off, state file lifecycle
    3. settings.json hooks for state cleanup and /clear recovery
  </what-built>
  <how-to-verify>
    1. Open a prototype file in the Playground (e.g., src/pages/CheckoutFlow/index.tsx)
    2. Send any message — verify Watson suggests activation via the ambient rule (not auto-activating)
    3. Type /watson — verify Watson activates with a brief welcome message and writes /tmp/watson-active.json
    4. Send a design message — verify Watson responds (Routing section fires)
    5. Send "fix the TypeScript error" — verify Watson stays silent (Tier 0 passthrough)
    6. Type /watson off — verify Watson confirms deactivation and /tmp/watson-active.json is deleted
    7. Verify /watson slash command still appears in autocomplete (paths: not in SKILL.md frontmatter)
    8. Check that SKILL.md is under 200 lines: wc -l ~/.claude/skills/watson/SKILL.md
  </how-to-verify>
  <verify>
    <automated>bash -c 'test -f ~/.claude/rules/watson-ambient.md && echo "Rule file: EXISTS" || echo "Rule file: MISSING"; grep -c "paths:" ~/.claude/rules/watson-ambient.md 2>/dev/null || echo "0"; ! grep -q "^paths:" ~/.claude/skills/watson/SKILL.md && echo "SKILL.md paths: ABSENT (good)" || echo "SKILL.md paths: PRESENT (bad)"'</automated>
  </verify>
  <done>All 8 verification steps pass. Watson activates via suggestion, toggles on/off, slash command preserved.</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- watson-ambient.md rule exists with paths glob and suggestion text
- SKILL.md has no paths: in frontmatter, has Routing section, /watson off handler, state file protocol, under 200 lines
- settings.json has hooks for SessionEnd cleanup and SessionStart clear recovery
- /watson slash command still works (not broken by paths:)
- Ambient rule fires in Playground context and suggests activation
</verification>

<success_criteria>
1. User working in src/pages/** sees Watson activation suggestion without knowing about /watson (AMBI-01)
2. /watson activates Watson, /watson off deactivates — session-level toggle
3. State file at /tmp/watson-active.json tracks active state for status line and /clear recovery
4. All existing SKILL.md routing logic (blueprint gate, Tier 0, STATUS.md, returning flow) preserved
5. SKILL.md stays under 200 lines with /watson slash command intact
</success_criteria>

<output>
After completion, create `.planning/phases/06-ambient-activation-status-md-schema/06-02-SUMMARY.md`
</output>
