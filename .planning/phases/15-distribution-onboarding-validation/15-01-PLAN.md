---
phase: 15-distribution-onboarding-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/marketplace.json
  - README.md
autonomous: true
requirements:
  - DIST-01
  - DIST-02
  - DIST-03
  - DIST-04

must_haves:
  truths:
    - "marketplace.json exists and passes `claude plugin validate .`"
    - "README contains exact 3-step install sequence (marketplace add, plugin install, reload-plugins)"
    - "README documents Figma MCP setup inline with 3 steps"
    - "README documents ambient rule manual copy step"
    - "README includes auto-update enable instruction as optional step"
    - "README includes 2-3 troubleshooting items"
  artifacts:
    - path: ".claude-plugin/marketplace.json"
      provides: "Marketplace catalog enabling /plugin marketplace add austindick/watson"
      contains: '"name": "watson"'
    - path: "README.md"
      provides: "Onboarding document for designers and PMs"
      min_lines: 40
  key_links:
    - from: ".claude-plugin/marketplace.json"
      to: ".claude-plugin/plugin.json"
      via: "source: '.' points to repo root where plugin.json lives"
      pattern: '"source":\\s*"\\."'
    - from: "README.md"
      to: "skills/watson/references/watson-ambient.md"
      via: "Documents manual copy step for ambient rule"
      pattern: "watson-ambient\\.md"
---

<objective>
Create the marketplace catalog and onboarding README so teammates can install Watson from a GitHub repo with minimal friction.

Purpose: Without marketplace.json, the `/plugin marketplace add` command fails. Without README.md, teammates have no install instructions. These are the two artifacts that unlock distribution.
Output: marketplace.json (validated), README.md (complete onboarding doc)
</objective>

<execution_context>
@/Users/austindick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austindick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-distribution-onboarding-validation/15-CONTEXT.md
@.planning/phases/15-distribution-onboarding-validation/15-RESEARCH.md

<interfaces>
<!-- Existing plugin.json that marketplace.json must align with -->
From .claude-plugin/plugin.json:
```json
{
  "name": "watson",
  "version": "1.2.0",
  "description": "Design discussion and prototype building for the Faire Prototype Playground",
  "author": {
    "name": "Austin Dick",
    "url": "https://github.com/austindick"
  },
  "repository": "https://github.com/austindick/watson"
}
```

<!-- Ambient rule reference file that README must document -->
Existing: skills/watson/references/watson-ambient.md
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create marketplace.json and validate plugin</name>
  <files>.claude-plugin/marketplace.json</files>
  <action>
Create `.claude-plugin/marketplace.json` with the verified structure from research:

```json
{
  "name": "watson",
  "owner": {
    "name": "Austin Dick"
  },
  "plugins": [
    {
      "name": "watson",
      "source": ".",
      "description": "Design discussion and prototype building for the Faire Prototype Playground"
    }
  ]
}
```

Key constraints:
- Marketplace `name` MUST be "watson" (matches plugin name for `watson@watson` install syntax)
- Plugin `source` MUST be "." (repo root = plugin root, single-repo marketplace pattern)
- Plugin `name` MUST be "watson" (matches plugin.json name field)
- Do NOT include `version` in marketplace.json plugin entry — version comes from plugin.json

After creating, run `claude plugin validate .` from repo root to confirm no errors.
  </action>
  <verify>
    <automated>cd /Users/austindick/watson && test -f .claude-plugin/marketplace.json && python3 -c "import json; d=json.load(open('.claude-plugin/marketplace.json')); assert d['name']=='watson'; assert d['plugins'][0]['source']=='.'; assert d['plugins'][0]['name']=='watson'; print('PASS: marketplace.json valid')"</automated>
  </verify>
  <done>marketplace.json exists at .claude-plugin/marketplace.json, JSON is valid, name fields are "watson", source is "."</done>
</task>

<task type="auto">
  <name>Task 2: Write onboarding README with install flow, prerequisites, and troubleshooting</name>
  <files>README.md</files>
  <action>
Create `README.md` at repo root. Target audience: designers and PMs, not engineers. Keep it scannable.

**Structure (per CONTEXT.md locked decisions):**

1. **Beta badge** at top: `> **Beta** — internal Faire tool. DM Austin in Slack with questions.`

2. **One-sentence description:** Watson helps you design and prototype UI components in the Faire Prototype Playground using Claude Code.

3. **Prerequisites section:**
   - Claude Code installed and working
   - Figma MCP configured (see setup section below)

4. **Install section** — exact 3-step command sequence:
   ```
   /plugin marketplace add austindick/watson
   /plugin install watson@watson
   /reload-plugins
   ```
   Then the manual ambient rule copy step (DIST-04):
   - After install, copy the ambient rule file so Watson activates automatically in the Playground:
   ```bash
   cp ~/.claude/plugins/cache/watson/watson/*/skills/watson/references/watson-ambient.md ~/.claude/rules/watson-ambient.md
   ```
   Note: the glob `*` handles any version in the cache path.

5. **Figma MCP Setup section** — inline 3-step instructions (not a link):
   - Step 1: Install the Figma MCP server (link to Figma's MCP plugin page)
   - Step 2: Add Figma MCP to Claude Code settings (add to mcpServers in settings)
   - Step 3: Verify: ask Claude "can you access Figma?" — it should confirm the connection

6. **Auto-Update section** (optional, brief):
   - To receive future Watson updates automatically:
   ```
   /plugin -> Marketplaces tab -> select "watson" -> Enable auto-update
   ```
   - Or update manually anytime: `/plugin marketplace update watson`

7. **Versioning note:**
   - Watson version bumps on every push to main. Patch for fixes, minor for features.
   - No action needed from teammates — updates apply automatically if auto-update is enabled, or on next `/plugin marketplace update watson`.

8. **Troubleshooting section** — 2-3 items (Claude's discretion on exact wording):
   - `/watson` not found after install: Run `/reload-plugins`. If still missing, restart Claude Code.
   - Watson doesn't suggest itself in Playground: Check that `~/.claude/rules/watson-ambient.md` exists. Re-copy from install step if missing.
   - Figma MCP errors: Verify MCP server is running. Try "can you access Figma?" to diagnose.

**Constraints:**
- No architecture details, no agent explanations, no file tree (per CONTEXT.md)
- No CHANGELOG (deferred idea)
- Total length: aim for 60-90 lines of markdown
- Use simple language, numbered steps, code blocks for commands
  </action>
  <verify>
    <automated>cd /Users/austindick/watson && test -f README.md && python3 -c "
content = open('README.md').read()
checks = [
    ('Beta badge', 'Beta' in content),
    ('Install commands', 'marketplace add austindick/watson' in content),
    ('Plugin install', 'plugin install watson@watson' in content),
    ('reload-plugins', 'reload-plugins' in content),
    ('Figma MCP', 'Figma MCP' in content or 'Figma' in content),
    ('Ambient rule copy', 'watson-ambient.md' in content),
    ('Troubleshooting', 'Troubleshooting' in content or 'troubleshooting' in content),
    ('Auto-update', 'auto-update' in content.lower() or 'auto update' in content.lower()),
]
for name, passed in checks:
    status = 'PASS' if passed else 'FAIL'
    print(f'{status}: {name}')
assert all(p for _, p in checks), 'Some checks failed'
print('All README checks passed')
"</automated>
  </verify>
  <done>README.md exists at repo root with: beta badge, prerequisites, 3-step install, ambient rule copy step, inline Figma MCP setup, auto-update instructions, troubleshooting items. Scannable by non-engineers.</done>
</task>

</tasks>

<verification>
- `claude plugin validate .` returns no errors (marketplace.json + plugin.json both valid)
- README.md contains all sections per CONTEXT.md locked decisions
- `skills/watson/references/watson-ambient.md` still exists (not accidentally moved)
- No architecture details or advanced usage in README
</verification>

<success_criteria>
- marketplace.json is valid and enables `/plugin marketplace add austindick/watson`
- README documents the complete install flow including ambient rule copy
- README documents Figma MCP setup inline
- README includes troubleshooting for common failure modes
- All files pass validation
</success_criteria>

<output>
After completion, create `.planning/phases/15-distribution-onboarding-validation/15-01-SUMMARY.md`
</output>
