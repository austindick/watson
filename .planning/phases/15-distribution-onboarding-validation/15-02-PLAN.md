---
phase: 15-distribution-onboarding-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified: []
autonomous: false
requirements:
  - VALD-01
  - VALD-02
nyquist_override: "All 3 tasks are checkpoint:human-action — they require live Claude Code sessions and real-person testing on separate machines. No automated proxy exists for these validations. See 15-RESEARCH.md Validation Architecture: Framework = Manual checklist."

must_haves:
  truths:
    - "Author migration works: no double-firing hooks, /watson:watson invocable, statusLine shows"
    - "Fresh install works: beta tester follows README to working Watson without author help"
    - "enabledPlugins committed to Playground settings only after both validations pass"
  artifacts:
    - path: "~/faire/frontend/.claude/settings.json"
      provides: "enabledPlugins entry for Watson plugin"
      contains: '"watson@watson": true'
  key_links:
    - from: "README.md"
      to: "beta tester's machine"
      via: "Tester follows README steps exactly"
      pattern: "manual validation"
    - from: "~/faire/frontend/.claude/settings.json"
      to: ".claude-plugin/plugin.json"
      via: "enabledPlugins references watson@watson which resolves to installed plugin"
      pattern: '"watson@watson"'
---

<objective>
Validate that Watson works for both the author (migration) and a fresh installer (beta tester), then commit enabledPlugins to the Playground repo.

Purpose: The plugin infrastructure is built but unproven. This plan gates broader rollout on real-world validation — author self-test first, then beta tester fresh install. enabledPlugins is committed only after both pass.
Output: Validated install flow, enabledPlugins committed to Playground settings
</objective>

<execution_context>
@/Users/austindick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austindick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-distribution-onboarding-validation/15-CONTEXT.md
@.planning/phases/15-distribution-onboarding-validation/15-RESEARCH.md
@.planning/phases/15-distribution-onboarding-validation/15-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Push repo to GitHub and validate author migration (VALD-02)</name>
  <files>n/a — human action on GitHub and local Claude Code session</files>
  <action>
Push the Watson repo to GitHub as a public repo (austindick/watson), then run the author migration checklist to verify VALD-02.

**Step 1: Push to GitHub**
Create the public repo `austindick/watson` on GitHub (if not already created), set it as remote, and push:
```bash
# If repo doesn't exist yet, create it:
gh repo create austindick/watson --public --source=. --push

# If repo exists but remote isn't set:
git remote add origin git@github.com:austindick/watson.git
git push -u origin main
```

**Step 2: Author Migration Checklist (VALD-02)**
Run through each item. ALL must pass:

- [ ] Watson hooks do NOT appear in `~/.claude/settings.json` (check: `grep -c "watson" ~/.claude/settings.json` should be 0 for hook entries)
- [ ] Start a new Claude Code session in `~/faire/frontend`
- [ ] `/watson:watson` resolves and activates Watson
- [ ] After activation: `/tmp/watson-active.json` exists
- [ ] StatusLine shows in terminal (watson-statusline.js running)
- [ ] Run `/clear` — `/tmp/watson-session-end.json` is written (SessionEnd hook fires)
- [ ] No double-firing: recovery notification appears at most once per session start
- [ ] Old `~/.claude/skills/watson/` directory does not cause duplicate hook registration (if old dir still exists)
- [ ] Ambient rule at `~/.claude/rules/watson-ambient.md` triggers Watson suggestion in Playground context
  </action>
  <verify>
    <automated>cd /Users/austindick/watson && git remote -v | grep -q "austindick/watson" && echo "PASS: GitHub remote configured" || echo "FAIL: GitHub remote not set"</automated>
  </verify>
  <done>Author confirms all 9 checklist items pass. GitHub repo austindick/watson is public and pushed. No double-firing, /watson:watson works, statusLine shows, hooks fire correctly.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Beta tester fresh install validation (VALD-01)</name>
  <files>n/a — human action on beta tester's machine</files>
  <action>
Hand-pick a beta tester (designer or PM) who has never used Watson. Send them the GitHub repo link and ask them to follow the README with zero coaching.

**Fresh Install Checklist (VALD-01)**
After the beta tester attempts the install, confirm:

- [ ] Beta tester ran exactly the steps in README, no extra coaching from you
- [ ] `/watson:watson` is invocable after install
- [ ] Figma MCP connection works (if they completed the prerequisite)
- [ ] SessionStart hook fires (recovery message shows after `/clear` if a session was active)
- [ ] Ambient rule suggestion works in Playground context (if they copied watson-ambient.md)
- [ ] Beta tester did NOT need to contact Austin for help

**If beta tester hits issues:** Document the failure, fix the README or plugin as needed, have them retry. Do not proceed until a clean install succeeds.
  </action>
  <verify>
    <automated>echo "VALD-01 is a real-person test on a different machine. Human confirms pass/fail verbally."</automated>
  </verify>
  <done>Beta tester confirms working Watson install by following README alone. No author intervention required.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Commit enabledPlugins to Playground settings</name>
  <files>~/faire/frontend/.claude/settings.json</files>
  <action>
Both VALD-01 and VALD-02 have passed. Commit the enabledPlugins entry to the Playground's shared settings.

**Step 1: Add enabledPlugins to Playground settings**
Edit `~/faire/frontend/.claude/settings.json` and add (merge into existing object):
```json
{
  "enabledPlugins": {
    "watson@watson": true
  }
}
```

**Step 2: Commit to Playground repo**
```bash
cd ~/faire/frontend
git add .claude/settings.json
git commit -m "Enable Watson plugin for Playground users"
git push
```

**Step 3: Verify**
- Non-installers: the entry is silently ignored (no errors)
- Installers: Watson is available without per-user enabledPlugins config
  </action>
  <verify>
    <automated>cd ~/faire/frontend && python3 -c "import json; d=json.load(open('.claude/settings.json')); assert d.get('enabledPlugins',{}).get('watson@watson') == True; print('PASS: enabledPlugins contains watson@watson')" 2>/dev/null || echo "PENDING: enabledPlugins not yet committed (expected before Task 3 completes)"</automated>
  </verify>
  <done>enabledPlugins committed and pushed to Playground repo. Watson 1.2 Plugin Deployment milestone is complete.</done>
</task>

</tasks>

<verification>
- GitHub repo austindick/watson is public and contains marketplace.json + README.md
- Author migration checklist (VALD-02) passes completely
- Beta tester fresh install (VALD-01) passes without author intervention
- enabledPlugins is committed to ~/faire/frontend/.claude/settings.json
- Non-installers are unaffected by the enabledPlugins entry
</verification>

<success_criteria>
- VALD-02: Author's existing Watson install works via plugin with no double-firing or broken paths
- VALD-01: A fresh user follows the README and gets a working Watson without help
- enabledPlugins committed to Playground only after both validations pass
- Watson 1.2 Plugin Deployment milestone is complete
</success_criteria>

<output>
After completion, create `.planning/phases/15-distribution-onboarding-validation/15-02-SUMMARY.md`
</output>
