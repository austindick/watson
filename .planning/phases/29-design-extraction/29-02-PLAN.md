---
phase: 29-design-extraction
plan: "02"
type: execute
wave: 2
depends_on:
  - "29-01"
files_modified:
  - skills/core/SKILL.md
autonomous: true
requirements:
  - DSGN-01
  - DSGN-03

must_haves:
  truths:
    - "Core SKILL.md redirects explicit /design invocations to the standalone skill"
    - "Tier 2 ambient dispatches route to @skills/design/SKILL.md (not loupe.md)"
    - "Discuss->Build Chain dispatches route to @skills/design/SKILL.md (not loupe.md)"
    - "No circular references between core SKILL.md and skills/design/SKILL.md"
  artifacts:
    - path: "skills/core/SKILL.md"
      provides: "Updated routing layer dispatching standalone /design skill"
      contains: "@skills/design/SKILL.md"
  key_links:
    - from: "skills/core/SKILL.md"
      to: "skills/design/SKILL.md"
      via: "Tier 2 dispatch and Discuss->Build Chain"
      pattern: "@skills/design/SKILL.md"
---

<objective>
Update core SKILL.md routing to dispatch the standalone /design skill instead of the loupe.md subskill. Add /design redirect at shortcut level, update Tier 2 and Discuss->Build Chain dispatches, verify clean separation.

Purpose: Ambient routing and explicit /design invocations reach the standalone skill created in Plan 01.
Output: Updated skills/core/SKILL.md with all loupe.md references replaced.
</objective>

<execution_context>
@/Users/austindick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austindick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-design-extraction/29-01-SUMMARY.md

@skills/core/SKILL.md

<interfaces>
<!-- The core SKILL.md currently dispatches @skills/loupe.md in 3 locations. -->
<!-- Pattern reference: Phase 28-02 updated /think routing identically. -->

Current /design shortcut (line 42):
```
- `/design` → Tier 2 (build)
```
Target: Add redirect pattern matching /think and /play:
```
- `/design` → Respond: "Building is handled by /design directly. Just type /design." Exit. (Do not process /design in core — plugin.json routes /design to skills/design/SKILL.md)
```

Current Tier 2 dispatch (lines 88-91):
```
Dispatch `@skills/loupe.md` with:
- `blueprintPath`, `sections`, `hasFullFrame`, `fullFrameUrl`, `crossSectionFlows`
- `mode`: 'figma' (Figma URL detected) | 'prod-clone' (experience reference detected) | null (let loupe resolve)
- `experienceName`: extracted from message if mode='prod-clone', otherwise null
```
Target:
```
Dispatch `@skills/design/SKILL.md` with:
- `blueprintPath`, `sections`, `hasFullFrame`, `fullFrameUrl`, `crossSectionFlows`
- `mode`: 'figma' (Figma URL detected) | 'prod-clone' (experience reference detected) | null (let design resolve)
- `experienceName`: extracted from message if mode='prod-clone', otherwise null
```

Current Discuss->Build Chain dispatches (lines 112-113):
```
Dispatch `@skills/loupe.md` with ...
```
Target: All three dispatch instances → `@skills/design/SKILL.md`

Constraints section (line 140):
```
- Skills (/think, /design) and subskills (loupe.md) contain all execution logic
```
Target: Update to reflect that /design is now a standalone skill, not a subskill dispatch.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update core SKILL.md routing to dispatch /design skill</name>
  <files>skills/core/SKILL.md</files>
  <action>
Update skills/core/SKILL.md with these specific changes (same pattern as Phase 28-02 which updated /think routing):

1. **Shortcut redirect** (Intent Classification section, explicit shortcuts): Change `/design` from `→ Tier 2 (build)` to the redirect pattern:
   ```
   - `/design` → Respond: "Building is handled by /design directly. Just type /design." Exit. (Do not process /design in core — plugin.json routes /design to skills/design/SKILL.md)
   ```

2. **Tier 2 dispatch** (Routing section, Tier 2 block): Replace `@skills/loupe.md` with `@skills/design/SKILL.md`. Change "let loupe resolve" to "let design resolve" in the mode comment.

3. **Discuss->Build Chain dispatches** (3 instances): Replace all `@skills/loupe.md` with `@skills/design/SKILL.md`:
   - `ready_for_build` dispatch
   - `ready_for_hybrid_build` dispatch
   - The "Before dispatching loupe:" paragraph — change to "Before dispatching design:"

4. **Pending warning paragraph**: Change "Before dispatching loupe:" to "Before dispatching /design:" in the Tier 2 section.

5. **Constraints section**: Update "Skills (/think, /design) and subskills (loupe.md) contain all execution logic" to "Skills (/play, /think, /design, /save) contain all execution logic — agents are dispatched by skills, never by core SKILL.md"

6. **Colon variant line**: Update `/design:loupe` reference. Since /design is now standalone, this colon variant may still exist as a Claude Code independent skill — keep the line but update the note: `/think:discuss` or `/design:loupe` → these are legacy colon-variant skills handled by Claude Code; the standalone /think and /design skills are the canonical entry points.

7. **Red Flags table**: Check for any "dispatch loupe" or "loupe" references and update to "/design" or remove.

Do NOT change: Gate logic, Tier 0/1/3 routing, /think dispatch, /play redirect, /save dispatch, Library First Rule, line count constraint (keep under 165 lines).
  </action>
  <verify>
    <automated>grep -c "@skills/loupe.md\|@skills/loupe" skills/core/SKILL.md | awk '{if ($1 == 0) print "PASS: no loupe dispatch refs"; else print "FAIL: "$1" loupe dispatch refs remain"}'</automated>
    <automated>grep -c "@skills/design/SKILL.md" skills/core/SKILL.md | awk '{if ($1 >= 3) print "PASS: "$1" design dispatch refs"; else print "FAIL: "$1" design dispatch refs (need >=3)"}'</automated>
    <automated>grep -q "Building is handled by /design directly" skills/core/SKILL.md && echo "PASS: /design redirect present" || echo "FAIL: missing /design redirect"</automated>
    <automated>wc -l < skills/core/SKILL.md | awk '{if ($1 <= 165) print "PASS: "$1" lines (under 165 limit)"; else print "FAIL: "$1" lines (over 165 limit)"}'</automated>
    <automated>grep -c "loupe" skills/core/SKILL.md | awk '{if ($1 <= 1) print "PASS: "$1" loupe refs (0-1 acceptable for legacy note)"; else print "FAIL: "$1" loupe refs remain"}'</automated>
  </verify>
  <done>
    - /design shortcut redirects with "Building is handled by /design directly. Just type /design."
    - Tier 2 dispatches @skills/design/SKILL.md (not loupe.md)
    - All 3 Discuss->Build Chain dispatches use @skills/design/SKILL.md
    - Zero @skills/loupe.md references remain
    - File stays under 165 lines
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify /design and core separation is clean</name>
  <files>skills/core/SKILL.md, skills/design/SKILL.md</files>
  <action>
Run verification checks to confirm clean separation between core routing and standalone /design skill:

1. **No circular references**: Verify skills/design/SKILL.md does not dispatch back to skills/core/SKILL.md (it dispatches to @skills/think/SKILL.md for describe-only mode, and to @skills/core/agents/ for agent dispatches — these are correct cross-skill references, not circular).

2. **All 12 agents reachable**: Verify all 12 agent files exist at the paths referenced in skills/design/SKILL.md:
   ```bash
   for agent in decomposer layout design interaction builder reviewer consolidator surface-resolver source-layout source-design source-interaction librarian; do
     test -f skills/core/agents/$agent.md && echo "OK: $agent" || echo "MISSING: $agent"
   done
   ```

3. **No @skills/discuss.md references anywhere in the two files**: Both core SKILL.md and design SKILL.md should reference /think, not discuss.md.

4. **Plugin.json registration**: Verify skills/design/SKILL.md is registered in plugin.json commands array.

5. **Return status compatibility**: Verify skills/design/SKILL.md Phase -1 return status handling (ready_for_build, ready_for_hybrid_build, discussion_only, cancelled) matches what core SKILL.md Discuss->Build Chain expects from @skills/think/SKILL.md returns. These are the same statuses — /think returns them, core reads them, and /design also returns from describe-only via /think.

If any issues found, fix them in the appropriate file before completing.
  </action>
  <verify>
    <automated>grep -c "@skills/core/SKILL.md" skills/design/SKILL.md | awk '{if ($1 == 0) print "PASS: no circular ref to core"; else print "FAIL: "$1" circular refs to core SKILL.md"}'</automated>
    <automated>for agent in decomposer layout design interaction builder reviewer consolidator surface-resolver source-layout source-design source-interaction librarian; do test -f skills/core/agents/$agent.md && echo "OK: $agent" || echo "MISSING: $agent"; done</automated>
    <automated>grep -c "@skills/discuss.md\|@skills/discuss" skills/core/SKILL.md skills/design/SKILL.md | grep -v ":0$" | awk '{if (NR==0) print "PASS: no discuss.md refs"; else print "FAIL: discuss.md refs found"}'</automated>
    <automated>grep -q "skills/design/SKILL.md" .claude-plugin/plugin.json && echo "PASS: plugin.json registration" || echo "FAIL: not registered in plugin.json"</automated>
  </verify>
  <done>
    - No circular references between core and design SKILL.md
    - All 12 agent files exist at referenced paths
    - No @skills/discuss.md references in either file
    - skills/design/SKILL.md registered in plugin.json
    - Return status schema is compatible across /think, core, and /design
  </done>
</task>

</tasks>

<verification>
Run the full verification suite:
```bash
# Plan 01 output check
wc -l skills/design/SKILL.md
grep -c "@skills/core/agents/" skills/design/SKILL.md

# Plan 02 output check
grep -c "@skills/loupe.md" skills/core/SKILL.md
grep -c "@skills/design/SKILL.md" skills/core/SKILL.md
wc -l skills/core/SKILL.md

# Cross-file checks
grep -c "@skills/discuss.md" skills/core/SKILL.md skills/design/SKILL.md
grep -c "@skills/core/SKILL.md" skills/design/SKILL.md
```
</verification>

<success_criteria>
- Core SKILL.md redirects /design to standalone skill (matching /think and /play redirect pattern)
- All 3 loupe.md dispatches replaced with @skills/design/SKILL.md
- No circular references, no discuss.md references, no stale loupe.md dispatches
- Both files stay within line count limits (core: <=165, design: >=380)
- Plugin.json correctly routes /design to skills/design/SKILL.md
</success_criteria>

<output>
After completion, create `.planning/phases/29-design-extraction/29-02-SUMMARY.md`
</output>
