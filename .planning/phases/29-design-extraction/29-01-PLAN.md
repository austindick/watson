---
phase: 29-design-extraction
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/design/SKILL.md
autonomous: true
requirements:
  - DSGN-01
  - DSGN-02
  - DSGN-03
  - DSGN-11
  - DSGN-12

must_haves:
  truths:
    - "/design SKILL.md contains full pipeline logic — not a stub"
    - "Phase -1 standalone setup detects Figma URL, experience name, or bare invocation"
    - "All 12 agents are dispatched with correct paths from skills/design/ context"
    - "PRD (CONTEXT.md) is read when present, absent PRD does not block pipeline"
    - "Consolidator writes LAYOUT.md, DESIGN.md, INTERACTION.md to blueprint directory"
    - "No Watson or Loupe branding in user-facing text"
  artifacts:
    - path: "skills/design/SKILL.md"
      provides: "Full standalone /design pipeline skill"
      min_lines: 380
  key_links:
    - from: "skills/design/SKILL.md"
      to: "skills/core/agents/*.md"
      via: "@skills/core/agents/ dispatch paths"
      pattern: "@skills/core/agents/"
    - from: "skills/design/SKILL.md"
      to: "skills/think/SKILL.md"
      via: "describe-only mode dispatch"
      pattern: "@skills/think/SKILL.md"
    - from: "skills/design/SKILL.md"
      to: "blueprint/CONTEXT.md"
      via: "Phase 0a PRD read"
      pattern: "CONTEXT.md"
---

<objective>
Port the full pipeline from skills/core/skills/loupe.md into skills/design/SKILL.md as a standalone /design skill. All 12 agents dispatched, 3-mode entry working, PRD read integrated, blueprint files written.

Purpose: Users can run /design independently — no Watson session, no core SKILL.md routing required.
Output: skills/design/SKILL.md (~400+ lines) replacing the 11-line stub.
</objective>

<execution_context>
@/Users/austindick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austindick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-design-extraction/29-RESEARCH.md

@skills/core/skills/loupe.md
@skills/design/SKILL.md

<interfaces>
<!-- The executor ports loupe.md nearly verbatim into SKILL.md. Key modifications listed below. -->

Current stub frontmatter (KEEP EXACTLY):
```yaml
---
name: design
description: "Build pixel-perfect prototypes from Figma frames, production references, or design descriptions. Use when ready to build a prototype or run the design pipeline."
---
```

Agent dispatch path convention (from research open question 1):
- loupe.md uses `@agents/{name}.md` — resolves relative to skills/core/
- New SKILL.md lives in skills/design/ — `@agents/` would look in skills/design/agents/ (wrong)
- USE: `@skills/core/agents/{name}.md` for all 12 agent dispatches — explicit, unambiguous

The 12 agents to dispatch (all in skills/core/agents/):
- decomposer.md (foreground, Phase 1)
- surface-resolver.md (foreground, Phase 1)
- layout.md (background, Phase 2)
- design.md (background, Phase 2)
- interaction.md (background, Phase 2)
- source-layout.md (background, Phase 2)
- source-design.md (background, Phase 2)
- source-interaction.md (background, Phase 2)
- builder.md (background, Phase 3)
- reviewer.md (background, Phase 3)
- consolidator.md (background, Phase 4)
- librarian.md (utility, not dispatched in build pipeline but counted for DSGN-03)

Describe-only dispatch target:
- loupe.md dispatches `@skills/discuss.md` — OUTDATED
- New SKILL.md dispatches `@skills/think/SKILL.md` with `describeOnly: true`

Library path:
- loupe.md uses `${CLAUDE_PLUGIN_ROOT}/skills/core/library/LIBRARY.md` — KEEP AS-IS (library is shared at plugin level)

PRD read (DSGN-11 — new step not in loupe.md):
- Add Phase 0a before library resolution
- Read {blueprintPath}/CONTEXT.md if it exists
- If present: note as design context (agents already receive blueprintPath)
- If absent: proceed without error — standalone invocation is normal
- This is a reading step, not a validation gate
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port loupe.md to standalone /design SKILL.md</name>
  <files>skills/design/SKILL.md</files>
  <action>
Replace the 11-line stub in skills/design/SKILL.md with the full pipeline from skills/core/skills/loupe.md. This is a near-verbatim port with these specific modifications:

1. **Frontmatter**: Keep the existing stub frontmatter exactly (name: design, description: "Build pixel-perfect prototypes..."). Do NOT use loupe.md's frontmatter.

2. **Title and intro**: Replace "# Build Pipeline Subskill" with "# /design" and update the intro paragraph to say "You are the standalone build pipeline for the Design Toolkit" (drop "subskill" language). Keep the "Never mention agent names..." constraint paragraph.

3. **Agent dispatch paths** (11 changes): Replace ALL `@agents/{name}.md` dispatch references with `@skills/core/agents/{name}.md`. The agents live in skills/core/agents/ and the new SKILL.md is in skills/design/. The 11 dispatch locations are:
   - Phase 1: `@agents/decomposer.md` -> `@skills/core/agents/decomposer.md`
   - Phase 1: `@agents/surface-resolver.md` -> `@skills/core/agents/surface-resolver.md`
   - Phase 2 figma: `@agents/layout.md` -> `@skills/core/agents/layout.md`
   - Phase 2 figma: `@agents/design.md` -> `@skills/core/agents/design.md`
   - Phase 2 figma: `@agents/interaction.md` -> `@skills/core/agents/interaction.md`
   - Phase 2 prod-clone: `@agents/source-layout.md` -> `@skills/core/agents/source-layout.md`
   - Phase 2 prod-clone: `@agents/source-design.md` -> `@skills/core/agents/source-design.md`
   - Phase 2 prod-clone: `@agents/source-interaction.md` -> `@skills/core/agents/source-interaction.md`
   - Phase 3: `@agents/builder.md` -> `@skills/core/agents/builder.md`
   - Phase 3: `@agents/reviewer.md` -> `@skills/core/agents/reviewer.md`
   - Phase 4: `@agents/consolidator.md` -> `@skills/core/agents/consolidator.md`

4. **Describe-only dispatch** (1 change in Phase -1, Step 1, option 3): Replace `@skills/discuss.md` with `@skills/think/SKILL.md`. Keep all parameters (blueprintPath, describeOnly: true) and return status handling unchanged.

5. **PRD read step** (NEW — add as Phase 0a before existing Phase 0): Insert between Phase -1 and Phase 0:
   ```
   ## Phase 0a: PRD Read

   Read {blueprintPath}/CONTEXT.md if it exists.
   - If present and populated: note problem statement, design decisions, constraints. Agents receive blueprintPath and can read CONTEXT.md themselves for amendment context.
   - If absent or template-only: proceed without error — /design works without a prior /think session.
   - Never block pipeline entry on missing PRD.
   ```

6. **Branding cleanup**: Remove or replace any remaining Watson/Loupe references in user-facing strings:
   - The note about "locked decision from CONTEXT.md" referencing discuss.md dispatch can stay as an internal implementation note but update the dispatch target reference
   - Replace any "loupe" references in comments or notes with "design pipeline" or remove
   - The Phase -1 "discuss.md" references in the Note paragraph should reference "/think" instead
   - The Constraints section reference to "loupe.md" should reference "SKILL.md" or "this file"

7. **Constraints section**: Update the last constraint from "SKILL.md passes `blueprintPath` and `sections[]` to loupe.md; loupe.md does not ask the user for these" to describe the new standalone context: "When dispatched from core SKILL.md, blueprintPath and sections[] are passed directly — Phase -1 is skipped. When invoked standalone, Phase -1 resolves all inputs."

8. **Keep everything else verbatim**: All Phase 0-5 logic, error handling, designer-language progress reference table, all parameter passing, all conditional branching, all agent parameters (quietMode, libraryPaths, blueprintPath, etc.) — port exactly as-is.
  </action>
  <verify>
    <automated>wc -l skills/design/SKILL.md | awk '{if ($1 > 380) print "PASS: "$1" lines"; else print "FAIL: "$1" lines (need >380)"}'</automated>
    <automated>grep -c "@skills/core/agents/" skills/design/SKILL.md | awk '{if ($1 >= 11) print "PASS: "$1" agent dispatches"; else print "FAIL: "$1" agent dispatches (need >=11)"}'</automated>
    <automated>grep -q "@skills/think/SKILL.md" skills/design/SKILL.md && echo "PASS: /think dispatch present" || echo "FAIL: missing /think dispatch"</automated>
    <automated>grep -q "Phase 0a\|PRD Read" skills/design/SKILL.md && echo "PASS: PRD read step present" || echo "FAIL: missing PRD read step"</automated>
    <automated>grep -q "CONTEXT.md" skills/design/SKILL.md && grep -q "proceed without error\|works without\|absent" skills/design/SKILL.md && echo "PASS: graceful PRD fallback" || echo "FAIL: missing graceful PRD fallback"</automated>
    <automated>grep -c "@agents/" skills/design/SKILL.md | awk '{if ($1 == 0) print "PASS: no bare @agents/ paths"; else print "FAIL: "$1" bare @agents/ paths remain"}'</automated>
    <automated>grep -c "loupe" skills/design/SKILL.md | awk '{if ($1 == 0) print "PASS: no loupe references"; else print "WARN: "$1" loupe references (check if internal notes only)"}'</automated>
    <automated>grep -q "consolidator" skills/design/SKILL.md && echo "PASS: consolidator present (DSGN-12)" || echo "FAIL: missing consolidator"</automated>
    <automated>grep -q "figma\|Figma" skills/design/SKILL.md && grep -q "prod-clone" skills/design/SKILL.md && grep -q "Describe\|describe" skills/design/SKILL.md && echo "PASS: 3-mode entry (DSGN-02)" || echo "FAIL: missing mode(s)"</automated>
  </verify>
  <done>
    - skills/design/SKILL.md is 380+ lines with full pipeline logic (not a stub)
    - All 11 agent dispatch paths use @skills/core/agents/ prefix (no bare @agents/)
    - Describe-only mode dispatches @skills/think/SKILL.md (not discuss.md)
    - Phase 0a PRD read step exists with graceful fallback for missing CONTEXT.md
    - 3-mode entry works: Figma URL detection, prod-clone experience name, bare /design prompt
    - Consolidator dispatched in Phase 4 writing LAYOUT.md, DESIGN.md, INTERACTION.md
    - No Watson or Loupe branding in user-facing text
  </done>
</task>

</tasks>

<verification>
Run all automated checks in sequence:
```bash
wc -l skills/design/SKILL.md
grep -c "@skills/core/agents/" skills/design/SKILL.md
grep -c "@agents/" skills/design/SKILL.md
grep -q "@skills/think/SKILL.md" skills/design/SKILL.md && echo "think dispatch: OK"
grep -q "Phase 0a\|PRD Read" skills/design/SKILL.md && echo "PRD read: OK"
grep -q "consolidator" skills/design/SKILL.md && echo "consolidator: OK"
grep -c "loupe\|watson\|Watson\|Loupe" skills/design/SKILL.md
```
</verification>

<success_criteria>
- skills/design/SKILL.md contains the complete build pipeline (380+ lines)
- All 12 agents are referenceable via @skills/core/agents/ dispatch paths (11 explicit dispatches + librarian as shared utility)
- 3-mode entry (figma, prod-clone, describe) fully functional
- PRD read from /think is integrated with graceful absence handling
- Blueprint consolidation (LAYOUT.md, DESIGN.md, INTERACTION.md) dispatched via consolidator
- Zero bare @agents/ paths, zero Watson/Loupe user-facing references
</success_criteria>

<output>
After completion, create `.planning/phases/29-design-extraction/29-01-SUMMARY.md`
</output>
