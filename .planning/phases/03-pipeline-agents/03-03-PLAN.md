---
phase: 03-research-agents
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - "~/.claude/skills/watson/agents/decomposer.md"
  - "~/.claude/skills/watson/agents/layout.md"
  - "~/.claude/skills/watson/agents/design.md"
autonomous: true
gap_closure: true
requirements: [LOUP-01, LOUP-02, LOUP-03]

must_haves:
  truths:
    - "All three Figma-facing agents reference mcp__figma__get_figma_data as the primary MCP tool"
    - "No agent file contains mcp__figma__get_design_context or mcp__figma__get_metadata"
    - "decomposer.md does not prohibit mcp__figma__get_figma_data"
  artifacts:
    - path: "~/.claude/skills/watson/agents/decomposer.md"
      provides: "Corrected MCP tool references"
      contains: "mcp__figma__get_figma_data"
    - path: "~/.claude/skills/watson/agents/layout.md"
      provides: "Corrected MCP tool references"
      contains: "mcp__figma__get_figma_data"
    - path: "~/.claude/skills/watson/agents/design.md"
      provides: "Corrected MCP tool references"
      contains: "mcp__figma__get_figma_data"
  key_links:
    - from: "decomposer.md"
      to: "mcp__figma__get_figma_data"
      via: "Step 2 metadata fetch"
      pattern: "mcp__figma__get_figma_data"
    - from: "layout.md"
      to: "mcp__figma__get_figma_data"
      via: "Step 3 Figma fetch"
      pattern: "mcp__figma__get_figma_data"
    - from: "design.md"
      to: "mcp__figma__get_figma_data"
      via: "Step 3 Figma fetch"
      pattern: "mcp__figma__get_figma_data"
---

<objective>
Fix incorrect MCP tool names in decomposer.md, layout.md, and design.md agent files.

Purpose: The smoke test (03-02-SUMMARY.md) confirmed the actual Figma MCP tool is `mcp__figma__get_figma_data`, but all three Figma-facing agents reference nonexistent tools (`mcp__figma__get_design_context`, `mcp__figma__get_metadata`). Worse, decomposer.md line 16 explicitly prohibits the real tool. Agents would fail at Figma fetch if dispatched as-is.
Output: Three corrected agent files with working MCP tool references.
</objective>

<execution_context>
@/Users/austindick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austindick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.0-phases/03-research-agents/03-VERIFICATION.md
@.planning/milestones/v1.0-phases/03-research-agents/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix MCP tool names in all three Figma-facing agents</name>
  <files>~/.claude/skills/watson/agents/decomposer.md, ~/.claude/skills/watson/agents/layout.md, ~/.claude/skills/watson/agents/design.md</files>
  <action>
Fix MCP tool references in three agent files. The actual working Figma MCP tool is `mcp__figma__get_figma_data` per the smoke test findings.

**decomposer.md** — 6 references to fix:
- Line 14: replace `mcp__figma__get_metadata` with `mcp__figma__get_figma_data` in the "Never fetch page-level node" constraint
- Line 16: COMPLETELY REWRITE this line. Currently says "use mcp__figma__get_metadata. Do NOT use mcp__figma__get_figma_data (stale, may not resolve)." Change to: "**Correct MCP tool** — use `mcp__figma__get_figma_data`. This is the only available Figma MCP tool."
- Line 38: replace `mcp__figma__get_metadata` with `mcp__figma__get_figma_data`
- Line 41: replace `mcp__figma__get_metadata` with `mcp__figma__get_figma_data`
- Line 94: replace `mcp__figma__get_metadata` with `mcp__figma__get_figma_data`
- Line 99: replace `mcp__figma__get_metadata` with `mcp__figma__get_figma_data`

**layout.md** — 4 references to fix:
- Line 14: replace entire constraint. Currently says "Use mcp__figma__get_design_context (NOT get_figma_data or get_metadata)". Change to: "Use `mcp__figma__get_figma_data` as primary fetch tool — this is the only available Figma MCP tool"
- Line 54: replace `mcp__figma__get_design_context(fileKey, nodeId)` with `mcp__figma__get_figma_data(fileKey, nodeId)`
- Line 56: replace entire fallback line. Currently references get_metadata fallback and "Do not call get_figma_data under any circumstances." Change to: "**On truncation or tool error:** call `mcp__figma__get_figma_data` individually on each direct child nodeId to get data in smaller chunks."

**design.md** — 4 references to fix (same pattern as layout.md):
- Line 14: replace entire constraint. Currently says "Use mcp__figma__get_design_context (NOT get_figma_data or get_metadata)". Change to: "Use `mcp__figma__get_figma_data` as primary fetch tool — this is the only available Figma MCP tool"
- Line 56: replace `mcp__figma__get_design_context(fileKey, nodeId)` with `mcp__figma__get_figma_data(fileKey, nodeId)`
- Line 58: replace entire fallback line. Currently references get_metadata fallback and "Do not call get_figma_data under any circumstances." Change to: "**On truncation or tool error:** call `mcp__figma__get_figma_data` individually on each direct child nodeId to get data in smaller chunks."

After editing, verify zero occurrences of `get_design_context` or `get_metadata` remain in any of the three files.
  </action>
  <verify>
    <automated>grep -c "get_design_context\|get_metadata" ~/.claude/skills/watson/agents/decomposer.md ~/.claude/skills/watson/agents/layout.md ~/.claude/skills/watson/agents/design.md | grep -v ":0$" | wc -l | tr -d ' ' | grep -q "^0$" && echo "PASS: no stale tool names" || echo "FAIL: stale tool names remain"</automated>
  </verify>
  <done>All three agent files reference only mcp__figma__get_figma_data. Zero occurrences of mcp__figma__get_design_context or mcp__figma__get_metadata in any file. The prohibition on get_figma_data in decomposer.md line 16 is removed.</done>
</task>

</tasks>

<verification>
- `grep -rn "mcp__figma" ~/.claude/skills/watson/agents/decomposer.md ~/.claude/skills/watson/agents/layout.md ~/.claude/skills/watson/agents/design.md` shows only `mcp__figma__get_figma_data` references
- No file contains "Do NOT use" or "under any circumstances" prohibitions against the real tool
</verification>

<success_criteria>
All three Figma-facing Watson agents can be dispatched without failing at the Figma fetch step due to nonexistent MCP tool names.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/03-research-agents/03-03-SUMMARY.md`
</output>
