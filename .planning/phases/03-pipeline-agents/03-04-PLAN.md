---
phase: 03-research-agents
plan: "04"
type: execute
wave: 2
depends_on: ["03-03"]
files_modified:
  - ".planning/REQUIREMENTS.md"
autonomous: false
gap_closure: true
requirements: [LOUP-04, LOUP-05, LOUP-06]

must_haves:
  truths:
    - "MCP tool passthrough to background subagents is tested and the result (works or platform limitation) is documented"
    - "LOUP-04 is marked Deferred in REQUIREMENTS.md (interaction agent is a placeholder, not complete)"
    - "LOUP-05 status reflects actual parallel dispatch capability"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected requirement statuses for LOUP-04 and LOUP-05"
    - path: ".planning/milestones/v1.0-phases/03-research-agents/03-04-SUMMARY.md"
      provides: "Documentation of MCP passthrough test results"
  key_links: []
---

<objective>
Test MCP tool passthrough to background subagents and update requirement statuses based on findings.

Purpose: The smoke test (03-02) confirmed agent logic works but could not validate background dispatch because MCP tools did not pass through to spawned subagents. This plan tests whether that limitation persists after fixing tool names (03-03), documents the result, and corrects REQUIREMENTS.md statuses that were prematurely marked Complete.
Output: Documented MCP passthrough test result; corrected REQUIREMENTS.md with accurate LOUP-04 (Deferred) and LOUP-05 statuses.
</objective>

<execution_context>
@/Users/austindick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austindick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.0-phases/03-research-agents/03-VERIFICATION.md
@.planning/milestones/v1.0-phases/03-research-agents/03-02-SUMMARY.md
@.planning/milestones/v1.0-phases/03-research-agents/03-03-SUMMARY.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test MCP passthrough and parallel dispatch</name>
  <files>~/.claude/skills/watson/agents/layout.md</files>
  <action>
Attempt to dispatch `~/.claude/skills/watson/agents/layout.md` as a background subagent against the same Figma section used in the smoke test (Product List Container, nodeId 3984:37123, from the Catalog Connect adoption sprint file).

Provide the agent with:
- nodeId: "3984:37123"
- sectionName: "Product-List-Container"
- libraryPaths: the same paths used in 03-02 smoke test (read 03-02-SUMMARY.md for exact paths)
- blueprintPath: ".watson/blueprint" (or whatever was used in smoke test)

**If MCP tools work in the subagent:**
1. Record success
2. Dispatch both layout.md and design.md simultaneously as background agents against the same section
3. Record wall-clock timing: note start time before both dispatches, end time after both complete
4. Compare total time against single-agent time to confirm parallelism (total should be ~1x, not ~2x)

**If MCP tools do NOT work in the subagent** (agent fails at Figma fetch step):
1. Document this as a known Claude Code platform limitation: "MCP tools (e.g., mcp__figma__get_figma_data) are not available in spawned background subagents"
2. Note the workaround: "Run Figma-fetching agents in the orchestrator session directly; the agent instructions and output format are correct"
3. Note that the 03-02 smoke test already validated the agent logic produces correct output when run in the orchestrator session
  </action>
  <verify>
    <automated>echo "Manual test — results documented in SUMMARY"</automated>
  </verify>
  <done>MCP passthrough to subagents is tested. Result is either: (a) both agents dispatched in parallel with timing data, or (b) platform limitation documented with workaround noted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify MCP passthrough test results</name>
  <files>none</files>
  <action>
Human reviews MCP passthrough test results from Task 1 before proceeding to REQUIREMENTS.md updates.
  </action>
  <verify>
    <automated>echo "Human checkpoint — no automated verification"</automated>
  </verify>
  <done>User has reviewed and approved the MCP passthrough findings.</done>
  <what-built>MCP passthrough test executed. Review the test results and confirm the finding before requirement status updates.</what-built>
  <how-to-verify>
    1. Review the test output from Task 1
    2. If MCP worked: confirm parallel timing looks correct (not serialized)
    3. If MCP failed: confirm the platform limitation documentation is accurate
    4. Approve to proceed with REQUIREMENTS.md updates
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update REQUIREMENTS.md statuses</name>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
Read `.planning/REQUIREMENTS.md` and make these updates:

1. **LOUP-04** — Change from `[x]` to `[ ]` and append " (Deferred — interaction agent is documented placeholder, deferred to post-v1 per 03-CONTEXT.md)". In the Traceability table, change Status from "Complete" to "Deferred".

2. **LOUP-05** — Based on Task 1 results:
   - **If MCP passthrough worked:** Leave as `[x]` Complete. Update description if needed to note "2-agent parallel" instead of "3-agent" (since interaction is deferred).
   - **If MCP passthrough failed:** Change from `[x]` to `[ ]` and append " (Deferred — MCP tools do not pass through to background subagents; workaround: run in orchestrator session)". In Traceability table, change Status from "Complete" to "Deferred to platform fix".

Do NOT change any other requirement statuses. LOUP-01, LOUP-02, LOUP-03, LOUP-06 remain Complete.
  </action>
  <verify>
    <automated>grep -n "LOUP-04\|LOUP-05" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>LOUP-04 is marked Deferred. LOUP-05 status accurately reflects MCP passthrough test results. All other LOUP requirements unchanged.</done>
</task>

</tasks>

<verification>
- MCP passthrough test result is documented in SUMMARY
- REQUIREMENTS.md LOUP-04 status is "Deferred" (not "Complete")
- REQUIREMENTS.md LOUP-05 status reflects actual test findings
- No other requirement statuses were changed
</verification>

<success_criteria>
Phase 3 gaps are closed: agent files have correct MCP tool names (03-03), MCP passthrough is tested and documented (03-04), and REQUIREMENTS.md accurately reflects what is complete vs deferred.
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/03-research-agents/03-04-SUMMARY.md`
</output>
