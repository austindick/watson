---
phase: 10-3-agent-parallel-dispatch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/skills/watson/agents/interaction.md
  - ~/.claude/skills/watson/skills/loupe.md
  - ~/.claude/skills/watson/references/agent-contract.md
autonomous: true
requirements: [PARA-01, PARA-02, PARA-03, PARA-04]

must_haves:
  truths:
    - "All three agents (layout, design, interaction) are dispatched as background agents in the same parallel block per figma section"
    - "Builder does not start until all three agent outputs are present or interaction has fallen back to null"
    - "Interaction agent failure does not block the pipeline — interactionPath falls back to null after one silent retry"
    - "Discuss-only sections skip all three agents identically (referenceType = figma guard wraps all dispatches)"
  artifacts:
    - path: "~/.claude/skills/watson/agents/interaction.md"
      provides: "Refactored interaction agent with direct Figma MCP fetch for component detection"
      contains: "Fetch the Figma node using nodeId"
    - path: "~/.claude/skills/watson/skills/loupe.md"
      provides: "3-agent parallel dispatch in Phase 2 with unified wait gate"
      contains: "Dispatch @agents/interaction.md as **background** agent in parallel"
    - path: "~/.claude/skills/watson/references/agent-contract.md"
      provides: "Updated interaction agent dispatch params and mode note"
  key_links:
    - from: "~/.claude/skills/watson/skills/loupe.md"
      to: "~/.claude/skills/watson/agents/interaction.md"
      via: "parallel dispatch with nodeId, interactionContext, crossSectionFlows (no layoutPath/designPath)"
      pattern: "Dispatch.*interaction.*background.*parallel"
    - from: "~/.claude/skills/watson/skills/loupe.md"
      to: "wait gate"
      via: "INTERACTION.md existence check with retry-once + null fallback"
      pattern: "INTERACTION.md missing.*retry.*null"
---

<objective>
Convert the Watson loupe pipeline from sequential interaction agent dispatch to simultaneous 3-agent parallel dispatch per Figma section.

Purpose: Eliminates the sequential bottleneck where the interaction agent waits for layout + design to complete before running. All three research agents now run in parallel, with no increase in total build time.

Output: Updated interaction.md (direct Figma fetch), loupe.md (parallel dispatch + unified wait gate), agent-contract.md (updated params/notes).
</objective>

<execution_context>
@/Users/austindick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austindick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-3-agent-parallel-dispatch/10-CONTEXT.md
@.planning/phases/10-3-agent-parallel-dispatch/10-RESEARCH.md

@~/.claude/skills/watson/agents/interaction.md
@~/.claude/skills/watson/skills/loupe.md
@~/.claude/skills/watson/references/agent-contract.md

<interfaces>
<!-- Current interaction.md inputs (will be modified): -->
- nodeId — section nodeId (accepted per contract; reserved for Phase 10)
- sectionName — used to construct output path
- interactionContext — optional structured JSON
- crossSectionFlows — optional array
- blueprintPath, libraryPaths, watsonMode — shared params
- layoutPath — path to LAYOUT.md (REMOVE in this plan)
- designPath — path to DESIGN.md (REMOVE in this plan)

<!-- Current interaction.md Constraint #4 (will be replaced): -->
4. No Figma calls — work entirely from LAYOUT.md, DESIGN.md, and library PAGE.md files

<!-- Current interaction.md Constraint #5 (will be replaced): -->
5. No MCP tool calls of any kind

<!-- Current loupe.md Phase 2 structure (lines 88-148): -->
- Lines 88-112: Parallel layout + design dispatch per figma section + wait gate for 2 agents
- Lines 114-148: Sequential interaction dispatch AFTER layout+design complete (TO BE DELETED)

<!-- Current loupe.md Designer-Language Progress Reference has row: -->
| Interaction agent running | "Detailing interaction states for the [section name]..." |
<!-- This row is REMOVED in Phase 10 -->

<!-- agent-contract.md interaction row: -->
| interaction | agents/interaction.md | foreground* | nodeId (string), interactionContext? (string) | .watson/sections/{sectionName}/INTERACTION.md |
<!-- layoutPath/designPath not listed here but are in agent inputs; footnote needs update -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor interaction agent for direct Figma fetch</name>
  <files>~/.claude/skills/watson/agents/interaction.md</files>
  <action>
Modify `agents/interaction.md` to remove the sequential dependency on LAYOUT.md and DESIGN.md. Four changes:

**1. Remove `layoutPath` and `designPath` from Inputs section.**
Remove these two rows entirely from the Inputs table. Do not keep as optional — clean removal per research recommendation. Also update the `nodeId` description: remove the parenthetical "(accepted per contract; reserved for Phase 10 parallel dispatch — not used in Phase 9)" and replace with a clear description: "Figma section nodeId — used to fetch the node for component identification".

**2. Replace Constraint #4.**
Current: `4. No Figma calls — work entirely from LAYOUT.md, DESIGN.md, and library PAGE.md files`
New: `4. Component detection uses direct Figma MCP fetch via nodeId — read the Figma node to identify DS components present in the section. Do NOT depend on layoutPath or designPath (not provided in parallel dispatch mode).`

**3. Replace Constraint #5.**
Current: `5. No MCP tool calls of any kind`
New: `5. Single MCP call permitted: Figma node fetch via nodeId for component detection (Step 1). No other MCP calls.`

**4. Replace Step 1 entirely.**
Current Step 1 reads LAYOUT.md and DESIGN.md to extract component names from the Component Tree block and Component Mapping table.

New Step 1: "Fetch Figma node and extract component names"

```
### Step 1: Fetch Figma node and extract component names

Fetch the Figma node using `nodeId` via `mcp__figma__get_figma_data`. From the returned node structure, identify DS components present in the section by inspecting component names in the node tree (layer names that correspond to design system components).

Extract component names from the Figma node hierarchy. Look for:
- Component instance names (these map to DS component names)
- Layer names that match known DS component patterns

**Normalize each name to kebab-case** for CHAPTER.md lookup: insert a hyphen before each uppercase letter (after the first), lowercase everything, strip any leading hyphen. Handle special cases:
```

Keep the existing normalization table (Input/Select, IconButton, TextInput, etc.) exactly as-is — copy it from the current Step 1. The normalization table is unchanged.

All other steps (Step 2 through Step 5) remain unchanged. Step 2 still reads CHAPTER.md and PAGE.md for states. Steps 3-5 still build tiers and write INTERACTION.md.
  </action>
  <verify>
    <automated>grep -c "layoutPath\|designPath" ~/.claude/skills/watson/agents/interaction.md | grep "^0$" && grep -c "mcp__figma__get_figma_data" ~/.claude/skills/watson/agents/interaction.md | grep -v "^0$" && grep -c "No Figma calls" ~/.claude/skills/watson/agents/interaction.md | grep "^0$" && echo "PASS"</automated>
  </verify>
  <done>interaction.md has no references to layoutPath/designPath as inputs, Constraint #4 permits Figma MCP fetch, Constraint #5 permits the single MCP call, Step 1 uses direct Figma node fetch via mcp__figma__get_figma_data, normalization table preserved</done>
</task>

<task type="auto">
  <name>Task 2: Wire parallel dispatch in loupe.md and update agent-contract.md</name>
  <files>~/.claude/skills/watson/skills/loupe.md, ~/.claude/skills/watson/references/agent-contract.md</files>
  <action>
Two files to update:

**A. loupe.md — Phase 2 rewrite (lines 88-148)**

Replace the entire Phase 2 section (from `## Phase 2:` through the end of the discuss-only skip rules before `## Phase 3:`) with the new parallel structure. Critical changes:

1. **Add interaction to the parallel dispatch block.** After the design agent dispatch (current line 110), add a third dispatch in the same loop:

```
4. Dispatch `@agents/interaction.md` as **background** agent in parallel:
   nodeId: {section.nodeId}
   sectionName: {section.name}
   interactionContext: {sections[i].interactionContext}
   crossSectionFlows: {crossSectionFlows}
   blueprintPath: {blueprintPath}
   libraryPaths: {libraryPaths}
   watsonMode: true
```

Note: no layoutPath or designPath in the dispatch params.

2. **Delete the entire sequential interaction dispatch block** (current lines 114-148 starting with "**Interaction agent dispatch (sequential per figma section):**"). This ENTIRE block is removed — not supplemented. This is critical to avoid double-dispatch (Pitfall 1 from research).

3. **Extend the wait gate** (current line 112). Replace:
   `Wait for every layout and design agent across all sections to complete before proceeding to Phase 3.`
   With:
   ```
   **After ALL sections have been dispatched:** Wait for every layout, design, and interaction agent across all sections to complete before proceeding to Phase 3. Do NOT begin Phase 3 for any section until all research agents have finished. Verify each expected output file:
   - LAYOUT.md missing -> layoutPath: null (existing fallback rule)
   - DESIGN.md missing -> designPath: null (existing fallback rule)
   - INTERACTION.md missing -> retry interaction agent once silently. On second failure: interactionPath: null
   ```

   This migrates the retry-once + null fallback logic from the deleted sequential block into the unified wait gate (Pitfall 2 from research).

4. **Keep the discuss-only skip rule** but consolidate to one line after the wait gate:
   `For sections where referenceType = "discuss-only": **skip Phase 2 entirely.** These sections have no Figma node. Blueprint files were populated by discuss. Proceed directly to Phase 3.`
   Remove the duplicate discuss-only skip line that currently appears at line 146.

5. **Update the Designer-Language Progress Reference table** at the bottom of loupe.md. Remove the row:
   `| Interaction agent running | "Detailing interaction states for the [section name]..." |`
   And update the layout+design row description to: `| Layout + design + interaction agents running | "Mapping out the [section name]..." |`

**B. agent-contract.md updates**

1. In the Agent Registry table, update the interaction row. Change Agent-Specific Params from:
   `nodeId (string), interactionContext? (string — pre-gathered context from discuss)`
   To:
   `nodeId (string), interactionContext? (structured JSON — pre-gathered context from discuss), crossSectionFlows? (array — cross-section flows from discuss)`

2. Update the Interaction Agent Footnote section. Add a sentence at the end:
   `In loupe.md parallel dispatch (Phase 10+), interaction always runs as background alongside layout and design — watsonMode=true is always set. layoutPath and designPath are not provided in parallel dispatch mode.`

3. In the Agent Registry table, update interaction dispatch mode from `foreground*` to `foreground*` (no change to the cell, but the footnote update above covers the behavioral change).
  </action>
  <verify>
    <automated>grep -c "sequential per figma section" ~/.claude/skills/watson/skills/loupe.md | grep "^0$" && grep -c "Detailing interaction states" ~/.claude/skills/watson/skills/loupe.md | grep "^0$" && grep -c "interaction.*background.*parallel" ~/.claude/skills/watson/skills/loupe.md | grep -v "^0$" && grep -c "INTERACTION.md missing" ~/.claude/skills/watson/skills/loupe.md | grep -v "^0$" && grep -c "crossSectionFlows" ~/.claude/skills/watson/references/agent-contract.md | grep -v "^0$" && echo "PASS"</automated>
  </verify>
  <done>loupe.md Phase 2 dispatches all three agents in the same parallel loop, sequential interaction block is fully deleted, wait gate checks LAYOUT.md + DESIGN.md + INTERACTION.md with retry-once for interaction, "Detailing interaction states..." progress row removed, agent-contract.md footnote updated with parallel dispatch note and crossSectionFlows param added</done>
</task>

</tasks>

<verification>
Read the final loupe.md Phase 2 section end-to-end and confirm:
1. Interaction dispatch appears INSIDE the per-section parallel loop (not after the wait gate)
2. No sequential interaction dispatch block remains anywhere in the file
3. Wait gate explicitly checks INTERACTION.md with retry-once + null fallback
4. "Detailing interaction states..." does not appear anywhere in the file
5. The discuss-only skip rule appears exactly once

Read interaction.md and confirm:
1. No layoutPath or designPath in Inputs
2. Constraint #4 references Figma MCP fetch (not "No Figma calls")
3. Constraint #5 permits the single MCP call
4. Step 1 uses mcp__figma__get_figma_data
5. Normalization table is intact

Read agent-contract.md and confirm:
1. Interaction agent params include crossSectionFlows
2. Footnote mentions parallel dispatch mode
</verification>

<success_criteria>
- PARA-01: loupe.md dispatches layout, design, and interaction as background agents in the same per-section loop
- PARA-02: Wait gate requires all three agent outputs (or null fallback) before Phase 3
- PARA-03: INTERACTION.md missing triggers retry-once then interactionPath: null — does not block pipeline
- PARA-04: referenceType = "figma" guard wraps all three dispatches; discuss-only sections skip Phase 2 entirely
</success_criteria>

<output>
After completion, create `.planning/phases/10-3-agent-parallel-dispatch/10-01-SUMMARY.md`
</output>
