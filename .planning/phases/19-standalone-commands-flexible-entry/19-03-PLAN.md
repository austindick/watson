---
phase: 19-standalone-commands-flexible-entry
plan: 03
type: execute
wave: 3
depends_on: ["19-01", "19-02"]
files_modified:
  - skills/core/skills/loupe.md
  - skills/core/utilities/watson-init.md
autonomous: true
gap_closure: true
requirements: [STND-02, FLEX-01, FLEX-02]

must_haves:
  truths:
    - "Standalone loupe auto-resolves targetFilePath from blueprintPath without asking the user"
    - "Playground URL resolves to the correct branch even when branch uses prototype/* or other non-watson/* naming"
    - "Watson offers explicit scaffold options when branch health check finds missing blueprint"
  artifacts:
    - path: "skills/core/skills/loupe.md"
      provides: "Phase -1 Step 5 targetFilePath derivation from blueprintPath"
      contains: "targetFilePath"
    - path: "skills/core/utilities/watson-init.md"
      provides: "Branch search in url and slug handlers; explicit scaffold AskUserQuestion in branch-list health check"
      contains: "git branch --list"
  key_links:
    - from: "skills/core/skills/loupe.md Phase -1 Step 5"
      to: "skills/core/skills/loupe.md Phase 3 Resolve targetFilePath"
      via: "targetFilePath set in preamble, consumed by Phase 3"
      pattern: "targetFilePath"
    - from: "skills/core/utilities/watson-init.md Phase 0C url handler"
      to: "git branch search"
      via: "branch search step before filesystem find"
      pattern: "git branch --list"
---

<objective>
Close 3 UAT gaps from Phase 19 testing: (1) loupe standalone asks user for write target instead of auto-resolving, (2) Playground URL only searches watson/* branches missing prototype/* and other naming, (3) branch-list health check doesn't offer concrete scaffold options.

Purpose: Fix user-facing friction discovered during UAT — these are the last 3 issues before Phase 19 ships clean.
Output: Updated loupe.md and watson-init.md with gap fixes applied.
</objective>

<execution_context>
@/Users/austindick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austindick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-standalone-commands-flexible-entry/19-01-SUMMARY.md
@.planning/phases/19-standalone-commands-flexible-entry/19-02-SUMMARY.md

@skills/core/skills/loupe.md
@skills/core/utilities/watson-init.md

<interfaces>
<!-- Key sections the executor needs to understand -->

From skills/core/skills/loupe.md Phase -1 Step 5 (lines 40-47):
```markdown
**Step 5: Set pipeline inputs and proceed to Phase 0**
Set the standard loupe inputs from what was gathered:
- `blueprintPath`: resolved from Step 2
- `sections`: null (standalone doesn't have pre-built sections unless returning from a prior discuss)
- `hasFullFrame`: true if Figma URL detected, false otherwise
- `fullFrameUrl`: extracted Figma URL or null
- `crossSectionFlows`: null
```
Note: targetFilePath is NOT set here — that's the gap. Phase 3 (line 194-197) falls back to asking the user.

From skills/core/skills/loupe.md Phase 3 Resolve targetFilePath (lines 194-197):
```markdown
**Resolve targetFilePath:**
- If blueprint already records a target file (from prior build), use it.
- Otherwise, ask the user once: "Where should I write the code? Share the prototype file path." Use that path for all sections.
- Derive `sectionScope` from the section name (e.g., section "hero" → sectionScope "hero").
```

From skills/core/utilities/watson-init.md Phase 0C Step 2 url handler (line 66):
```markdown
- **url:** Extract slug from URL. Resolve via slug logic below.
```
Note: url handler extracts slug then falls through to slug handler which only does filesystem find — no branch search.

From skills/core/utilities/watson-init.md Phase 0C Step 2 slug handler (line 68):
```markdown
- **slug:** Use the resolved directory from `find`. Then follow directory logic above.
```
Note: slug handler only does `find src/pages/ -maxdepth 1 -iname "{slug}"` — never searches git branches.

From skills/core/utilities/watson-init.md Branch List step 6 health check (line 335):
```markdown
- **Health check after switch:** discover blueprint path via Blueprint Discovery (filesystem variant since branch is now checked out). Verify `{blueprintPath}/STATUS.md` exists. If missing, inform user and offer to re-scaffold via watson-init.
```
Note: "inform user and offer to re-scaffold" is vague — no AskUserQuestion, no concrete options. Compare to Phase 0C directory handler (line 67) which has the explicit pattern.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix loupe standalone targetFilePath and watson-init URL/slug branch search</name>
  <files>skills/core/skills/loupe.md, skills/core/utilities/watson-init.md</files>
  <action>
**Gap 1 — loupe.md Phase -1 Step 5: Add targetFilePath derivation**

In loupe.md Phase -1 Step 5, add a `targetFilePath` bullet between the existing `blueprintPath` and `sections` bullets. The derivation logic:

1. Derive `protoDir` from `blueprintPath` by stripping the trailing `/blueprint` segment (same derivation used in Phase 0).
2. Check if `protoDir` contains an `index.tsx` file — if yes, set `targetFilePath = {protoDir}/index.tsx`.
3. If no `index.tsx`, check for any single `.tsx` file in `protoDir` — if exactly one exists, set `targetFilePath` to that file.
4. If multiple `.tsx` files or none: check STATUS.md frontmatter for a previously-recorded target file path. If found, use it.
5. If still unresolved: set `targetFilePath = null` (Phase 3 will ask the user as its existing fallback).

The step should read:
```
- `targetFilePath`: derived from blueprintPath — strip `/blueprint` to get protoDir, then resolve: (1) `{protoDir}/index.tsx` if it exists, (2) the sole `.tsx` file in protoDir if exactly one exists, (3) STATUS.md recorded target file, (4) null (Phase 3 fallback asks user)
```

Also update the Phase 3 "Resolve targetFilePath" section (around line 194) to document the standalone preamble path. Replace the current bullet list with:
```
**Resolve targetFilePath:**
- If `targetFilePath` was already set by Phase -1 (standalone) or the caller, use it.
- If blueprint already records a target file (from prior build), use it.
- Otherwise, ask the user once: "Where should I write the code? Share the prototype file path." Use that path for all sections.
- Derive `sectionScope` from the section name (e.g., section "hero" -> sectionScope "hero").
```

**Gap 2 — watson-init.md Phase 0C Step 2: Add branch search to url and slug handlers**

In watson-init.md Phase 0C Step 2, replace the current `url` and `slug` resolution bullets with expanded versions that search git branches BEFORE falling through to filesystem:

For **url** handler (replace the single-line bullet):
```
- **url:** Extract slug from URL path (last path segment, strip query params). Then:
  1. Search all local branches for the slug: `git branch --list "*${slug}*" 2>/dev/null`. Filter results to branches that contain the slug as a path segment (not substring noise).
  2. If exactly one branch matches: Run Auto-Commit Guard. `git checkout {matched_branch}`. Discover blueprint path via Blueprint Discovery (filesystem variant). If found, set `blueprintPath` and proceed to Step 3. If no blueprint, offer scaffold (same as directory handler pattern).
  3. If multiple branches match: AskUserQuestion — header: "Multiple matches", question: "Found multiple branches matching '{slug}':", options: [list of matching branch names, "None of these"]. On selection, checkout and discover blueprint. On "None of these", fall through to filesystem.
  4. If no branch matches: fall through to slug filesystem resolution below.
```

For **slug** handler (replace the single-line bullet):
```
- **slug:** First search git branches: `git branch --list "*${userInput}*" 2>/dev/null`. Filter to branches containing the slug as a path segment.
  1. If exactly one branch matches: checkout and discover blueprint (same as url handler step 2).
  2. If multiple matches: present selection (same as url handler step 3).
  3. If no branch matches: attempt filesystem resolution: `find src/pages/ -maxdepth 1 -iname "{userInput}" -type d 2>/dev/null | head -1`. If found, follow directory logic. If not found, fall through to unknown handler.
```
  </action>
  <verify>
    <automated>grep -q "strip.*/blueprint" skills/core/skills/loupe.md && grep -q "Phase -1 (standalone)" skills/core/skills/loupe.md && grep -c "git branch --list" skills/core/utilities/watson-init.md | xargs test 6 -le && echo "PASS"</automated>
  </verify>
  <done>
- loupe.md Phase -1 Step 5 includes targetFilePath derivation from blueprintPath (protoDir/index.tsx -> sole .tsx -> STATUS.md -> null)
- loupe.md Phase 3 "Resolve targetFilePath" documents the standalone preamble path as first priority
- watson-init.md Phase 0C url handler searches all local branches before falling to filesystem
- watson-init.md Phase 0C slug handler searches all local branches before falling to filesystem find
- Multiple branch matches present selection; single match auto-selects
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix watson-init branch-list health check scaffold offer</name>
  <files>skills/core/utilities/watson-init.md</files>
  <action>
**Gap 3 — watson-init.md Branch List step 6 health check: Add concrete scaffold AskUserQuestion**

In watson-init.md "Branch List and Switching" step 6, replace the vague health check line:
```
- **Health check after switch:** discover blueprint path via Blueprint Discovery (filesystem variant since branch is now checked out). Verify `{blueprintPath}/STATUS.md` exists. If missing, inform user and offer to re-scaffold via watson-init.
```

With a concrete version that matches the Phase 0C directory handler pattern:
```
- **Health check after switch:** discover blueprint path via Blueprint Discovery (filesystem variant since branch is now checked out). Verify `{blueprintPath}/STATUS.md` exists. If STATUS.md is missing or blueprint path could not be discovered:
  AskUserQuestion — header: "Missing Blueprint", question: "This branch doesn't have a blueprint yet. Would you like to create one?", options: ["Yes, create blueprint", "Cancel"]
  - "Yes, create blueprint": determine prototype directory from branch name (strip `watson/` prefix, find matching directory in `src/pages/`). If directory found, scaffold 5 template files in `{protoDir}/blueprint/` using the Template Content section. Set `blueprintPath`. If no matching directory, ask user for the prototype path.
  - "Cancel": return to branch list.
```

This makes the scaffold offer explicit with AskUserQuestion and concrete follow-through, matching the pattern already used in Phase 0C's directory handler (line 67 of watson-init.md).
  </action>
  <verify>
    <automated>grep -A 5 "Health check after switch" skills/core/utilities/watson-init.md | grep -q "AskUserQuestion" && grep -q "Missing Blueprint" skills/core/utilities/watson-init.md && echo "PASS"</automated>
  </verify>
  <done>
- Branch List step 6 health check uses AskUserQuestion with "Yes, create blueprint" / "Cancel" options
- Scaffold follow-through resolves prototype directory from branch name and creates 5 template files
- Pattern matches Phase 0C directory handler's explicit scaffold offer
  </done>
</task>

</tasks>

<verification>
All three UAT gaps addressed:
1. Re-run UAT Test 2 (standalone loupe): `/watson:loupe` should auto-resolve targetFilePath from blueprintPath without asking "Where should I write the code?"
2. Re-run UAT Test 6 (URL flexible continue): Paste a Playground URL — Watson should search all local branches (not just watson/*) and find prototype/* or other naming conventions
3. Re-run UAT Test 7 (scaffold offer): Switch to a branch with missing blueprint — Watson should present "Yes, create blueprint" / "Cancel" options
</verification>

<success_criteria>
- loupe.md Phase -1 Step 5 derives targetFilePath from blueprintPath automatically
- watson-init.md Phase 0C url and slug handlers search all git branches before filesystem
- watson-init.md Branch List health check presents concrete AskUserQuestion scaffold offer
- No existing Phase 0-5 (loupe) or Phase 0B/0C (watson-init) logic is broken by these changes
</success_criteria>

<output>
After completion, create `.planning/phases/19-standalone-commands-flexible-entry/19-03-SUMMARY.md`
</output>
