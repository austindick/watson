---
phase: 28-think-skill
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/think/SKILL.md
  - skills/think/references/questioning-flow.md
  - skills/think/references/blueprint-writing.md
  - skills/think/references/mid-build.md
autonomous: true
requirements: [THINK-01, THINK-02, THINK-03, THINK-04, THINK-05, THINK-06]

must_haves:
  truths:
    - "User can run /think with no prior session and get standalone design discussion grounded in library books"
    - "Simple requests skip deep exploration; complex requests get multi-step thinking"
    - "Decisions written to PRD (CONTEXT.md) with [PENDING]/[COMMITTED] amendment tracking in LAYOUT/DESIGN/INTERACTION files"
    - "SKILL.md is under 100 lines with detailed behavior in reference files"
    - "When discussion reaches build-readiness, /think suggests /design as a natural handoff"
    - "Library books (design-system, playground-conventions) are consulted for all component recommendations"
  artifacts:
    - path: "skills/think/SKILL.md"
      provides: "Standalone /think skill — gate, activation, standalone setup, library loading, complexity scaling, routing to reference files, constraints"
      min_lines: 70
    - path: "skills/think/references/questioning-flow.md"
      provides: "AskUserQuestion discipline, gentle challenges, core questions, contextual questions, pattern research — the conversation engine"
      min_lines: 120
    - path: "skills/think/references/blueprint-writing.md"
      provides: "CONTEXT.md writes, surgical amendments, discuss-only build path, reference inventory, ready-to-build detection, return status schema, standalone chain handling, dedup contract"
      min_lines: 200
    - path: "skills/think/references/mid-build.md"
      provides: "Mid-build adaptive behavior, summary and confirmation, hybrid intent detection"
      min_lines: 40
  key_links:
    - from: "skills/think/SKILL.md"
      to: "skills/think/references/questioning-flow.md"
      via: "@references/questioning-flow.md dispatch"
      pattern: "@references/questioning-flow"
    - from: "skills/think/SKILL.md"
      to: "skills/think/references/blueprint-writing.md"
      via: "@references/blueprint-writing.md dispatch"
      pattern: "@references/blueprint-writing"
    - from: "skills/think/SKILL.md"
      to: "skills/think/references/mid-build.md"
      via: "@references/mid-build.md dispatch"
      pattern: "@references/mid-build"
    - from: "skills/think/SKILL.md"
      to: "library/LIBRARY.md"
      via: "Library loading in Phase 0"
      pattern: "LIBRARY.md|library"
    - from: "skills/think/references/blueprint-writing.md"
      to: "shared/references/artifact-schemas/"
      via: "Blueprint write schemas"
      pattern: "CONTEXT.md|LAYOUT.md|DESIGN.md|INTERACTION.md"
---

<objective>
Write the full /think skill — replacing the Phase 26 stub with a standalone design thinking partner. Extract the 713-line discuss.md into a ~100-line SKILL.md with 3 reference files that contain the detailed questioning, blueprint writing, and mid-build behavior.

Purpose: /think is the design conversation partner for the Design Toolkit. After this plan, users can run /think to explore design decisions grounded in library books, with all decisions persisted to the PRD, independently of /play or /design.

Output: 4 files — skills/think/SKILL.md (main skill), skills/think/references/questioning-flow.md (conversation engine), skills/think/references/blueprint-writing.md (persistence and handoff), skills/think/references/mid-build.md (mid-build behavior)
</objective>

<execution_context>
@/Users/austindick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austindick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-play-skill/27-01-SUMMARY.md

@skills/core/skills/discuss.md
@skills/think/SKILL.md
@skills/core/SKILL.md
@shared/references/blueprint-contract.md
@shared/references/artifact-schemas/CONTEXT.md
@shared/references/artifact-schemas/STATUS.md
@library/LIBRARY.md

<interfaces>
<!-- From plugin.json — /think resolves to this file: -->
commands: ["./skills/think/SKILL.md", ...]

<!-- From shared/references/blueprint-contract.md — cross-skill rules: -->
- Always use Edit tool for blueprint writes (never Write except scaffold)
- Amendment protocol: [PENDING] / [COMMITTED] lifecycle markers
- STATUS.md uses YAML frontmatter as machine-readable state
- CONTEXT.md sections: overwrite semantics (latest discuss session replaces)
- LAYOUT.md / DESIGN.md / INTERACTION.md: "## Discuss Amendments" is append-only

<!-- From library/LIBRARY.md — book loading protocol: -->
- playground-conventions: load immediately on activation
- design-system: load on-demand when touching components/tokens/variants
- codebase-map: load only in describeOnly mode for hybrid detection

<!-- Current discuss.md structure being extracted (713 lines): -->
Phase -1: Standalone Setup (blueprint detection, conditional activation)
Phase 0: Library Loading (LIBRARY.md, playground-conventions, on-demand design-system)
Phase 1: Blueprint State Reading (template vs populated detection)
Phase 2: Complexity Scaling (simple/complex/ambiguous routing)
Phases 3-7: Conversation engine (questions, challenges, research)
Phase 8: Mid-Build Adaptive Behavior
Phase 9: Summary and Confirmation
Blueprint Write sections (CONTEXT.md, surgical amendments, discuss-only build path)
Reference Inventory, Ready-to-Build Detection, Return Status, Standalone Chain, Dedup Contract

<!-- From /play SKILL.md — suggest-not-dispatch pattern: -->
/think suggests /design after discussion, does not dispatch it
Standalone invocations end with "Ready to build -- run /design to start."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /think reference files from discuss.md</name>
  <files>skills/think/references/questioning-flow.md, skills/think/references/blueprint-writing.md, skills/think/references/mid-build.md</files>
  <action>
Create the `skills/think/references/` directory.

Port content from `skills/core/skills/discuss.md` into 3 reference files. Each reference file is a self-contained instruction set — NOT an agent, NOT a standalone skill. They are loaded by /think's SKILL.md via `@references/` paths.

**questioning-flow.md** (~140 lines) — Port from discuss.md Phases 3-7:

Add frontmatter:
```yaml
---
name: questioning-flow
type: reference
description: "Conversation engine for /think — question discipline, gentle challenges, core questions, contextual questions, pattern research"
---
```

Add a title: `# Reference: Questioning Flow`

Port these sections verbatim from discuss.md with minimal changes:
1. **Phase 3: AskUserQuestion Discipline** (discuss.md lines 152-169) — copy as-is
2. **Phase 4: Gentle Challenges** (discuss.md lines 171-194) — copy as-is
3. **Phase 5: Core Questions** (discuss.md lines 196-243) — copy as-is. Library grounding rules stay.
4. **Phase 6: Contextual Questions** (discuss.md lines 245-292) — copy as-is. Library grounding rules stay.
5. **Phase 7: Pattern Research** (discuss.md lines 294-326) — copy as-is.

Changes from discuss.md:
- No changes to the question content — these are proven conversation patterns
- Keep all AskUserQuestion format examples
- Keep all library grounding instructions (component names from books, never improvise)
- Keep the DS state override challenge in Phase 5.3
- Ensure no Watson branding (should be clean from Phase 26)

**blueprint-writing.md** (~280 lines) — Port from discuss.md Blueprint Write sections, Reference Inventory, Ready-to-Build, Return Status, Standalone Chain, Dedup Contract:

Add frontmatter:
```yaml
---
name: blueprint-writing
type: reference
description: "Blueprint persistence and handoff for /think — CONTEXT.md writes, surgical amendments, discuss-only build path, return status, dedup contract"
---
```

Add a title: `# Reference: Blueprint Writing and Handoff`

Port these sections from discuss.md:
1. **Blueprint Write: CONTEXT.md** (discuss.md lines 382-400) — copy as-is
2. **Blueprint Write: Surgical Amendments** (discuss.md lines 402-457) — copy as-is. Keep the same-property update-in-place rule, STATUS.md drafts update, amendment removal rules.
3. **Discuss-Only Build Path** (discuss.md lines 459-497) — copy as-is. Keep hybrid detection logic, anti-patterns.
4. **Reference Inventory** (discuss.md lines 499-520) — copy as-is. Keep Figma URL association and background fetch logic.
5. **"Ready to Build?" Detection** (discuss.md lines 522-568) — copy as-is. Keep pending amendment diff, commit-all sequence.
6. **Loupe Handoff: Return Status** (discuss.md lines 570-666) — copy as-is. Keep full JSON schema, all status values, section schema. IMPORTANT: "Loupe Handoff" becomes "Build Handoff" or "Return Status" — the return status is now consumed by the core SKILL.md Discuss->Build Chain, not by /think itself.
7. **Standalone Chain Handling** (discuss.md lines 668-676) — update the text: When /think was invoked standalone and returns `ready_for_build`, display "Ready to build -- run `/design` to start." Do NOT dispatch loupe or /design. The suggest-not-dispatch pattern applies (same as /play).
8. **Dedup Contract** (discuss.md lines 678-700) — copy as-is. Keep mid-build pending model.

Changes from discuss.md:
- Section title "Loupe Handoff: Return Status" rename to "Return Status" (loupe is an internal name)
- Standalone chain message updated to suggest /design (not loupe)
- All "discuss" references in user-facing context become "design discussion" or are rephrased for /think identity
- No changes to any JSON schemas, amendment protocols, or write rules

**mid-build.md** (~60 lines) — Port from discuss.md Phases 8-9:

Add frontmatter:
```yaml
---
name: mid-build
type: reference
description: "Mid-build behavior for /think — adaptive re-entry, hybrid intent detection, summary and confirmation"
---
```

Add a title: `# Reference: Mid-Build Behavior`

Port these sections from discuss.md:
1. **Phase 8: Mid-Build Adaptive Behavior** (discuss.md lines 328-352) — copy as-is. Keep hybrid intent detection, mid-build question reduction.
2. **Phase 9: Summary and Confirmation** (discuss.md lines 354-380) — copy as-is. Keep the summary format, "Look good? Ready to build?" gate.

Changes from discuss.md:
- After mid-build conclude, the "rebuild now" option suggests running /design instead of dispatching loupe directly. Use the suggest-not-dispatch pattern: "Run /design to rebuild the affected section(s)." vs "Save for later."
- Ensure no Watson branding

All three files: ensure no references to `@utilities/`, `@skills/discuss.md`, or `@skills/loupe.md`. All cross-references within /think use `@references/` paths. References to shared resources use `@shared/` paths. Library references use `${CLAUDE_PLUGIN_ROOT}/library/` paths.
  </action>
  <verify>
    <automated>test -f skills/think/references/questioning-flow.md && test -f skills/think/references/blueprint-writing.md && test -f skills/think/references/mid-build.md && QF_LINES=$(wc -l < skills/think/references/questioning-flow.md) && BW_LINES=$(wc -l < skills/think/references/blueprint-writing.md) && MB_LINES=$(wc -l < skills/think/references/mid-build.md) && test "$QF_LINES" -gt 100 && test "$BW_LINES" -gt 180 && test "$MB_LINES" -gt 35 && grep -q "AskUserQuestion" skills/think/references/questioning-flow.md && grep -q "Core Questions" skills/think/references/questioning-flow.md && grep -q "CONTEXT.md" skills/think/references/blueprint-writing.md && grep -q "Surgical Amendments\|Discuss Amendments" skills/think/references/blueprint-writing.md && grep -q "Return Status\|return status" skills/think/references/blueprint-writing.md && grep -q "Mid-Build" skills/think/references/mid-build.md && ! grep -rqi "watson" skills/think/references/ && ! grep -q "@utilities/" skills/think/references/questioning-flow.md && ! grep -q "@skills/discuss" skills/think/references/blueprint-writing.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - questioning-flow.md exists with AskUserQuestion discipline, gentle challenges, core questions, contextual questions, pattern research (120+ lines)
    - blueprint-writing.md exists with CONTEXT.md write, surgical amendments, discuss-only build path, reference inventory, ready-to-build detection, return status schema, standalone chain, dedup contract (200+ lines)
    - mid-build.md exists with mid-build adaptive behavior and summary/confirmation (40+ lines)
    - All library grounding rules preserved from discuss.md
    - No Watson branding, no @utilities/ paths, no @skills/discuss.md references
    - Standalone chain suggests /design (not loupe dispatch)
  </done>
</task>

<task type="auto">
  <name>Task 2: Write full /think SKILL.md replacing the stub</name>
  <files>skills/think/SKILL.md</files>
  <action>
Replace the current 12-line stub with the full /think design thinking skill. This is the orchestration layer that loads reference files for detailed behavior.

**Structure of the new SKILL.md** (target: 80-100 lines):

**Frontmatter** (keep from stub):
```yaml
---
name: think
description: "Design thinking partner grounded in library books -- explore layout, components, and interactions before building. Use when exploring design decisions or discussing prototype approach."
---
```

**Section 1: Header + Purpose** (3-5 lines)
- "# /think"
- "Standalone design discussion skill for the Design Toolkit. Explore layout, components, and interactions grounded in library books (design-system, playground-conventions). Persists all decisions to the PRD."
- "**This file is the orchestration layer.** Detailed behavior lives in @references/ files."

**Section 2: Inputs** (port from discuss.md lines 18-31)
- Table with blueprintPath, figmaUrl, describeOnly parameters
- describeOnly mode note (brief — details in reference files)

**Section 3: Phase -1: Standalone Setup** (port from discuss.md lines 33-54)
- Detection: if blueprintPath not provided, this is standalone
- Step 1: Detect blueprint directory (find pattern, walk parents, dt/* branch check, scaffold offer)
- Step 2: Conditional activation (dt/* branch writes dt-active.json, non-dt skips)
- Step 3: Proceed to On Activation

**Section 4: On Activation** (port from discuss.md lines 56-72)
- Output header: "Design Toolkit > Design Discussion"
- Append action to dt-active.json state file

**Section 5: Phase 0: Library Loading** (port from discuss.md lines 74-96)
- Read LIBRARY.md
- Load playground-conventions immediately
- Load design-system on-demand
- Conditional codebase-map loading for describeOnly mode
- Never improvise component names rule

**Section 6: Phase 1: Blueprint State Reading** (port from discuss.md lines 98-122)
- Read all four blueprint files
- Detect template vs populated content
- Categorize: fresh start / prior context / mid-build
- Adapt opening based on state

**Section 7: Phase 2: Complexity Scaling** (port from discuss.md lines 124-150)
- Clearly simple: skip discussion, minimal grounding, write CONTEXT.md, return status
- Clearly complex: proceed to full conversation via @references/questioning-flow.md
- Ambiguous: AskUserQuestion to choose
- describeOnly mode variants for each path
- Mid-build modifier note

**Section 8: Conversation Flow** (brief — dispatches to references)
- "For full conversation: load @references/questioning-flow.md — contains AskUserQuestion discipline, gentle challenges, core questions, contextual questions, and pattern research."
- "For mid-build re-entry: load @references/mid-build.md — contains hybrid intent detection, question reduction, and summary/confirmation."
- "For blueprint writes and handoff: load @references/blueprint-writing.md — contains CONTEXT.md write rules, surgical amendments, discuss-only build path, reference inventory, ready-to-build detection, and return status schema."

**Section 9: Handoff** (THINK-06 — 3-5 lines)
- "When discussion reaches build-readiness and user confirms 'Let's build':
  - If standalone invocation (Phase -1 ran): display 'Ready to build -- run /design to start.' Do NOT dispatch /design.
  - If dispatched by core SKILL.md: return status JSON to core. Core handles the discuss -> build chain."
- Natural suggestion, not a gate.

**Section 10: Constraints** (5-7 lines)
- This file must stay under 100 lines
- /think does NOT dispatch /design, loupe, or any agent — it returns status or suggests /design
- After standalone invocation, user's next message is handled by the ambient core skill for routing
- No MCP calls, no agent dispatches — only @references/ file dispatches
- All component names from library books. Never improvise.
- All blueprint writes use Edit tool (never Write except scaffold), per blueprint contract @shared/references/blueprint-contract.md

**KEY POINTS:**
- Do NOT include the full questioning flow inline — that's in questioning-flow.md
- Do NOT include blueprint write rules inline — that's in blueprint-writing.md
- Do NOT include the return status JSON schema inline — that's in blueprint-writing.md
- DO reference all 3 reference files via @references/ paths
- DO include Phase -1 standalone setup (THINK-01 requirement)
- DO include library loading (THINK-02 requirement)
- DO include complexity scaling (THINK-03 requirement)
- Mention that CONTEXT.md is the PRD — the living context document where decisions are persisted (THINK-04)
  </action>
  <verify>
    <automated>test -f skills/think/SKILL.md && LINES=$(wc -l < skills/think/SKILL.md) && test "$LINES" -gt 65 && test "$LINES" -lt 110 && grep -q "@references/questioning-flow" skills/think/SKILL.md && grep -q "@references/blueprint-writing" skills/think/SKILL.md && grep -q "@references/mid-build" skills/think/SKILL.md && grep -q "Standalone Setup\|standalone" skills/think/SKILL.md && grep -q "Library Loading\|LIBRARY.md\|library" skills/think/SKILL.md && grep -q "Complexity Scaling\|complexity" skills/think/SKILL.md && grep -q "/design" skills/think/SKILL.md && ! grep -qi "watson" skills/think/SKILL.md && ! grep -q "@skills/discuss" skills/think/SKILL.md && ! grep -q "## Discuss Amendments\|AskUserQuestion Discipline\|Gentle Challenge\|Core Questions\|Contextual Questions\|Pattern Research\|Return Status" skills/think/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - skills/think/SKILL.md is a complete orchestration skill (70-100 lines) with frontmatter
    - Contains: standalone setup (Phase -1), library loading (Phase 0), blueprint state reading (Phase 1), complexity scaling (Phase 2), reference file dispatches, handoff suggestion, constraints
    - Does NOT contain inline questioning flow, blueprint write rules, return status schema, or mid-build behavior (those are in reference files)
    - References all 3 @references/ files
    - Suggests /design on build-readiness (not dispatch)
    - No Watson branding, no @skills/discuss.md references
  </done>
</task>

</tasks>

<verification>
1. `skills/think/SKILL.md` is a complete orchestration skill (70-100 lines) with standalone setup, library loading, complexity scaling
2. `skills/think/references/questioning-flow.md` contains the full conversation engine (120+ lines)
3. `skills/think/references/blueprint-writing.md` contains all persistence and handoff logic (200+ lines)
4. `skills/think/references/mid-build.md` contains mid-build behavior and summary/confirmation (40+ lines)
5. All @references point to files within skills/think/ scope
6. No inline duplication of reference file content in SKILL.md
7. Standalone invocation suggests /design (not dispatch)
8. No Watson branding in any file
9. All library grounding rules from discuss.md preserved in reference files
10. Amendment protocol ([PENDING]/[COMMITTED]) preserved exactly from discuss.md
</verification>

<success_criteria>
- /think SKILL.md fully replaces the stub with design thinking orchestration
- All 6 THINK requirements addressed: standalone (THINK-01), library grounding (THINK-02), complexity scaling (THINK-03), PRD persistence (THINK-04), file structure (THINK-05), handoff suggestion (THINK-06)
- Reference files are self-contained within skills/think/ scope
- The skill works as a standalone — no dependency on core SKILL.md for design discussion
- Total content across all 4 files covers the full 713 lines of discuss.md functionality
</success_criteria>

<output>
After completion, create `.planning/phases/28-think-skill/28-01-SUMMARY.md`
</output>
