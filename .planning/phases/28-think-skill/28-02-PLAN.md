---
phase: 28-think-skill
plan: "02"
type: execute
wave: 2
depends_on: ["28-01"]
files_modified:
  - skills/core/SKILL.md
autonomous: true
requirements: [THINK-01, THINK-05]

must_haves:
  truths:
    - "Explicit /think invocations in core redirect to the /think skill rather than dispatching discuss.md inline"
    - "Ambient Tier 1 design routing dispatches the /think skill's instruction set, not discuss.md"
    - "No circular references between core and /think — clean separation of concerns"
    - "Discuss -> Build Chain still works (core reads /think return status and routes to /design)"
  artifacts:
    - path: "skills/core/SKILL.md"
      provides: "Updated routing layer — /think redirected, Tier 1 dispatches /think, discuss->build chain references updated"
      min_lines: 130
  key_links:
    - from: "skills/core/SKILL.md"
      to: "skills/think/SKILL.md"
      via: "Tier 1 dispatch and /think redirect"
      pattern: "@skills/think|/think"
    - from: "skills/core/SKILL.md"
      to: "skills/core/skills/loupe.md"
      via: "Discuss -> Build Chain dispatches loupe after /think returns"
      pattern: "@skills/loupe"
---

<objective>
Update core SKILL.md routing to dispatch the /think skill instead of discuss.md — completing the extraction by wiring the new standalone skill into the ambient routing layer.

Purpose: After Plan 01 created the /think skill, the core SKILL.md still dispatches the old discuss.md. This plan updates routing so all design discussion flows through /think, matching the /play redirect pattern established in Phase 27-02.

Output: Updated skills/core/SKILL.md with /think routing
</objective>

<execution_context>
@/Users/austindick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austindick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-think-skill/28-01-SUMMARY.md
@.planning/phases/27-play-skill/27-02-SUMMARY.md

@skills/core/SKILL.md
@skills/think/SKILL.md
@skills/core/skills/discuss.md

<interfaces>
<!-- Current core SKILL.md routing for /think (line 41): -->
- `/think` -> Tier 1 (discuss)

<!-- Current Tier 1 dispatch (line 77-79): -->
Tier 1 (discuss):
Dispatch @skills/discuss.md with:
- blueprintPath, figmaUrl

<!-- Current Discuss -> Build Chain (line 108): -->
After @skills/discuss.md returns, read the return status JSON

<!-- /play redirect pattern from Phase 27-02 (line 9): -->
"Session management is handled by /play directly. Just type /play."

<!-- /think SKILL.md from Plan 01 — contains: -->
- Phase -1 standalone setup
- Phase 0 library loading
- Phases 1-2 blueprint reading and complexity scaling
- Dispatches to @references/ files for conversation, writing, mid-build
- Returns status JSON (same schema as discuss.md)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update core SKILL.md routing to dispatch /think</name>
  <files>skills/core/SKILL.md</files>
  <action>
Update the core SKILL.md routing to dispatch the /think skill instead of discuss.md, matching the /play redirect pattern from Phase 27-02.

**Changes to make (use Edit tool for each):**

1. **Intent Classification shortcut (line 41):**
   Change: `/think` -> Tier 1 (discuss)
   To: `/think` -> Respond: "Design thinking is handled by /think directly. Just type /think." Exit. (Same redirect pattern as /play on line 9)

2. **Tier 1 (discuss) dispatch (lines 77-79):**
   Change:
   ```
   Tier 1 (discuss):
   Dispatch @skills/discuss.md with:
   - blueprintPath, figmaUrl
   ```
   To:
   ```
   Tier 1 (think):
   Dispatch @skills/think/SKILL.md with:
   - blueprintPath: resolved blueprint/ path
   - figmaUrl: if detected in user's message (optional)
   ```
   Note: The dispatch path changes from @skills/discuss.md (a subskill in skills/core/skills/) to @skills/think/SKILL.md (the standalone /think skill). The parameters are identical.

3. **Discuss -> Build Chain header (line 108):**
   Change: `After @skills/discuss.md returns, read the return status JSON:`
   To: `After @skills/think/SKILL.md returns, read the return status JSON:`
   The chain logic (ready_for_build, ready_for_hybrid_build, discussion_only, cancelled) is unchanged — /think returns the exact same status schema.

4. **Skill exclusivity note (line 16):**
   Change: "The discuss subskill handles all design exploration"
   To: "The /think skill handles all design exploration"

5. **Red Flags table (lines 125-131):**
   Update references to "discuss" to "the /think skill" where they appear in user-facing guidance:
   - "design questions go through discuss" -> "design questions go through /think"
   - "route to discuss" -> "route to /think"
   - "the discuss subskill handles all design exploration" -> "the /think skill handles all design exploration"

6. **Tier 3 (ask) options (line 96):**
   Change options text: "Think it through first" / "Just start building" — these are fine as-is (no discuss reference).

7. **Help text (line 102):**
   Verify it already says "Use /think to start a design conversation" — should be correct from prior phases.

8. **Constraints section (line 140):**
   Change: "Subskills (discuss.md, loupe.md) contain all execution logic"
   To: "Skills (/think, /design) and subskills (loupe.md) contain all execution logic"

9. **Default classification note (line 63):**
   Change: "Bare or ambiguous invocation with no signal -> Tier 1 (discuss). Starting a conversation gathers context either way."
   To: "Bare or ambiguous invocation with no signal -> Tier 1 (think). Starting a conversation gathers context either way."

10. **Line 66 (Never answer design questions inline):**
    Change: "it MUST route to Tier 1 (discuss) or Tier 3 (ask)"
    To: "it MUST route to Tier 1 (think) or Tier 3 (ask)"

11. **Note about save-blueprint (line 117):**
    Change: "When discuss is dispatched by save-blueprint, it returns to save-blueprint -- not this chain."
    To: "When /think is dispatched by save-blueprint, it returns to save-blueprint -- not this chain."

Do NOT change:
- The Discuss -> Build Chain logic itself (status handling for ready_for_build, ready_for_hybrid_build, etc.) — the return status schema is identical
- The Tier 2 (build) dispatch to @skills/loupe.md — that stays as-is
- The /save dispatch to @skills/save-blueprint.md — unchanged
- The /play redirect on line 9 — already correct from Phase 27

After edits, the file should remain under 145 lines (currently 142).
  </action>
  <verify>
    <automated>LINES=$(wc -l < skills/core/SKILL.md) && test "$LINES" -lt 150 && grep -q "@skills/think/SKILL.md" skills/core/SKILL.md && grep -q "Tier 1 (think)" skills/core/SKILL.md && ! grep -q "Tier 1 (discuss)" skills/core/SKILL.md && ! grep -q "@skills/discuss.md" skills/core/SKILL.md && grep -q "@skills/loupe.md" skills/core/SKILL.md && grep -q "ready_for_build" skills/core/SKILL.md && grep -q "/think" skills/core/SKILL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - core SKILL.md dispatches @skills/think/SKILL.md for Tier 1 (not @skills/discuss.md)
    - Explicit /think invocations redirect to /think skill (same pattern as /play)
    - Discuss -> Build Chain references updated to @skills/think/SKILL.md
    - All "discuss" terminology in routing context updated to "/think"
    - Tier 2 (build) and /save dispatches unchanged
    - File remains under 145 lines
    - No references to @skills/discuss.md remain
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify /think and core separation is clean</name>
  <files></files>
  <action>
Verify the separation between /think and core is clean — no circular references, no duplication, no broken paths.

Run these verification checks:

1. **No @skills/discuss.md references in core SKILL.md:**
   `grep -c "@skills/discuss" skills/core/SKILL.md` should return 0

2. **No circular references — /think does not reference core SKILL.md:**
   `grep -r "@skills/core/SKILL.md" skills/think/` should return nothing

3. **/think references only @references/ paths (not @skills/ paths except shared):**
   `grep -r "@skills/" skills/think/ | grep -v "@skills/core/references/watson-banner"` should return nothing (the banner reference is an approved exception per Phase 27)

4. **No duplication — SKILL.md does not contain inline questioning flow or blueprint write rules:**
   `grep -c "AskUserQuestion Discipline\|Gentle Challenge\|Core Questions\|Contextual Questions\|Pattern Research\|Surgical Amendments\|Return Status\|ready_for_build.*ready_for_hybrid" skills/think/SKILL.md` should return 0

5. **All /think reference files exist and are non-empty:**
   `test -s skills/think/references/questioning-flow.md && test -s skills/think/references/blueprint-writing.md && test -s skills/think/references/mid-build.md`

6. **No Watson branding in any /think file:**
   `grep -rli "watson" skills/think/ | grep -v "watson-banner"` should return nothing

7. **Return status schema exists in blueprint-writing.md (not lost in extraction):**
   `grep -c "ready_for_build\|ready_for_hybrid_build\|discussion_only\|cancelled" skills/think/references/blueprint-writing.md` should be >= 4

8. **Core SKILL.md Discuss -> Build Chain still references all 4 status values:**
   `grep -c "ready_for_build\|ready_for_hybrid_build\|discussion_only\|cancelled" skills/core/SKILL.md` should be >= 4

If any check fails: diagnose the issue and fix it using Edit tool. Then re-run the failing check.
  </action>
  <verify>
    <automated>! grep -q "@skills/discuss" skills/core/SKILL.md && ! grep -r "@skills/core/SKILL.md" skills/think/ 2>/dev/null | grep -q . && test -s skills/think/references/questioning-flow.md && test -s skills/think/references/blueprint-writing.md && test -s skills/think/references/mid-build.md && STATUSES=$(grep -c "ready_for_build\|ready_for_hybrid_build\|discussion_only\|cancelled" skills/core/SKILL.md) && test "$STATUSES" -ge 4 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - No @skills/discuss.md references in core SKILL.md
    - No circular references between /think and core
    - /think uses only @references/ paths (no @skills/ cross-references except approved banner)
    - No content duplication between SKILL.md and reference files
    - All reference files exist and are non-empty
    - Return status schema preserved in both /think references and core chain
    - No Watson branding
  </done>
</task>

</tasks>

<verification>
1. `skills/core/SKILL.md` dispatches @skills/think/SKILL.md for Tier 1 routing (not @skills/discuss.md)
2. Explicit /think invocations redirect to /think skill (redirect pattern, not inline handling)
3. Discuss -> Build Chain reads return status from @skills/think/SKILL.md
4. No @skills/discuss.md references remain in core SKILL.md
5. No circular references between /think and core
6. All 4 return status values (ready_for_build, ready_for_hybrid_build, discussion_only, cancelled) preserved in both core chain and /think reference files
7. Core SKILL.md remains under 145 lines
</verification>

<success_criteria>
- Core SKILL.md routes all design discussion through /think skill (not discuss.md)
- The /play redirect pattern is applied consistently for /think
- The discuss -> build chain works identically (same status values, same routing)
- Clean separation: /think owns conversation logic, core owns routing logic
</success_criteria>

<output>
After completion, create `.planning/phases/28-think-skill/28-02-SUMMARY.md`
</output>
