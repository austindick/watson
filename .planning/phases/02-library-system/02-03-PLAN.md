---
phase: 02-library-system
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - ~/.claude/skills/watson/library/design-system/BOOK.md
  - ~/.claude/skills/watson/library/design-system/global-theme/CHAPTER.md
  - ~/.claude/skills/watson/library/design-system/components/CHAPTER.md
  - ~/.claude/skills/watson/library/design-system/components/button/PAGE.md
  - ~/.claude/skills/watson/library/LIBRARY.md
autonomous: true
requirements: [LIB-02, LIB-03, LIB-08]

must_haves:
  truths:
    - "A design system book exists at library/design-system/ with BOOK.md index, at least two chapters (global-theme, components), and at least one component page"
    - "The book uses Book/Chapter/Page hierarchy — BOOK.md is an index only, chapters contain content or page manifests, pages contain granular component data"
    - "The book is source-agnostic in structure — swapping FauxDS source for a different design system and re-running Librarian produces a different book with the same schema"
    - "Agents reading the design system book get component props, variants, states, tokens, and typography without ever touching source files"
  artifacts:
    - path: "~/.claude/skills/watson/library/design-system/BOOK.md"
      provides: "Design system book index with chapter manifest"
      contains: "type: book"
    - path: "~/.claude/skills/watson/library/design-system/global-theme/CHAPTER.md"
      provides: "Global theme chapter with CSS tokens, spacing, typography"
      contains: "type: chapter"
    - path: "~/.claude/skills/watson/library/design-system/components/CHAPTER.md"
      provides: "Components chapter with page manifest"
      contains: "type: chapter"
  key_links:
    - from: "~/.claude/skills/watson/library/design-system/BOOK.md"
      to: "~/.claude/skills/watson/library/design-system/global-theme/CHAPTER.md"
      via: "chapters[] manifest in BOOK.md frontmatter"
      pattern: "global-theme/CHAPTER.md"
    - from: "~/.claude/skills/watson/library/design-system/BOOK.md"
      to: "~/.claude/skills/watson/library/design-system/components/CHAPTER.md"
      via: "chapters[] manifest in BOOK.md frontmatter"
      pattern: "components/"
    - from: "~/.claude/skills/watson/library/LIBRARY.md"
      to: "~/.claude/skills/watson/library/design-system/BOOK.md"
      via: "Book entry in LIBRARY.md"
      pattern: "library/design-system/BOOK.md"
---

<objective>
Generate the design system book from FauxDS source files, producing a complete Book/Chapter/Page hierarchy that agents can read instead of raw source material.

Purpose: This is the first real library book — it proves the Librarian generate mode works end-to-end and gives all downstream agents (layout, design, builder, reviewer) a structured reference for component props, design tokens, and typography. Source-agnosticism is verified by the schema structure: the book contains FauxDS data but the format is generic.
Output: `library/design-system/` directory with BOOK.md, chapters, and component pages. LIBRARY.md updated with the new book entry.
</objective>

<execution_context>
@/Users/austindick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austindick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-library-system/02-01-SUMMARY.md

<interfaces>
<!-- Book schema all output must conform to -->

From ~/.claude/skills/watson/references/book-schema.md:
```yaml
# BOOK.md
---
type: book
title: string
last_updated: string
source_paths: string[]
chapters:
  - id: string
    path: string
    summary: string
use_when: string
source_hash: string
---

# CHAPTER.md (with pages)
---
type: chapter
title: string
last_updated: string
pages:
  - id: string
    path: string
---

# CHAPTER.md (without pages — inline content)
---
type: chapter
title: string
last_updated: string
---

# PAGE.md
---
type: page
title: string
last_updated: string
---
```

From ~/.claude/skills/watson/references/agent-contract.md:
- Librarian params: mode (generate), sourcePaths (string[]), outputBookPath (string)
- Shared: blueprintPath, libraryPath, watsonMode

From ~/.claude/skills/loupe/references/librarian-output-schema.md (output format reference):
- Section 1: Global Theme (CSS variables grouped by category)
- Section 2: Components (one entry per component: Import, Variants, Sizes, Props, States, Gotchas, Usage)
- Section 3: Design Tokens (tables: Colors, Spacing, Radii, Shadows, Z-Index)
- Section 4: Typography Presets (table: Preset, Font, Size, Weight, Line Height)
- Section 5: Icons (Built-in + External)
- Section 6: Common Patterns (code snippets)

FauxDS source locations (from playground-conventions.md):
- Components: packages/design/prototype-playground/src/components/design-system/
- Tokens/Styles: packages/design/prototype-playground/src/styles/
- Icons: packages/design/prototype-playground/src/components/icons/SlateIcons.tsx
- Slate tokens: packages/design/prototype-playground/tokens/
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Librarian generate mode to produce design system book</name>
  <files>
~/.claude/skills/watson/library/design-system/BOOK.md
~/.claude/skills/watson/library/design-system/global-theme/CHAPTER.md
~/.claude/skills/watson/library/design-system/components/CHAPTER.md
~/.claude/skills/watson/library/design-system/components/button/PAGE.md
  </files>
  <action>
Invoke the Librarian agent in generate mode to scan FauxDS source files and produce the design system book. The Librarian agent file (`agents/librarian.md`) contains the full instructions — follow them.

**Librarian invocation parameters:**
- `mode`: generate
- `sourcePaths`:
  - `packages/design/prototype-playground/src/components/design-system/`
  - `packages/design/prototype-playground/src/styles/`
  - `packages/design/prototype-playground/tokens/`
  - `packages/design/prototype-playground/src/components/icons/SlateIcons.tsx`
- `outputBookPath`: `~/.claude/skills/watson/library/design-system/`
- `libraryPath`: `~/.claude/skills/watson/library/`

**Expected output structure:**

```
library/design-system/
  BOOK.md                    # Index: title "Design System", chapter manifest, use_when, source_hash
  global-theme/
    CHAPTER.md               # Inline content: CSS custom properties, spacing scale, radii, shadows, z-index, typography presets
  components/
    CHAPTER.md               # Page manifest: one page per FauxDS component
    button/
      PAGE.md                # Props, variants, sizes, states, gotchas, usage
    text-input/
      PAGE.md
    modal/
      PAGE.md
    ... (one PAGE.md per component found in source)
  icons/
    CHAPTER.md               # Inline content: available icons, import path, props
```

**Chapter mapping (from Loupe's output schema to Watson's Book/Chapter/Page):**

| Loupe output-schema section | Watson chapter | Format |
|---|---|---|
| Section 1: Global Theme | global-theme/CHAPTER.md | Inline content (CSS variables, token tables) |
| Section 2: Components | components/CHAPTER.md + component pages | Page manifest in CHAPTER.md, one PAGE.md per component |
| Section 3: Design Tokens | Merged into global-theme/CHAPTER.md | Token tables (Colors, Spacing, Radii, Shadows, Z-Index) |
| Section 4: Typography | Merged into global-theme/CHAPTER.md | Typography presets table |
| Section 5: Icons | icons/CHAPTER.md | Inline content (icon list, import paths) |
| Section 6: Common Patterns | patterns/CHAPTER.md | Inline content (code snippets) |

**Component PAGE.md format** (follows Loupe's output-schema Section 2 entry shape):
```markdown
---
type: page
title: Button Component
last_updated: "2026-03-29"
---

# Button

Import: `import { Button } from "@components/design-system"`
Variants: primary, secondary, ghost
Sizes: sm, md, lg
Props:
  - variant: "primary" | "secondary" | "ghost" (default: "primary")
  - size: "sm" | "md" | "lg" (default: "md")
  - disabled: boolean (default: false)
States: hover (built-in), disabled (opacity 0.5), active (built-in)
Gotchas: [any from source inspection]
Usage: `<Button variant="primary" size="md">Label</Button>`
```

**BOOK.md frontmatter:**
```yaml
---
type: book
title: Design System
last_updated: "2026-03-29"
source_paths:
  - packages/design/prototype-playground/src/components/design-system
  - packages/design/prototype-playground/src/styles
  - packages/design/prototype-playground/tokens
  - packages/design/prototype-playground/src/components/icons/SlateIcons.tsx
chapters:
  - id: global-theme
    path: global-theme/CHAPTER.md
    summary: CSS custom properties, spacing, radii, shadows, typography presets
  - id: components
    path: components/
    summary: Component library — props, variants, states, usage per component
  - id: icons
    path: icons/CHAPTER.md
    summary: Available icons, import paths, props
  - id: patterns
    path: patterns/CHAPTER.md
    summary: Common composition patterns from the codebase
use_when: Agent needs component props, design tokens, typography presets, or icon references
source_hash: sha256:{computed from source}
---
```

**Critical constraints:**
- All output MUST conform to book-schema.md (YAML frontmatter types, required fields)
- BOOK.md is an index ONLY — no content in the book file itself
- Component data is extracted from actual TypeScript source (types, props, exports) — not hallucinated
- Source-agnosticism: the book structure is generic. FauxDS-specific names appear in content (component names, token names) but the schema/format works for any design system
- If a source path doesn't exist or is empty, warn but continue with other paths — do not fail the entire generation

**After book generation, update LIBRARY.md** (per Librarian auto-update step):
- Add the design system book entry to LIBRARY.md
- Update `**Last updated:**` and `**Books:**` count
  </action>
  <verify>
    <automated>test -f ~/.claude/skills/watson/library/design-system/BOOK.md && grep -q "type: book" ~/.claude/skills/watson/library/design-system/BOOK.md && test -f ~/.claude/skills/watson/library/design-system/global-theme/CHAPTER.md && test -f ~/.claude/skills/watson/library/design-system/components/CHAPTER.md && test -d ~/.claude/skills/watson/library/design-system/components/ && echo "PASS: Book structure exists" || echo "FAIL: Missing book files"</automated>
  </verify>
  <done>
- library/design-system/BOOK.md exists with valid frontmatter (type: book, title, chapters, source_hash, use_when)
- global-theme/CHAPTER.md contains CSS tokens, spacing, typography from FauxDS source
- components/CHAPTER.md contains page manifest listing all FauxDS components found
- At least 5 component PAGE.md files exist with props/variants/states extracted from source
- icons/CHAPTER.md lists available icons from SlateIcons.tsx
- LIBRARY.md updated with design system book entry
- All YAML frontmatter conforms to book-schema.md
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify source-agnosticism and agent readability</name>
  <files>~/.claude/skills/watson/library/design-system/BOOK.md</files>
  <action>
Post-generation verification to confirm the design system book meets LIB-03 (source-agnostic structure) and LIB-08 (agents read books, not source).

**Source-agnosticism check (LIB-03):**
1. Read BOOK.md frontmatter — confirm `source_paths` lists actual FauxDS paths (the book knows where it came from), but all other structural elements (type, chapters, use_when format) are generic
2. Confirm CHAPTER.md and PAGE.md files use the generic schema (type: chapter/page in frontmatter, standard heading structure)
3. The test: if you replaced FauxDS source paths with a hypothetical "SlateDS" source and re-ran generate, the output would have different content but identical schema structure

**Agent readability check (LIB-08):**
1. Simulate agent navigation path: LIBRARY.md -> find design system entry -> read BOOK.md -> find components chapter -> read components/CHAPTER.md -> find Button page -> read button/PAGE.md
2. Confirm each step provides enough metadata to navigate to the next without globbing
3. Confirm component PAGE.md contains: import path, props with types, variants, states, usage example — everything an agent needs to generate code

**Fix any issues found:**
- If BOOK.md frontmatter is missing required fields, add them
- If component pages are missing props or usage examples, enhance them
- If LIBRARY.md entry is missing or incomplete, update it
- If navigation chain breaks (e.g., CHAPTER.md doesn't list a page that exists), fix the manifest
  </action>
  <verify>
    <automated>grep -q "type: book" ~/.claude/skills/watson/library/design-system/BOOK.md && grep -q "type: chapter" ~/.claude/skills/watson/library/design-system/global-theme/CHAPTER.md && grep -q "type: chapter" ~/.claude/skills/watson/library/design-system/components/CHAPTER.md && grep -q "design-system" ~/.claude/skills/watson/library/LIBRARY.md && echo "PASS: Schema conformance verified" || echo "FAIL: Schema issues found"</automated>
  </verify>
  <done>
- All frontmatter fields match book-schema.md required fields
- Navigation chain works: LIBRARY.md -> BOOK.md -> CHAPTER.md -> PAGE.md without gaps
- Component pages contain sufficient data for agent code generation (props, types, usage)
- LIBRARY.md entry is complete with path, last_updated, chapters, use_when
  </done>
</task>

</tasks>

<verification>
Plan 02-03 is complete when:
1. library/design-system/ directory tree exists with BOOK.md, chapters, and component pages
2. BOOK.md frontmatter is valid (type: book, all required fields present)
3. global-theme chapter contains real token data from FauxDS source
4. components chapter has page manifest and at least 5 component PAGE.md files
5. LIBRARY.md contains the design system book entry with accurate metadata
6. An agent can navigate LIBRARY.md -> BOOK.md -> CHAPTER.md -> PAGE.md without dead links
</verification>

<success_criteria>
- Design system book is generated from real FauxDS source data, not placeholder content
- Book structure conforms to book-schema.md at every level
- LIBRARY.md is updated with the new book entry
- Agent navigation path works end-to-end without globbing
</success_criteria>

<output>
After completion, create `.planning/phases/02-library-system/02-03-SUMMARY.md`
</output>
