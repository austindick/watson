---
phase: 13-plugin-scaffold-path-portability
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - skills/watson/skills/loupe.md
  - skills/watson/skills/discuss.md
  - skills/watson/references/agent-contract.md
  - skills/watson/docs/maintainer.md
  - skills/watson/docs/architecture.md
  - skills/watson/docs/roadmap.md
  - skills/watson/library/playground-conventions/BOOK.md
autonomous: false
requirements:
  - PLUG-02
  - PLUG-04

must_haves:
  truths:
    - "Zero grep hits for ~/.claude in the skills/ directory"
    - "Agents can read library books via ${CLAUDE_PLUGIN_ROOT}/skills/watson/library/ paths"
    - "All @-dispatch references resolve correctly in plugin context (or are rewritten to absolute paths if broken)"
  artifacts:
    - path: "skills/watson/skills/loupe.md"
      provides: "Loupe subskill with portable library paths"
      contains: "${CLAUDE_PLUGIN_ROOT}/skills/watson/library/"
    - path: "skills/watson/skills/discuss.md"
      provides: "Discuss subskill with portable library path"
      contains: "${CLAUDE_PLUGIN_ROOT}/skills/watson/library/"
    - path: "skills/watson/references/agent-contract.md"
      provides: "Agent contract with portable path example"
      contains: "${CLAUDE_PLUGIN_ROOT}/skills/watson/library/"
    - path: "skills/watson/docs/maintainer.md"
      provides: "Maintainer docs with portable paths"
      contains: "${CLAUDE_PLUGIN_ROOT}/skills/watson/"
  key_links:
    - from: "skills/watson/skills/loupe.md"
      to: "skills/watson/library/LIBRARY.md"
      via: "${CLAUDE_PLUGIN_ROOT} variable substitution"
      pattern: "\\$\\{CLAUDE_PLUGIN_ROOT\\}/skills/watson/library/LIBRARY\\.md"
    - from: "skills/watson/skills/discuss.md"
      to: "skills/watson/library/LIBRARY.md"
      via: "${CLAUDE_PLUGIN_ROOT} variable substitution"
      pattern: "\\$\\{CLAUDE_PLUGIN_ROOT\\}/skills/watson/library/LIBRARY\\.md"
    - from: "skills/watson/skills/loupe.md"
      to: "skills/watson/agents/builder.md"
      via: "@agents/ dispatch convention (or absolute path if broken)"
      pattern: "@agents/builder\\.md|\\$\\{CLAUDE_PLUGIN_ROOT\\}/skills/watson/agents/builder\\.md"
---

<objective>
Replace all hardcoded ~/.claude/skills/watson/ paths with ${CLAUDE_PLUGIN_ROOT}/skills/watson/ across all Watson skill files, then validate that the plugin works end-to-end in a --plugin-dir session.

Purpose: Make Watson fully portable — any user installing the plugin gets working library paths regardless of their home directory. This is the core path portability requirement for plugin distribution.

Output: All 7 files with hardcoded paths updated, zero grep hits for ~/.claude, validated in a live plugin session.
</objective>

<execution_context>
@/Users/austindick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austindick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-plugin-scaffold-path-portability/13-CONTEXT.md
@.planning/phases/13-plugin-scaffold-path-portability/13-RESEARCH.md
@.planning/phases/13-plugin-scaffold-path-portability/13-01-SUMMARY.md

<interfaces>
<!-- Path replacement targets from research. Executor uses these exact locations. -->

From skills/watson/skills/loupe.md (10 replacements):
- Line 37: `~/.claude/skills/watson/library/LIBRARY.md` (LIBRARY.md read instruction)
- Lines 45-53: 9 paths in libraryPaths example array (`~/.claude/skills/watson/library/design-system/...` and `~/.claude/skills/watson/library/playground-conventions/...`)

From skills/watson/skills/discuss.md (1 replacement):
- Line 37: `~/.claude/skills/watson/library/LIBRARY.md` (LIBRARY.md read instruction)

From skills/watson/references/agent-contract.md (1 replacement):
- Line 16: `~/.claude/skills/watson/library/...` in libraryPaths table example

From skills/watson/docs/maintainer.md (5 replacements):
- Line 5: `~/.claude/skills/watson/` (Watson home description)
- Line 11: `~/.claude/skills/watson/library/` (library location)
- Line 52: `~/.claude/skills/watson/library/design-system/` (outputBookPath)
- Line 147: `~/.claude/skills/watson/agents/` (agents location)
- Line 186: `~/.claude/skills/watson/skills/` (subskills location)

From skills/watson/docs/architecture.md (1 replacement):
- Line 261: `~/.claude/skills/watson/` (directory tree header)

From skills/watson/docs/roadmap.md (1 replacement):
- Line 56: `~/.claude/rules/watson-ambient.md` (ambient rule path reference — this references ~/.claude/rules/ NOT ~/.claude/skills/watson/, so it stays as-is since the ambient rule MUST live in the user's personal ~/.claude/rules/ directory)

From skills/watson/library/playground-conventions/BOOK.md (1 replacement):
- Line 8: `~/.claude/skills/watson-lite/references/playground-conventions.md` (source reference — note: watson-lite is a legacy path, update to ${CLAUDE_PLUGIN_ROOT}/skills/watson/references/ if the referenced file exists, otherwise leave as documentation-only source provenance)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace all hardcoded paths with ${CLAUDE_PLUGIN_ROOT}</name>
  <files>skills/watson/skills/loupe.md, skills/watson/skills/discuss.md, skills/watson/references/agent-contract.md, skills/watson/docs/maintainer.md, skills/watson/docs/architecture.md, skills/watson/library/playground-conventions/BOOK.md</files>
  <action>
Perform a targeted find-and-replace across the 7 files listed in the interfaces block above. The replacement pattern is:

**Primary replacement:** `~/.claude/skills/watson/` -> `${CLAUDE_PLUGIN_ROOT}/skills/watson/`

Apply to these files (exact counts in parentheses):
1. `skills/watson/skills/loupe.md` (10 replacements) — LIBRARY.md read path + 9 libraryPaths array entries
2. `skills/watson/skills/discuss.md` (1 replacement) — LIBRARY.md read path
3. `skills/watson/references/agent-contract.md` (1 replacement) — libraryPaths example in table
4. `skills/watson/docs/maintainer.md` (5 replacements) — Watson home, library location, outputBookPath, agents location, subskills location
5. `skills/watson/docs/architecture.md` (1 replacement) — directory tree header
6. `skills/watson/library/playground-conventions/BOOK.md` (1 replacement) — source reference. Note: this file references `~/.claude/skills/watson-lite/references/...` (legacy watson-lite path). Replace with `${CLAUDE_PLUGIN_ROOT}/skills/watson/references/playground-conventions.md` if that file exists in the plugin, otherwise update to reflect accurate source provenance.

**Exceptions — do NOT replace:**
- `skills/watson/docs/roadmap.md` line 56: `~/.claude/rules/watson-ambient.md` — this is correct as-is because the ambient rule genuinely lives in the user's personal ~/.claude/rules/ directory, not in the plugin. Plugin cannot bundle rules/.
- Any `/tmp/watson-*.json` paths — these are correct as-is (system temp directory, works across all users).

**After replacement, run verification:**
```bash
grep -rn "~/.claude/skills/watson" ~/watson/skills/
```
Expected output: zero hits.

Also run the broader check:
```bash
grep -rn "~/.claude" ~/watson/skills/ ~/watson/.claude-plugin/
```
Expected: only the roadmap.md ambient rule reference (`~/.claude/rules/watson-ambient.md`) should appear. All `~/.claude/skills/watson/` references should be gone.

**Total expected replacements: ~20** (10 + 1 + 1 + 5 + 1 + 1 = 19, plus or minus based on the BOOK.md legacy path).
  </action>
  <verify>
    <automated>count=$(grep -rc "~/.claude/skills/watson" ~/watson/skills/ 2>/dev/null | awk -F: '{s+=$2} END {print s+0}'); echo "Remaining ~/.claude/skills/watson refs: $count"; test "$count" -eq 0 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>
    - Zero occurrences of `~/.claude/skills/watson` in any file under skills/watson/
    - All library paths use ${CLAUDE_PLUGIN_ROOT}/skills/watson/library/ format
    - The only ~/.claude reference remaining is the ambient rule path in roadmap.md (which is correct)
    - ~20 replacements made across 6-7 files
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify Watson works end-to-end as a plugin with portable paths</name>
  <action>Human verifies end-to-end plugin functionality. What was built: All hardcoded paths replaced with ${CLAUDE_PLUGIN_ROOT}/skills/watson/. Watson should now be fully functional as a plugin with portable library access.</action>
  <how-to-verify>
    1. Run `claude --plugin-dir ~/watson` to start a test session with the updated plugin
    2. Type `/watson:watson` to activate Watson
    3. Test @-dispatch: give Watson a Figma URL or design request that triggers the Loupe pipeline — verify agents dispatch correctly (watch for "file not found" errors on @agents/builder.md etc.)
    4. Test library access: during the build, verify agents successfully read library books (no "file not found" on ${CLAUDE_PLUGIN_ROOT}/skills/watson/library/ paths)
    5. If @-dispatch references BREAK (agents not found):
       - Report which specific @references failed
       - Plan executor will add ${CLAUDE_PLUGIN_ROOT}/skills/watson/ prefix to all @agents/, @skills/, @utilities/ references (~15 references across SKILL.md and loupe.md)
    6. If library paths BREAK (file not found on library reads):
       - Report the exact error path
       - Likely indicates a missed replacement or incorrect ${CLAUDE_PLUGIN_ROOT} resolution
    7. Run `claude plugin validate` from ~/watson/ to confirm no structural errors remain

    Expected outcome: Watson fully functional as a plugin — discuss and loupe both work, agents dispatch, library books load.
  </how-to-verify>
  <resume-signal>Type "approved" if Watson works end-to-end as a plugin, or describe what broke (especially @-reference or library failures)</resume-signal>
</task>

</tasks>

<verification>
- `grep -rn "~/.claude/skills/watson" ~/watson/skills/` returns zero hits (PLUG-02)
- Library books load without error in a --plugin-dir session (PLUG-04)
- `claude plugin validate` passes from ~/watson/
- @-dispatch references resolve correctly OR have been rewritten to absolute paths
</verification>

<success_criteria>
- Zero hardcoded ~/.claude/skills/watson/ paths in any plugin file
- Library books (design-system, playground-conventions) accessible to all agents via ${CLAUDE_PLUGIN_ROOT} paths
- Watson fully functional end-to-end in a --plugin-dir test session
- Both discuss and loupe subskills dispatch correctly
</success_criteria>

<output>
After completion, create `.planning/phases/13-plugin-scaffold-path-portability/13-02-SUMMARY.md`
</output>
