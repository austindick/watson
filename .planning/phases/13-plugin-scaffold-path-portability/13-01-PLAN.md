---
phase: 13-plugin-scaffold-path-portability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude-plugin/plugin.json
  - skills/watson/SKILL.md
  - skills/watson/agents/builder.md
  - skills/watson/agents/consolidator.md
  - skills/watson/agents/decomposer.md
  - skills/watson/agents/design.md
  - skills/watson/agents/interaction.md
  - skills/watson/agents/layout.md
  - skills/watson/agents/librarian.md
  - skills/watson/agents/reviewer.md
  - skills/watson/skills/discuss.md
  - skills/watson/skills/loupe.md
  - skills/watson/utilities/watson-init.md
  - skills/watson/references/agent-contract.md
  - skills/watson/library/LIBRARY.md
  - skills/watson/library/design-system/BOOK.md
  - skills/watson/library/playground-conventions/BOOK.md
  - skills/watson/docs/architecture.md
  - skills/watson/docs/maintainer.md
  - skills/watson/docs/roadmap.md
autonomous: false
requirements:
  - PLUG-01
  - PLUG-03
  - PLUG-05

must_haves:
  truths:
    - "plugin.json exists at .claude-plugin/plugin.json with name=watson and version=1.2.0"
    - "All Watson skill files exist under skills/watson/ in the ~/watson repo"
    - "Plugin directory structure matches Claude Code plugin spec (skills/ at root, .claude-plugin/ for manifest)"
  artifacts:
    - path: ".claude-plugin/plugin.json"
      provides: "Plugin manifest with name, version, description, author, repository"
      contains: "\"name\": \"watson\""
    - path: "skills/watson/SKILL.md"
      provides: "Main Watson skill entry point"
      min_lines: 50
    - path: "skills/watson/agents/builder.md"
      provides: "Builder agent (one of 8 agents)"
    - path: "skills/watson/skills/loupe.md"
      provides: "Loupe subskill"
    - path: "skills/watson/skills/discuss.md"
      provides: "Discuss subskill"
    - path: "skills/watson/library/LIBRARY.md"
      provides: "Library index"
  key_links:
    - from: ".claude-plugin/plugin.json"
      to: "skills/watson/SKILL.md"
      via: "Claude Code plugin discovery (name=watson + skills/watson/SKILL.md = /watson:watson)"
      pattern: "\"name\": \"watson\""
---

<objective>
Create the plugin manifest and copy all Watson skill files from ~/.claude/skills/watson/ into the ~/watson repo under skills/watson/.

Purpose: Establish the foundational plugin directory structure so Watson can be loaded with `claude --plugin-dir ~/watson`. This is the packaging migration — no behavioral changes to Watson itself.

Output: .claude-plugin/plugin.json manifest + complete skills/watson/ directory tree mirroring the current ~/.claude/skills/watson/ layout.
</objective>

<execution_context>
@/Users/austindick/.claude/get-shit-done/workflows/execute-plan.md
@/Users/austindick/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-plugin-scaffold-path-portability/13-CONTEXT.md
@.planning/phases/13-plugin-scaffold-path-portability/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plugin.json manifest and copy skill files into plugin structure</name>
  <files>.claude-plugin/plugin.json, skills/watson/ (entire directory tree)</files>
  <action>
1. Create `.claude-plugin/` directory at ~/watson/ root and write `plugin.json`:
   ```json
   {
     "name": "watson",
     "version": "1.2.0",
     "description": "Design discussion and prototype building for the Faire Prototype Playground",
     "author": {
       "name": "Austin Dick",
       "url": "https://github.com/austindick"
     },
     "repository": "https://github.com/austindick/watson"
   }
   ```
   The `name: "watson"` field is critical — it sets the plugin namespace. Combined with `skills/watson/SKILL.md`, this yields `/watson:watson` per user decision.

2. Create `skills/watson/` directory at ~/watson/ root.

3. Copy the entire contents of `~/.claude/skills/watson/` into `~/watson/skills/watson/`:
   ```bash
   cp -r ~/.claude/skills/watson/* ~/watson/skills/watson/
   ```
   This copies: SKILL.md, agents/ (8 agents), skills/ (discuss.md, loupe.md), utilities/ (watson-init.md), references/ (agent-contract.md), library/ (LIBRARY.md + 2 books with chapters/pages), docs/ (architecture.md, maintainer.md, roadmap.md).

4. Verify the copy is complete by comparing directory listings:
   ```bash
   diff <(cd ~/.claude/skills/watson && find . -type f | sort) <(cd ~/watson/skills/watson && find . -type f | sort)
   ```
   Output should be empty (identical file trees).

IMPORTANT: Do NOT modify any file contents in this task. Path replacements happen in Plan 13-02.
IMPORTANT: Do NOT create hooks/ directory or hooks.json — that is Phase 14.
  </action>
  <verify>
    <automated>test -f ~/watson/.claude-plugin/plugin.json && python3 -c "import json; d=json.load(open('$HOME/watson/.claude-plugin/plugin.json')); assert d['name']=='watson'; assert d['version']=='1.2.0'; print('PASS: plugin.json valid')" && diff <(cd ~/.claude/skills/watson && find . -type f | sort) <(cd ~/watson/skills/watson && find . -type f | sort) && echo "PASS: file trees match"</automated>
  </verify>
  <done>
    - .claude-plugin/plugin.json exists with name=watson, version=1.2.0
    - skills/watson/ directory tree is an exact copy of ~/.claude/skills/watson/
    - No files missing or extra compared to source
    - No file contents modified (paths still hardcoded — that is Plan 13-02)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify plugin loads and /watson:watson is invocable</name>
  <action>Human verifies plugin loading. What was built: Plugin manifest created and all Watson skill files copied into plugin directory structure at ~/watson/. The plugin should now be loadable (though paths are still hardcoded and will break library access — that is Plan 13-02).</action>
  <how-to-verify>
    1. Run `claude --plugin-dir ~/watson` to start a test session
    2. Verify Watson loads — you should see `/watson:watson` available as a command (check with `/` and typing `watson`)
    3. Type `/watson:watson` and verify Watson activates (the initial greeting/response should work even though library paths are still hardcoded)
    4. Run `claude plugin validate` from ~/watson/ in a separate terminal to confirm no structural errors
    5. If `/watson:watson` is NOT available: check the plugin.json name field and the skills/watson/SKILL.md path
    6. Exit the test session

    Note: Library book access will fail in this session because paths still point to ~/.claude/skills/watson/. That is expected and fixed in Plan 13-02.
  </how-to-verify>
  <resume-signal>Type "approved" if /watson:watson is invocable and plugin validates, or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `claude plugin validate` returns no errors from ~/watson/
- `/watson:watson` appears in slash command list when loaded with `--plugin-dir`
- File tree under skills/watson/ matches ~/.claude/skills/watson/ exactly
</verification>

<success_criteria>
- plugin.json manifest is valid and loadable
- Watson registers as /watson:watson in a --plugin-dir session
- All skill files are present in the plugin directory
- No hooks.json or hooks/ directory created (Phase 14 scope)
</success_criteria>

<output>
After completion, create `.planning/phases/13-plugin-scaffold-path-portability/13-01-SUMMARY.md`
</output>
